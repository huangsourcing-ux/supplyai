# @chinasupply/schemas

Shared Zod API contracts. M0-T5c establishes the stable G-2
`localizedTextSchema` export and its inferred TypeScript type; the remaining
business API contracts begin in M1-T2.

The package exports TypeScript source directly so Metro and EAS can consume it
through the pnpm workspace without a package prebuild.
