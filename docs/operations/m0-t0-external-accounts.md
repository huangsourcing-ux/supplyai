# M0-T0 external accounts and identifiers

> Status: **In progress**  
> Last verified: 2026-07-22  
> Scope: account ownership, local CLI authentication, stable public identifiers, and human prerequisites only. No production deployment, paid-plan purchase, production migration, or secret material is recorded here.

M0-T0 must remain unchecked in `ChinaSupply.AI开发计划.md` until every required row below is ready or the frozen plan is explicitly amended. The user has chosen to defer Apple Developer Program and Google Play Console enrollment until the app is closer to completion; this is an acknowledged schedule risk rather than a completed prerequisite.

## Confirmed ownership and local access

| Item       | Confirmed state                                                                                                                                                                                                                                                                                                                                                                                                                  | Remaining work / owning task                                                                                                                                                                                                                                            |
| ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| GitHub     | Private repository `huangsourcing-ux/supplyai`; local `origin` is configured; current GitHub user has `ADMIN` permission; `gh` is authenticated.                                                                                                                                                                                                                                                                                 | Keep branch protections and CI permissions for M0-T6.                                                                                                                                                                                                                   |
| Vercel     | CLI authenticated as `huangsourcing-2373`; domain `chinasupply.ai` is present; existing production project is `chinasupply` (`prj_GPIU6YPao7lJH3I7sB3pjJqSG3Rs`) and serves `www.chinasupply.ai`. Separate staging project `chinasupply-web-staging` (`prj_ajGvtzL2BnIMud5BefJXsy9ARsNF`) exists with `apps/web` as its root; encrypted staging values are configured for both Vercel Production and Preview targets.            | Keep this repository linked only to the staging project during M0. M0-T6 owns the permanent CI/CD trigger policy; production cutover remains M5-T9.                                                                                                                     |
| Railway    | CLI authenticated as `huang.sourcing@gmail.com`; project `chinasupply-staging` exists and its PostGIS 17/PostGIS 3.5 service is online with a persistent volume.                                                                                                                                                                                                                                                                 | The trial allowance is accepted for current development and must be monitored. M0-T4 still owns API/Worker/Redis provisioning, health checks, and BullMQ verification; upgrade only when trial continuity or measured usage requires it.                                |
| Cloudflare | Wrangler authenticated to account `0431bd1fc2c8927a1433f273bf43bf5a`; OAuth credentials are encrypted and the encryption key is in macOS Keychain. Public DNS for `chinasupply.ai` uses Cloudflare nameservers `barbara.ns.cloudflare.com` and `sean.ns.cloudflare.com`. The usage-based R2 subscription and `chinasupply-staging` bucket are active; staging credentials are stored only in encrypted deployment configuration. | R2 has a USD 0 base charge but can incur overage. M0-T10 still owns CORS, custom domain/CDN, environment isolation, least-privilege token review, purge token, and trusted-proxy configuration.                                                                         |
| Clerk      | CLI authenticated as `huang.sourcing@gmail.com`; Development application `ChinaSupply.AI Staging` (`app_3Gr7epAgdYw8KEIS7m9t2OH9E6s`) exists and its real test key family is stored only in encrypted Vercel configuration.                                                                                                                                                                                                      | M0-T3 owns Restricted mode, disabled public sign-up, admin session claim, invited admin metadata, staging origin, redirect URL, and access smoke tests. Production instance remains M5-T9.                                                                              |
| Sentry     | Browser account and organization `huangsourcing` exist. Organization token `ChinaSupply.AI CLI` was created with the limited `org:ci` scope, and `sentry-cli` is authenticated through the user-level `~/.sentryclirc` configuration. The least-privilege token intentionally cannot enumerate organization/project inventory.                                                                                                   | The token is outside the repository and must never be copied into source control. M0-T7 owns the authoritative Web/API/Mobile project inventory, DSNs, integration, source maps, release names, and test-event verification.                                            |
| Expo / EAS | EAS CLI authenticated as Expo owner `huangsourcing`; team `huangsourcings-team` is also visible.                                                                                                                                                                                                                                                                                                                                 | M0-T5 owns mobile project creation/linking, version matrix, dev/preview builds, and EAS configuration. Apple/Google store submission credentials are intentionally deferred.                                                                                            |
| PostHog    | Google OAuth sign-in is complete. US organization `ChinaSupply.AI` and its default project (`523616`) exist. No SDK or client key has been added to the repository.                                                                                                                                                                                                                                                              | M3-T6 owns the consent-aware integration and environment configuration. Only the public project key/host may enter client configuration; personal access tokens remain outside the repository.                                                                          |
| MapTiler   | Google OAuth sign-in is complete and the account is active on the Free plan. No project-specific restricted keys have been added to the repository.                                                                                                                                                                                                                                                                              | The frozen stack calls for MapTiler Cloud Flex, so adequate production quota/billing remains a human financial decision. M0-T10 creates separate keys restricted to Web domain, iOS Bundle ID, and Android package. Never reuse an unrestricted key in a client bundle. |

## Product identifiers

| Identifier             | Value                                         | State                                                                                                                                                                                 |
| ---------------------- | --------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Public product name    | `ChinaSupply.AI`                              | Confirmed by the frozen product documents.                                                                                                                                            |
| Production apex domain | `chinasupply.ai`                              | Confirmed present in Vercel; DNS is on Cloudflare.                                                                                                                                    |
| Production Web URL     | `https://www.chinasupply.ai`                  | Existing Vercel production URL; production cutover still belongs to M5-T9.                                                                                                            |
| Staging Web URL        | `https://staging.chinasupply.ai`              | DNS, Vercel staging project, and HTTPS deployment are provisioned; `/` and `/admin` returned HTTP 200 on 2026-07-22.                                                                  |
| iOS Bundle ID          | `ai.chinasupply.app`                          | Proposed candidate awaiting human confirmation; **not reserved** because Apple Developer enrollment is deferred. Recheck availability before M0-T5 device signing and before release. |
| Android package        | `ai.chinasupply.app`                          | Proposed candidate awaiting human confirmation; **not reserved** because Play Console enrollment is deferred. Recheck availability before store registration.                         |
| Expo owner             | `huangsourcing`                               | Confirmed by authenticated EAS CLI.                                                                                                                                                   |
| Custom URL scheme      | `chinasupply`                                 | Proposed candidate awaiting human confirmation for M5-T10 callback verification; V1 does not implement Universal Links/App Links.                                                     |
| Privacy contact        | `privacy@chinasupply.ai`                      | Candidate only; mailbox or forwarding route has not been provisioned. Do not publish until delivery is tested.                                                                        |
| App icon placeholder   | `apps/mobile/assets/app-icon-placeholder.png` | Generated 1024×1024 RGB PNG placeholder. Replace with the final reviewed brand asset before store submission.                                                                         |

## Google OAuth and legal prerequisites

- Google OAuth credentials are not created yet. The Clerk Development application and live staging URL now exist, but credential creation and exact callbacks remain intentionally scoped to M3-T1.
- `/privacy` and `/terms` launch-ready legal copy remains a human deliverable due before M3 starts. F-11.1 implementation cannot substitute generated legal advice for approved copy.
- Apple Developer Program and Google Play Console enrollment are intentionally deferred by the user. This conflicts with the development plan's schedule warning that they should start in M0 because review can be slow; the deferral is recorded here without marking the accounts complete.

## Locally installed account tools

Verified on 2026-07-22:

```text
gh 2.92.0
vercel 56.5.0
railway 5.27.2
wrangler 4.113.0
sentry-cli 3.6.1
eas-cli 21.0.2
clerk 2.2.0
```

PostHog and MapTiler do not require a general-purpose local CLI for this milestone; their remaining setup is performed in their authenticated control panels. No secret, OAuth token, password, DSN, client secret, or API key belongs in this document.
