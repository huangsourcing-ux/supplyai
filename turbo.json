{
  "$schema": "https://turborepo.dev/schema.json",
  "globalEnv": [
    "APP_ENV",
    "CLERK_SECRET_KEY",
    "DATABASE_URL",
    "PORT",
    "REDIS_URL",
    "WEB_ORIGIN",
    "NEXT_PUBLIC_*",
    "PAYLOAD_SECRET",
    "R2_*",
    "SENTRY_AUTH_TOKEN"
  ],
  "ui": "stream",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**", "!.next/cache/**", "build/**", "dist/**"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "lint": {
      "dependsOn": ["^lint"],
      "outputs": []
    },
    "test:unit": {
      "dependsOn": ["^test:unit"],
      "outputs": []
    },
    "typecheck": {
      "dependsOn": ["^typecheck"],
      "outputs": []
    }
  }
}
