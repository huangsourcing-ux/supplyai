# ChinaSupply.AI 开发日志

> 记录规则见 `AGENTS.md` 第 10 节：每个任务包完成时在本文件末尾追加一条，与代码同一提交交付；只追加，不修改历史条目。

<!-- 在此行之下追加日志条目，格式：

## YYYY-MM-DD ｜ M?-T?（任务名）
- 内容：
- 主要文件：
- 验证：
- 遗留/人工项：

-->

## 2026-07-22 ｜ M0-T1（Monorepo）

- 内容：使用 create-turbo 基线建立 pnpm + Turborepo 单仓，落实 apps 与共享 packages 目录，提供统一质量命令和 `packages/config` 的 ESLint、TypeScript、Tailwind 基础预设；为 PRD G-2、G-6 的跨端共享结构建立工程基础，未实现业务功能。
- 主要文件：新增 `package.json`、`pnpm-workspace.yaml`、`turbo.json`、`pnpm-lock.yaml`、`apps/*` 占位目录、`packages/*` 工作区和 `packages/config` 配置、构建与结构测试；初始化本地 `main` Git 仓库。
- 验证：`pnpm lint`、`pnpm typecheck`、`pnpm test:unit`（4/4 通过）、`pnpm build`、`pnpm format:check` 均通过；`pnpm list --recursive --depth -1` 正确识别 6 个共享包。
- 遗留/人工项：M0-T0 仍未完成；当前 Git 仓库没有 GitHub remote，外部账号、标识符、域名及法律文案等人工前置仍需按计划推进。Web/API/Mobile 应用脚手架分别留待 M0-T3/M0-T4/M0-T5。

## 2026-07-22 ｜ M0-T2（环境与配置）

- 内容：落实 Local/Staging/Production 三环境契约，新增 API/Web/Mobile Zod 环境变量校验和完整 `.env.example`，建立 PostGIS 17 + Redis 7.4 本地 Compose，以及 Drizzle 核心表与 Payload 内容表相互独立的 migration release command 和 reusable CI workflow；对应 PRD G-9 与环境、迁移隔离约定。
- 主要文件：新增 `compose.yaml`、`packages/config/env/*`、三个应用的环境示例、`scripts/*` 基础设施/迁移命令、`.github/workflows/release-migrations.yml` 和环境/迁移运维文档；公开行为新增 `infra:*`、`release:migrate:core`、`release:migrate:cms` 命令。
- 验证：`docker compose config --quiet` 通过；`docker compose up -d --wait` 后 PostgreSQL/Redis 均 healthy，`pnpm infra:check` 查询到 PostGIS 3.5 且 Redis 返回 PONG；`docker compose down` 后两个数据 volume 均保留；`pnpm lint`、`pnpm typecheck`、`pnpm test:unit`（14/14 通过）、`pnpm build`、`pnpm format:check` 全部通过；未发现真实密钥。
- 遗留/人工项：M0-T0 外部账号和真实凭据仍未完成；应用启动入口需在 M0-T3/M0-T4/M0-T5 接入对应解析器；真实 Drizzle/Payload migration 分别待应用骨架和 M1-T1 实现。官方 `postgis/postgis:17-3.5` 仅发布 amd64，Apple Silicon 本地开发使用 Docker Desktop 内置模拟，已实测通过。

## 2026-07-22 ｜ M0-T3（Web 骨架）

- 内容：将最小 Next.js 16、React 19、Payload、Tailwind、next-intl 与 Clerk Web 基座落入 `apps/web`，删除模板业务内容；Payload 仅拥有 `cms_users` 及内部表，`/admin` 与 Clerk `/ops/**` 身份边界独立，严格以 `metadata.role = "admin"` 放行运营入口，对应 PRD G-2、G-7、G-9，并为 F-7.2 预留鉴权基础。
- 主要文件：新增 `apps/web` 配置、路由、i18n 文案、Payload collection、初始 CMS migration、鉴权工具与单元测试；更新根级 CMS release command、reusable migration workflow、README 和环境/迁移运维文档。外部行为新增 Railway staging PostGIS、Vercel `chinasupply-web-staging` 项目、Clerk Development 实例、R2 staging bucket 及 `https://staging.chinasupply.ai` HTTPS 部署。
- 验证：`pnpm lint`、`pnpm typecheck`、`pnpm test:unit`（47 项通过）、`pnpm build`、`pnpm format:check` 均通过；Payload types/import map 重生成无漂移；Railway CMS migration 首次成功且再次执行无待迁移，仅生成 `cms_users`/Payload 内部表；Vercel 部署 READY、运行时 error 日志为 0；正式域名 `/`、`/admin`、`/ops/sign-in`、`/ops/forbidden` 返回 200，匿名 `/ops` 307 到登录，非管理员 307 到 forbidden，管理员 Google/Clerk 登录后进入 `/ops`；Let's Encrypt 证书 SAN 为 `staging.chinasupply.ai`。
- 遗留/人工项：M0-T3 验收项无遗留；GitHub 仓库已连接 Vercel，但自动 Git 部署按计划留到 M0-T6，当前不会因 GitHub push 自动更新。普通用户登录、登录回跳和账户功能仍按计划留到 M3；M0-T0 其他外部账号、商店账号及法律文案人工项仍未完成。

## 2026-07-22 ｜ M0-T3（Vercel Preview 环境修复）

- 内容：修复 PR #1 的 Vercel Preview 构建环境，使 PR 验证与受控 staging 部署使用同一套真实 staging 配置；不改变生产环境、不放宽启动时 Zod 校验，对应 M0-T3 环境与部署验收。
- 主要文件：更新 `docs/operations/environments.md`，明确 Vercel `Production`/`Preview` 是同一 staging 项目的部署目标；外部配置将 19 个 staging 变量从 Vercel Production 安全同步到 Preview，并用真实 Clerk Development、Railway PostGIS 与新生成的 Payload secret 替换三项无效值。
- 验证：失败日志最初报告 19 项环境变量缺失；补齐 Preview 范围后收敛到 `CLERK_SECRET_KEY`、`DATABASE_URL`、`PAYLOAD_SECRET` 三项值无效；修正后受控 Vercel Preview redeploy 在 2 分钟内达到 `READY`。变量值未输出、未写入仓库。
- 遗留/人工项：GitHub PR 状态需由本次提交触发的新 Preview deployment 最终复核；M0-T6 仍负责正式 CI/CD 触发策略。

## 2026-07-22 ｜ M0-T4（API + Worker 骨架）

- 内容：建立 NestJS 11 + Fastify HTTP 与 BullMQ Worker 双入口，接入 M0 runtime Zod 环境校验，实现 G-4 成功/失败 envelope、冻结错误码、全局 Zod pipe、无依赖 liveness 与 PG/Redis readiness；新增并验证 `system:ping` 队列作业，对应 PRD G-4、G-6。
- 主要文件：新增 `apps/api` 工程、HTTP 公共层、健康检查、PG/Redis 连接、BullMQ processor/producer、18 项单测与 `railway.json`；更新 API 环境解析、README 和 staging 运维文档。外部行为新增 Railway staging `api`、`worker`、Redis SFO 服务及临时 API 域名；Worker 无公网域名。
- 验证：`pnpm lint`、`pnpm typecheck`、`pnpm test:unit`（66/66 通过）、`pnpm build`、`pnpm format:check` 全部通过；本地 Compose PostGIS/Redis healthy，live/ready 为 200，404 返回 `NOT_FOUND` envelope，独立 producer 完成 `system:ping`；Railway API 健康门部署成功，临时域名 live/ready 为 200，容器内 producer 返回 completed 且独立 Worker 日志记录同一 job，Redis `maxmemory-policy=noeviction`。
- 遗留/人工项：M0-T4 验收项无遗留；Railway 自动 Git 部署留 M0-T6，Sentry 留 M0-T7，Cloudflare staging API 自定义域名、可信代理与真实客户端 IP 留 M0-T10；M0-T0 的商店账号和法律文案等人工项仍未完成。

## 2026-07-22 ｜ M0-T5a（Obytes 入 Monorepo + 版本矩阵 ADR）

- 内容：以 Obytes Starter v9.0.0 为结构基线建立 `@chinasupply/mobile` Expo Development Build 应用壳，删除模板演示认证与业务页面，按冻结技术栈将 Uniwind 替换为 NativeWind；启用 New Architecture，建立 local/staging/production 候选应用标识和英文 i18n 壳，对应 PRD G-2、G-6，并产出移动版本矩阵 ADR。
- 主要文件：新增 `apps/mobile` 的 Expo Router、React Query、Zustand、i18next、MMKV、NativeWind 配置与 7 项单测，新增 `docs/adr/0001-mobile-version-matrix.md`，更新根级 `mobile:check` 和 `pnpm-lock.yaml`；Expo Doctor 要求将 Obytes 声明的 SDK 54 补丁基线对齐到 Expo 54.0.36、Expo Router 6.0.24，未加入 MapLibre、Clerk 或 EAS project ID。
- 验证：`pnpm mobile:check` 通过（Expo Doctor 18/18、TypeScript、Expo public config、iOS/Android export bundle）；`pnpm lint`、`pnpm typecheck`、`pnpm test:unit`（73/73 通过）、`pnpm build`、`pnpm format:check`、`pnpm install --frozen-lockfile` 全部通过；仅存在根目录一个 `pnpm-lock.yaml`，未生成或提交 `ios/`、`android/`。
- 遗留/人工项：M0-T5b 仍需安装 MapLibre RN 11.3.6、完成双端 dev build 及点/Polygon/聚合渲染；M0-T5c 仍需完成 Clerk Expo、共享包导入、EAS monorepo/Preview 构建。候选 Bundle ID/Android package 尚未在商店保留，父任务 M0-T5 保持未勾选。

## 2026-07-22 ｜ M0-T5b（MapLibre 双端构建与地图验证）

- 内容：锁定并接入 MapLibre React Native 11.3.6，在 New Architecture 下以离线 background-only Style 和义乌附近 WGS-84 fixtures 验证点、闭合 Polygon 与聚合点渲染；所有界面字符串走 i18next，并保留 MapTiler/OpenStreetMap 归因，为 PRD G-1、G-2 与 F-1.3 的后续产品实现提供兼容性证据，但未实现业务地图。
- 主要文件：新增 `apps/mobile/src/features/map-spike` 的地图屏幕、Style、GeoJSON fixtures、统一图层配置、加载状态与单测，新增 MapLibre Jest mock；更新 Expo config plugin、移动依赖、锁文件及 `docs/adr/0001-mobile-version-matrix.md`。clean prebuild 解析到 MapLibre Native iOS 6.26.0、Android 13.2.0，生成的 `ios/`、`android/` 保持忽略。
- 验证：`pnpm mobile:check` 通过（Expo Doctor 18/18、TypeScript、public config、iOS/Android export）；`pnpm lint`、`pnpm typecheck`、`pnpm test:unit`（79/79 通过）、`pnpm build`、`pnpm format:check`、`pnpm install --frozen-lockfile` 全部通过。iPhone 17 Pro Simulator（iOS 26.5，Xcode 26.6）原生 build/install/launch 成功；`diaoyouji_api_36`（API 36）Gradle build/install/launch 成功；双端截图均可见点、Polygon、橙色聚合圆、图例、归因及 ready 状态，运行日志无 native crash 或 style/source 加载错误。
- 遗留/人工项：本任务仅证明模拟器双端兼容；物理 iOS/Android 真机、签名、商店标识保留仍是 M0 人工门禁。真实 MapTiler 产品样式归 M0-T10；Clerk Expo、共享包导入与 EAS Preview 归 M0-T5c；父任务 M0-T5 保持未勾选。iOS 存在 React Native `filter` 属性名碰撞引发的非致命诊断，Android 存在 ViewManager setter fallback 警告，需在未来框架升级或真机验证时复查。

## 2026-07-22 ｜ M0-T5c（Clerk Expo、共享包导入与 EAS Preview）

- 内容：接入 Clerk Expo 4.0.1 的邮箱密码与首次设备信任验证码流程，以 SecureStore 中的随机 16 字节密钥加密独立 MMKV token cache；为 `packages/schemas`、`geo`、`i18n` 建立最小 TypeScript 源导出并由 mobile 启动链真实执行，完成 pnpm isolated workspace、Metro 与 EAS 云端解析验证，对应 PRD G-2、G-6。创建并链接 `@huangsourcing/chinasupply-ai`，真实 Android Preview、登录及进程级冷启动 session 恢复全部通过，父任务 M0-T5 已勾选。
- 主要文件：新增 mobile Clerk Provider、登录/设备信任页面、加密 token cache、workspace compatibility 模块、EAS Preview 配置及单测；新增三个共享包的稳定公开接口；更新 mobile env、Expo config、版本矩阵 ADR、移动 README、环境运维文档、开发计划和根锁文件。EAS project ID 为 `cac33d97-75d7-4975-899f-00d661bf979d`，最终 build ID 为 `cf218fc6-750c-4d7c-804b-5082d52e650d`。
- 验证：EAS Android Preview build `FINISHED`，APK 65,288,599 bytes、SHA-256 `4dc18155226bd520d29324c9f254228cb58e02515ac5b52965844d959ce332c3`；安装到 `diaoyouji_api_36` 后无 Metro 完成真实 Clerk 登录和 `424242` device trust，`am force-stop` 后 `LaunchState: COLD` 仍恢复签名会话与 `Offline map ready`，logcat 无 native fatal、React Native、Clerk/session 或 MapLibre style/source 错误。`eas config --platform android --profile preview`、`pnpm mobile:check`（Expo Doctor 18/18、TypeScript、public config、双端 export）、`pnpm lint`、`pnpm typecheck`、`pnpm test:unit`（mobile 25/25）、`pnpm build`、`pnpm format:check`、`pnpm install --frozen-lockfile` 全部通过。
- 遗留/人工项：M0-T5 验收边界已满足，但 M0 出口的物理 iOS/Android 真机地图、商店签名与标识保留仍未满足；OAuth/回跳/账户页留 M3/M4，真实 MapTiler 产品底图留 M0-T10。测试输入法曾短暂显示 smoke 密码，旧值已立即在 Clerk 作废并以新随机值覆盖 macOS Keychain，相关临时截图已删除。

## 2026-07-23 ｜ M0-T6（CI/CD 仓库实现；外部门禁待完成）

- 内容：实现 PR/main 分级 GitHub Actions、Mobile 路径检查、真实 PostGIS/Redis Testcontainers API e2e、CMS staging migration 与 `Staging Release Gate` 依赖链；新增仅 `rc-*`/手动触发的 Android EAS Preview，以及仅 `v*` + EAS 人工批准触发的双端 Production Build/Submit 配置。当前不提前伪造 M1-T1 的 core migration，对应 PRD G-4、G-9 与开发计划 M0-T6。
- 主要文件：新增 `.github/workflows/ci.yml`、API e2e 配置与用例、EAS Preview/Production workflows、`docs/operations/ci-cd.md`；更新 `railway.json` watch paths、迁移边界、环境文档和工作流契约测试。公开命令新增 `pnpm test:e2e`。
- 验证：`pnpm lint`、`pnpm typecheck`、`pnpm test:unit`（95/95）、`pnpm test:e2e`（4/4，真实 `postgis/postgis:17-3.5` + `redis:7.4-alpine`）、`pnpm build`、`pnpm mobile:check`（Expo Doctor 18/18、TypeScript、public config、iOS/Android export）、`pnpm format:check`、`pnpm install --frozen-lockfile` 全部通过；两个 EAS workflow 均通过 `eas workflow:validate`。未触发任何 EAS 云构建或 Submit。
- 遗留/人工项：GitHub 仍为 Free 私有仓库，无法配置计划要求的 Environment secrets、required `CI Gate`/分支保护；因此尚未连接 Railway Git Autodeploy + Wait for CI、尚未启用 Vercel required Deployment Check，也未完成真实 PR/main staging 发布验收，M0-T6 保持未勾选。需先升级 GitHub Pro，且 Apple/Google 商店账号、production EAS 环境与提交凭据仍按 M5-T10 人工前置处理。
- 云端复验：已创建 PR #7；GitHub 接受工作流并生成 `CI Gate` 等 checks，但所有 GitHub-hosted runner 在执行任何 step 前被账户账单状态拦截（GitHub 注解：近期付款失败或 Actions spending limit 需提高）。Vercel Preview 成功，且 PR 未出现 EAS Build/Submit。需先恢复 Actions 额度/账单后才能完成真实 PR CI 验收。

## 2026-07-23 ｜ M0-T6（公开仓库外部接线复验）

- 内容：仓库改为公开后完成 GitHub `staging` Environment、真实 CMS migration、稳定 release gate 与 `main` 分支保护接线；分支保护要求 PR 和 `CI Gate`、对管理员生效并禁止 force push/delete。将 GitHub/Vercel 的 staging 数据库统一到当前 Railway PostGIS public URL，复用 M0-T3 的真实 Payload secret；Railway API/Worker 已连接 `huangsourcing-ux/supplyai:main`，恢复 API `/health/ready` 门禁和两服务 `/railway.json` 配置。
- 主要文件：更新 `docs/operations/ci-cd.md` 的公开仓库能力与外部平台实况；外部配置新增 GitHub Environment secrets/variable 和分支保护，更新 Vercel Production/Preview staging 密钥，并连接 Railway 两项服务。未提交或输出任何密钥。
- 验证：PR #7 的 lint、typecheck、unit、Web/API build、API Testcontainers e2e、Mobile checks 与 `CI Gate` 全绿且未触发 EAS；main Actions run `29986094177` 按 CI → CMS migration → `Staging Release Gate` 全绿。Vercel staging 对应 main commit `1a829e016068bbdf3bfb3aa6350bfe5f79986f3d` 且 `/`、`/admin` 返回 200、`/ops` 正确跳转登录；Railway API/Worker 同一 commit 均为 SUCCESS，`/health/live`、`/health/ready` 成功，SSH 执行 `system:ping` 由 Worker 完成。
- 遗留/人工项：Vercel 当前没有 Deployment Check run，仍需在项目设置中把 `Staging Release Gate` 选为 required；Railway 拒绝创建 deployment trigger，原因是项目成员尚未通过 Railway GitHub App 获得该仓库 contributor access，需人工连接/授权后为 API 和 Worker 启用 Autodeploy + Wait for CI。上述两项完成前 M0-T6 保持未勾选；未创建 tag、未触发 EAS Build/Submit。

## 2026-07-23 ｜ M0-T6（外部门禁完成验收）

- 内容：完成 Vercel required Deployment Check 与 Railway API/Worker GitHub Autodeploy + Wait for CI 接线；修复 Mobile 可选 job 跳过时 reusable CMS migration 被 GitHub 隐式跳过的问题，使 `main` 始终按 CI → CMS migration → `Staging Release Gate` 放行 staging，对应 PRD G-4、G-9 与开发计划 M0-T6。
- 主要文件：更新 `.github/workflows/ci.yml` 与工作流契约测试，补充 `apps/api/README.md`、`docs/operations/ci-cd.md` 并勾选 M0-T6；外部配置新增 Vercel `Staging Release Gate` required Deployment Check，以及 Railway API/Worker 两个 `main` deployment trigger（`checkSuites=true`）。未向 GitHub 写入 Vercel/Railway token，未提交或输出密钥。
- 验证：PR #9 和 #10 的 `CI Gate` 全绿且没有 EAS Build/Submit；main Actions run `29989416236` 成功执行 CMS migration 和 `Staging Release Gate`。Vercel check run `ckr_3fe47e76-31b3-48ab-a14f-74b77d83c605` 为 `succeeded` 且阻断 `deployment-alias`，`staging.chinasupply.ai` 随后切换；Railway API/Worker deployment `33485e90-8d1d-4eac-82c9-85d98e145d84`、`a6e4fcd5-fc66-43d9-bbbf-3a10a170ee0a` 对同一 commit `5a5f7fad9ae07ecb7e376eb458f7c5d002bf1f8f` 均先为 `WAITING`、CI 全绿后转为 `SUCCESS`。`/health/live`、`/health/ready` 返回 200，SSH `system:ping` completed。
- 遗留/人工项：M0-T6 验收项无遗留；未创建 `v*` tag、未执行 EAS Production Build/Submit，Apple/Google 商店凭据与真实商店发布仍按 M5-T10 人工门禁处理。M1-T1 落地真实 Drizzle `db:migrate` 后仍须按计划增加 `core` migration target。

## 2026-07-23 ｜ M0-T7（Sentry 三端验证；外部门禁待完成）

- 内容：按 PRD N-4 为 Web（浏览器/Next.js server/edge）、API/Worker 与 Mobile 接入 Sentry；统一把 local/staging/production 映射为 dev/staging/prod，Web/API release 绑定部署 commit，增加受控三端 smoke exception、flush 与 event ID 输出，并配置 Web 与 Expo/EAS source map 上传路径。未引入业务功能。
- 主要文件：新增三端 Sentry 初始化、异常捕获与测试，新增 `packages/config/env/sentry.js` 的环境/release 约定，更新 Next.js、NestJS、Expo config/Metro、环境 schema、CI 与依赖锁；新增 `docs/operations/sentry.md` 的部署变量、release/source map 规则和外部验收清单。Mobile 锁定含 Metro 0.83 阻塞修复的 Sentry React Native 7.6.0，并以 `getSentryExpoConfig` 生成 debug ID。
- 验证：`pnpm lint`、`pnpm typecheck`、`pnpm test:unit`（101/101）、`pnpm test:e2e`（4/4，真实 PostGIS + Redis）、`pnpm build`、`pnpm mobile:check`（Expo Doctor 18/18、public config、iOS/Android production export）、`pnpm format:check`、`pnpm install --frozen-lockfile` 与 `git diff --check` 全部通过；本地未向 Sentry 发送伪造验收事件。
- 遗留/人工项：Sentry 后台项目 slug/三端 DSN 尚未获得可验证清单；现有 Vercel Web 上传 token 返回 401 且缺 `SENTRY_ORG`/`SENTRY_PROJECT`，M0-T0 的 `org:ci` token 无权枚举项目。需在 Sentry UI 确认或创建 Web/API/Mobile 项目并提供有上传权限的 token，随后配置 Vercel、Railway、EAS，完成同一 staging commit 的三端事件、release、Web/Mobile symbolication 实测。上述外部门禁未完成，因此开发计划 M0-T7 保持未勾选。

## 2026-07-23 ｜ M0-T7（Sentry 三端验证；外部门禁完成）

- 内容：完成 PRD N-4 与开发计划 M0-T7 的外部验收：创建并核验 Web/API/Mobile 三个 Sentry 项目，配置 Vercel、Railway、EAS 的 staging/production 变量，三端真实测试异常均成功进入 Sentry，environment 分别验证为 staging，release 与对应 Web/API commit 及 Android EAS artifact 一致；local/production 到 dev/prod 的映射由配置测试与生产变量检查覆盖，未向 production 发送测试异常。
- 主要文件：补充 `docs/operations/sentry.md` 的真实 event、release、EAS build 与 source map debug ID 证据并勾选 `ChinaSupply.AI开发计划.md` 的 M0-T7；Mobile 增加直接锁定的 `@sentry/cli` 2.58.0，使 EAS Android Gradle 上传任务能在 pnpm monorepo 中找到 CLI。外部行为包括 Vercel Web source map 上传、EAS Mobile bundle/source map 上传，以及 Railway API/Worker Sentry DSN 接线。
- 验证：Web event `3eac560cac1a4c38aabe14c8bb47ad67`、API event `248eb56016844c19be28eca95d65d5db`、Mobile event `8d01e0919bb0430f86034a4df6693d84` 均由 Sentry Events API 返回；Mobile 环境为 `staging`、release 为 `ai.chinasupply.app.staging@0.0.1+1`。EAS Preview build `2686dd8c-97fa-427b-a1b4-679309731768` 成功，日志确认 bundle/source map 上传与 debug ID `6950b9ec-52f1-43bc-91d1-0e12ddfa38e7`；Vercel Preview 日志确认 Next.js production source maps 上传成功。最终仓库检查结果见本任务提交与 PR checks。
- 遗留/人工项：无 M0-T7 验收遗留；生产环境仅检查配置且保持 smoke 开关关闭，未发送故意异常。聊天中暴露过的 Sentry 临时/上传令牌必须在本任务收尾时撤销并由最小权限 `org:ci` 新令牌替换，任何新令牌不得写入仓库或聊天。

## 2026-07-23 ｜ M0-T8（packages/geo 坐标转换）

- 内容：实现无运行时依赖的 WGS-84↔GCJ-02↔BD-09 六向纯函数，统一 `[lng, lat]` 顺序；GCJ-02→WGS-84 使用最多 30 次、`1e-7°` 收敛阈值的高精度迭代反算，中国适用范围外返回新的原值元组，对应 PRD G-1、F-6.2、F-9、N-5。
- 主要文件：新增 `packages/geo/src/coordinates.ts`、`conversions.ts` 与 9 项公开夹具单测，更新包导出、README、Vitest 脚本和锁文件；Mobile Jest/Metro 增加相对 `.js` 说明符到 TypeScript 源码的窄范围解析回退，确保 NodeNext 共享包可被测试与双端 Expo bundler 消费。未实现 `packages/geo/navigation` 或任何导航 URL。
- 验证：`pnpm --filter @chinasupply/geo test:unit`（9/9）、`pnpm lint`、`pnpm typecheck`、`pnpm test:unit`（112/112）、`pnpm build`、`pnpm mobile:check`（Expo Doctor 18/18、iOS/Android export）、`pnpm install --frozen-lockfile`、`pnpm format:check` 与 `git diff --check` 全部通过；北京、上海、深圳 WGS/GCJ 及公开 GCJ/BD 坐标对双向误差均不超过 0.5 米。
- 遗留/人工项：M0-T8 验收项无遗留；M0-T9 的五城市、双平台真机导航验证尚未执行，因此导航目标坐标模式、URL 模板和 `buildNavUrl` 仍未实现，M4 导航继续受门禁阻塞。

## 2026-07-23 ｜ M0-T9（导航验证门）

- 内容：完成北京、上海、义乌、深圳、东莞五城的 iPhone/Android 真机导航门禁，产品负责人批准所有目标统一打开“终点已填充的路线规划页”而非直接开始实时导航；正式规则采用 WGS-84，固化 Apple、Google、高德、百度主链接与 Web fallback，对应 PRD G-1、F-6.1、F-6.2、N-5。
- 主要文件：新增 `packages/geo/src/navigation` 的 `NavigationTarget` 判别联合类型、`buildNavUrl`、五城验证夹具和精确 URL 单测；更新 `@chinasupply/geo` 根导出与 `./navigation` 子路径、README、候选页生成器，以及 `docs/operations/m0-t9-navigation-validation.md`/`m0-t9-navigation-points.json`。Android Apple Maps 在类型层禁止；百度 Web `direction` 真机失败后改用已验证的官方 `marker` →“到这去”回落。
- 验证：`pnpm --filter @chinasupply/geo test:unit`（29/29）、`pnpm lint`、`pnpm typecheck`、`pnpm test:unit`（132/132）、`pnpm build`、`pnpm mobile:check`（Expo Doctor 18/18、iOS/Android export）、`pnpm format:check` 与 `git diff --check` 全部通过；正式候选页无需 `--allow-unconfirmed` 即可生成。
- 遗留/人工项：M0-T9 无遗留；M4-T5 仍必须使用 Release 包重新执行 F-6.4 真机落点 `<50m` 发布验收。连接的 Android 真机未安装 Google Maps，Google 安装态/回落结论来自产品负责人在可用设备和网络上的补充确认，版本号未提供并已在验证结论中如实记录。

## 2026-07-23 ｜ M0-T10（Cloudflare + MapTiler；外部门禁待完成）

- 内容：实现 Cloudflare→Railway 静态边缘密钥信任边界、单值 `CF-Connecting-IP` 校验与 `FastifyRequest.clientIp`，新增 no-store `/health/edge`；将 R2 契约拆为公开媒体/私有操作双桶并改用最小用途 `CLOUDFLARE_PURGE_TOKEN`；移动 MapTiler 拆为 iOS/Android key，并按现有 Bundle ID/Package 派生受限 User-Agent，对应 PRD G-10、G-11。
- 主要文件：新增 `apps/api/src/common/http/edge-proxy.ts`、API 单元/e2e 测试、`apps/mobile/src/config/maptiler-identity.ts`、MapLibre 请求初始化、`infra/cloudflare/r2-cors.staging.json` 和 `docs/operations/cloudflare-maptiler.md`；同步三份已确认文档、`AGENTS.md`、三端 `.env.example`、CI 假值和 Zod schema。外部已创建 `chinasupply-staging-media`、应用精确 CORS、关闭两桶 `r2.dev`，并在 Railway 创建 `api-staging.chinasupply.ai:3001` 域名声明。
- 验证：`pnpm lint`、`pnpm typecheck`、`pnpm test:unit`（API 26/26、Web 35/35、Geo 29/29、Mobile 32/32、Config 24/24）、`pnpm test:e2e`（6/6，真实 PostGIS + Redis）、`R2_MEDIA_BUCKET=chinasupply-dev-media pnpm build`、`pnpm mobile:check`（Expo Doctor 18/18、iOS/Android export）、`pnpm format:check`、`git diff --check` 全部通过。Wrangler 复核媒体 CORS 为唯一 origin、GET/HEAD/PUT、Content-Type、ETag、3600；私有桶无 CORS/自定义域名，两桶 `r2.dev` 均关闭。无环境覆盖的首次 `pnpm build` 如预期拒绝本机旧 `R2_BUCKET`，未改写私有 `.env.local`。
- 遗留/人工项：当前 Cloudflare 账号 API 无法枚举 `chinasupply.ai` zone，按阻塞策略未在其他账号建资源；因此 API CNAME/TXT、SSL Full、Transform Rule、媒体 custom domain、双桶最小权限凭据、Purge token verify 及外部 smoke 未完成。MapTiler 控制台因当前浏览器运行时连接失败，三把受限 key、EAS/Vercel/Railway 加密变量和限制 smoke 未完成。Railway 域名声明仍待 DNS 验证；代码未部署，M0-T10 保持未勾选，Production 仍留 M5-T9。

## 2026-07-23 ｜ M0-T10（Cloudflare 重新授权后接线）

- 内容：重新授权后定位 `chinasupply.ai` 的真实 zone 账号，完成 Cloudflare→Railway staging API DNS 与域名所有权接线；发现并明确旧 R2 桶与 zone 跨账号、不满足 custom domain 前提，未静默复用错误账号资源，对应 PRD G-10、G-11。
- 主要文件：更新 `docs/operations/cloudflare-maptiler.md` 与 `docs/operations/m0-t0-external-accounts.md` 的账号边界和实时状态。外部新增 proxied `api-staging.chinasupply.ai` CNAME 与 `_railway-verify.api-staging.chinasupply.ai` TXT；未删除旧账号 R2 桶。
- 验证：Railway domain status 返回 ownership verified、certificate valid；`https://api-staging.chinasupply.ai/health/live` 返回 200、标准 envelope、`server: cloudflare` 与 `CF-Ray`。`pnpm format:check`、`git diff --check` 通过。
- 遗留/人工项：zone 位于共享账号 `56d1a468b0a9e0095832fc44609dc25f`，该账号尚未启用 R2；旧桶位于个人账号 `0431bd1fc2c8927a1433f273bf43bf5a`，不能绑定目标 zone。当前连接器缺 Zone Settings/Rulesets 写权限，无法设置 SSL Full 和 Transform Rule；浏览器控制运行时仍无法打开 Cloudflare/MapTiler 控制台。M0-T10 继续保持未勾选。

## 2026-07-23 ｜ M0-T10（Cloudflare + MapTiler；外部门禁完成）

- 内容：完成 PRD G-10、G-11 与开发计划 M0-T10 的 staging 代码和外部配置验收：Cloudflare 可信边缘与真实客户端 IP、Railway 直连保护、公开媒体/私有操作 R2 分桶、最小权限 Purge token，以及 Web/iOS/Android 三把受限 MapTiler key；Production 资源仍延后至 M5-T9。
- 主要文件：完成 `apps/api/src/common/http/edge-proxy.ts`、`/health/edge`、双桶/最小权限环境 schema、Mobile 平台 User-Agent 映射和测试；同步 PRD、冻结技术栈、开发计划、`AGENTS.md`、三端环境示例、CI 假值、`docs/operations/cloudflare-maptiler.md` 与 R2 CORS 策略。外部完成 Cloudflare DNS/SSL/Transform Rule、两只正确账号 R2 桶及媒体 CDN、两组分桶凭据、Purge token、Railway API/Worker、Vercel staging 和 EAS Preview 加密变量，以及三把 MapTiler key；未记录任何密钥值。
- 验证：`pnpm lint`、`pnpm typecheck`、`pnpm test:unit`（API 26、Web 35、Geo 29、Mobile 32、Config 24）、`pnpm test:e2e`（6/6）、`R2_MEDIA_BUCKET=chinasupply-staging-media pnpm build`、`pnpm mobile:check`（Expo Doctor 18/18、iOS/Android export）、`pnpm format:check` 与 `git diff --check` 通过。Railway API/Worker 部署成功；Cloudflare live/ready/edge 均为 200，`CF-Ray`、`no-store`、真实出口 IP 匹配，伪造客户端 IP 被拒绝；Railway 直连 live/ready 为 200，edge/业务路径为标准 403 `FORBIDDEN`。R2 精确 origin 预检为 204、错误 origin 为 403、媒体 CDN 读取成功、私有桶不可公开访问且测试对象已删除。三把 MapTiler key 对正确 Origin/User-Agent 为 200，对错误或缺失值为 403。Purge token verify 为 active，未执行真实 purge。
- 遗留/人工项：M0-T10 验收无阻塞；Production 双桶、域名、凭据与 MapTiler production key 仍按 M5-T9 创建。个人旧账号中的同名 R2 桶未被 staging 引用，本任务未获删除旧资源的明确范围，因此保留待单独清理。

## 2026-07-23 ｜ M1-T1（Drizzle schema 与迁移）

- 内容：按 PRD 第 3 节与 G-1/G-3/G-8/G-9 建立 Drizzle 核心数据层，完整定义 9 张核心/关联表、WGS-84 SRID 4326 geometry、21 位 nanoid、timestamptz、JSONB/数组默认值、跨表延迟约束和 3.9 全部索引；新增唯一 `buildSearchText` 入口与严格 JSON Zod schema，`clusterStatsSchema` 明确拒绝 `factoryCount`。落地真实 core `db:migrate`，并把 staging 发布顺序扩展为 CI → CMS migration → Core migration → `Staging Release Gate`。
- 主要文件：新增 `apps/api/src/database/schema.ts`、PostGIS custom types、Drizzle config、`apps/api/drizzle/0000_initial_core.sql` 与 migration journal，并由 `DatabaseService.db` 暴露 typed client；新增 `packages/schemas` 的核心 JSON/search-text 契约及单测、core migration Testcontainers e2e；更新 CI、Railway watch paths、Payload/Core 双向所有权检查和迁移运维文档。Payload collections 与 migration 未被修改。
- 验证：`pnpm install --frozen-lockfile`、`pnpm --filter @chinasupply/api db:check`、`pnpm format:check`、`pnpm lint`、`pnpm typecheck`、`pnpm test:unit`（157/157）、`pnpm test:e2e`（12/12）和 `pnpm build` 全部通过。迁移 e2e 在 `postgis/postgis:17-3.5` 中先执行真实 CMS migration，再执行两次 core migration，验证 extension、表、SRID、索引、Payload 不变、ID/SRID 拒绝、两级类目和 primary-category 双向约束。
- 遗留/人工项：未执行 staging/production migration 或发布；首次合并 `main` 后需记录真实 CMS → Core workflow、`Staging Release Gate`、Railway API/Worker 与健康检查结果。Production migration 仍按 M5-T9 人工门禁执行。

## 2026-07-23 ｜ M1-T2（packages/schemas 全量 API 契约）

- 内容：按 PRD G-1/G-2/G-4/G-5/G-6、A-1～A-11、MAP-1～MAP-3、ADM-1～ADM-6 与 N-5，一次冻结并实现全部 V1 业务 API 的 path/query/body/response Zod schema；新增严格 WGS-84 GeoJSON、英语公开 DTO（A-5 地址保留 `{en,zh}`）、Admin 双语 DTO、统一 envelope/error、分页 meta、Base64URL cursor 编解码、收藏幂等回执、Clerk webhook 最小判别联合和 R2 预签名上传契约。
- 主要文件：新增 `packages/schemas/src/{primitives,geojson,pagination,envelope,entities,public-api,account-api,map-api,admin-api,webhook-api}.ts` 与全量契约/边界单测，统一从 package 根入口导出；API exception filter/interceptor 改为消费共享错误码和 envelope 类型；PRD 升级为 v1.2 Frozen 并同步 `AGENTS.md`、schemas README 与开发计划。未修改 `packages/api-client`，未实现业务路由或触发外部状态。
- 验证：`pnpm --filter @chinasupply/schemas test:unit`（85/85）、`pnpm lint`、`pnpm typecheck`、`pnpm test:unit`（236/236）、`pnpm test:e2e`（12/12，真实 PostGIS + Redis）、`pnpm build`、`pnpm mobile:check`（Expo Doctor 18/18、TypeScript、public config、iOS/Android export）、`pnpm format:check`、`pnpm install --frozen-lockfile`、`git diff --check` 全部通过。
- 遗留/人工项：无 M1-T2 验收遗留；M1-T3 仍需用这些 schema 派生 OpenAPI/Orval/API client/MSW，后续 A/MAP/ADM 任务实现路由与业务行为。

## 2026-07-23 ｜ M1-T3a（Zod → OpenAPI）

- 内容：按 PRD G-4、G-5、G-6、A-1～A-11、MAP-1～MAP-3、ADM-1～ADM-6 建立 30 个冻结业务操作加 `/health/live` 的路由契约注册表；注册表仅保存稳定 `operationId`、HTTP 元数据与鉴权，输入/响应继续直接引用 `packages/schemas` Zod 对象，并通过 `nestjs-zod` DTO 与 `zod-openapi` 6.0.0 派生 OpenAPI 3.1。
- 主要文件：新增 `apps/api/src/openapi`、`apps/api/scripts/generate-openapi.ts`、跟踪的 `apps/api/openapi.json` 与 health Zod schema；新增原始响应元数据，使 `GET /api/openapi.json` 不套业务 envelope，同时保持业务路径为 `/api/v1/**`。
- 验证：OpenAPI 单测验证 31 个唯一 operationId、路径参数、public/user/admin security 与跟踪文档一致；API e2e 验证 `/api/openapi.json` 返回原始 OpenAPI 3.1 JSON，最终 `pnpm test:unit` 中 API 34/34、`pnpm test:e2e` 14/14 通过。
- 遗留/人工项：无；本任务仅发布契约描述，30 个业务操作的数据库与控制器行为仍按 M1-T4/T5 实现。

## 2026-07-23 ｜ M1-T3b（Orval API Client 与双端接线）

- 内容：用 Orval 8.22.0 的 `react-query + fetch`、`tags-split` 模式生成全部类型、fetch 函数与 Query/Mutation hooks；新增 `configureApiClient({ baseUrl })`，从现有 `/api/v1` 环境变量派生 origin，并通过共享 fetcher保证 AbortSignal 透传和非 2xx 抛错，对应 PRD G-2、G-4、G-5。
- 主要文件：建立 `packages/api-client` 运行时、Orval 配置、生成目录与生产根入口；Web 保持 Server Component layout，仅增加窄范围 QueryClient Provider，并在 `/ops` 显示生成 health hook 状态；Mobile 复用现有 QueryClient，在 MapLibre spike 屏显示同一 hook 的 i18n 状态。两端用 injected workspace peer 解析各自 React Query，避免跨 React 版本重复实例。
- 验证：生成客户端单测通过真实 HTTP listener 验证 `/health/live` URL、成功 envelope 与 503 抛错；API e2e 通过生成的 `getHealthLive` 调用真实 Nest listener；Web/Mobile loading、ready、error 三态组件测试通过，`R2_MEDIA_BUCKET=chinasupply-dev-media pnpm build` 与 `pnpm mobile:check`（Expo Doctor 18/18、iOS/Android export）通过。
- 遗留/人工项：无；Web/App 未调用尚未实现的业务操作，M2 的业务场景应从 mock 子路径覆盖。

## 2026-07-23 ｜ M1-T3c（MSW 同源生成与 CI 漂移门禁）

- 内容：从同一 OpenAPI 链路生成 MSW 2.15.0 通配 host handlers 与 Faker response factories，只从 `@chinasupply/api-client/mocks` 子路径导出；生产入口不引用 MSW/Faker。新增统一生成、格式化和漂移检查，CI 在 lint 前验证 tracked 修改、删除和未跟踪生成文件，并将 Node 最低版本收紧到 22.18.0。
- 主要文件：新增 api-client mock 生成产物与 MSW 契约测试、`scripts/check-api-generation.mjs`、根级 `api:generate`/`api:generate:check`；更新 CI、工作流契约测试、Node 22.23.1 与移动变更路径检测，并将生成链路固定到仓库现用的 Prettier 3.9.6。
- 验证：`pnpm api:generate:check`、`pnpm lint`、`pnpm typecheck`、`pnpm test:unit`（252/252）、`pnpm test:e2e`（14/14，真实 PostGIS + Redis + Nest listener）、`R2_MEDIA_BUCKET=chinasupply-dev-media pnpm build`、`pnpm mobile:check`、`pnpm format:check`、`pnpm install --frozen-lockfile` 与 `git diff --check` 全部通过；生成 MSW handler 成功拦截同一 `getHealthLive` 客户端。
- 遗留/人工项：无；未部署、未修改外部环境，真实业务接口继续留给 M1-T4/T5。

## 2026-07-24 ｜ M1-T3（误操作恢复复验）

- 内容：检查 Claude 分支误操作后的仓库状态，确认 `main` 回退至 M1-T2 且 M1-T3a/b/c 已跟踪改动丢失；从 Git 未引用完整树对象恢复原实现，并逐文件核验 T3 的 Zod → OpenAPI、Orval client/TanStack Query 双端调用、MSW 与 CI 漂移门禁，对应 PRD G-2、G-4、G-5、G-6、A-1～A-11、MAP-1～MAP-3、ADM-1～ADM-6。
- 主要文件：恢复误操作前全部 504 个受影响文件，包括 `apps/api/openapi.json`、`apps/api/src/openapi`、`packages/api-client` 生成链路与产物、Web/Mobile health 调用、CI 配置、开发计划及 M1-T3a/b/c 原始开发日志；未引入恢复范围外的产品功能。
- 验证：504 个恢复文件与 Git 恢复树逐个 blob 对比为 0 差异；OpenAPI 为 25 条路径、31 个唯一 operationId，生成目录 447 个文件；`pnpm install --frozen-lockfile`、`pnpm api:generate:check`、`pnpm lint`、`pnpm typecheck`、`pnpm test:unit`（252/252）、`pnpm test:e2e`（14/14）、`R2_MEDIA_BUCKET=chinasupply-dev-media pnpm build`、`pnpm mobile:check`（Expo Doctor 18/18、iOS/Android export）通过。
- 遗留/人工项：恢复后的改动仍在本地工作树，尚未提交或推送；需由后续正常 Git 流程保存，避免再次被分支切换覆盖。

## 2026-07-24 ｜ M1-T4a（公开 API：categories + clusters）

- 内容：实现 A-7 两级类目树、A-1 published 产业带 cursor 列表与精确 category slug/region ID 筛选、A-2 published 产业带详情；落实 G-1/G-2/G-4/G-5/G-6/G-10，公开可翻译字段只返回英语、GeoJSON 使用 WGS-84、封面只返回 CDN URL，`factoryCount` 实时统计具有 `published_at` 的 published 工厂且不写入 stats。
- 主要文件：新增 `apps/api/src/categories`、`apps/api/src/clusters` 与 `apps/api/src/media` 的 NestJS 模块、Drizzle 查询和严格 DTO 映射；运行时配置开始要求 `R2_CDN_BASE_URL`；新增映射单测与 `public-catalog.e2e.ts`，未修改数据库 migration、冻结 OpenAPI wire shape 或生成的 API client。
- 验证：`pnpm api:generate:check`、`pnpm --filter @chinasupply/api db:check`、`pnpm lint`、`pnpm typecheck`、`pnpm test:unit`（257/257）、`pnpm test:e2e`（20/20，真实 PostGIS + Redis + Nest listener）、`R2_MEDIA_BUCKET=chinasupply-dev-media pnpm build`、`pnpm format:check` 与 `git diff --check` 全部通过；e2e 覆盖类目树稳定排序与空根类目、draft 隔离、组合筛选、同时间戳 cursor 无重复/遗漏、实时 factoryCount、A-2 boundary/stats/categories、Zod 400、404 及生成客户端真实调用。
- 遗留/人工项：无；缓存、限流、A-3～A-6 与 MAP-* 仍按后续 M1 任务包实现，本任务未部署 staging 或 production。

## 2026-07-24 ｜ M1-T4a（schemas 运行时打包修复）

- 内容：修复 M1-T2 起 `@chinasupply/schemas` 将 TypeScript 源码作为 Node 运行时入口导致 Railway `node dist/main.js` 解析 `.js` 子模块失败的问题；保持 PRD 4.6 wire contract 完全不变，将 schemas 编译为 ESM JavaScript 与声明文件，并让 API 生成、测试、Mobile、CI 和 Railway 在消费前构建该运行时包。
- 主要文件：`packages/schemas/package.json` 的 runtime/type exports 改指向 `dist`，新增 `tsconfig.build.json`；Railway 改用 filtered Turbo API build 以触发 `^build`；新增 `scripts/check-api-runtime.mjs`，由 CI 真正启动编译后的 API 并探测 `/health/live`；同步根命令、Mobile PR 检查、workspace/workflow 契约测试与部署文档。
- 验证：`pnpm --filter @chinasupply/schemas build` 生成全部 `dist/*.js`、`*.d.ts`；Railway 原样命令 `pnpm exec turbo run build --filter=@chinasupply/api` 依次成功构建 config/schemas/api；`pnpm api:runtime:check` 确认 Node 从 `packages/schemas/dist/index.js` 加载并由编译后的 `apps/api/dist/main.js` 返回 `/health/live` 200。`pnpm api:generate:check`、`pnpm --filter @chinasupply/api db:check`、`pnpm lint`、`pnpm typecheck`、`pnpm test:unit`（259/259）、`pnpm test:e2e`（20/20）、`R2_MEDIA_BUCKET=chinasupply-dev-media pnpm build`、`pnpm mobile:check`（Expo Doctor 18/18、TypeScript、public config、iOS/Android Metro export）、`pnpm format:check` 与 `git diff --check` 全部通过。
- 遗留/人工项：代码与本地验收无遗留；必须在本 PR 合并 `main` 后确认 Railway API 产生本 SHA 的全新成功部署，并对 staging 的 OpenAPI、A-7、A-1、A-2 完成真实 smoke，不能以旧容器健康代替。

## 2026-07-24 ｜ M1-T4b（公开 API：factories）

- 内容：实现 PRD A-3、A-4、A-5 与 G-1/G-2/G-4/G-5/G-6/G-10；新增 published 产业带工厂 cursor 列表、公开工厂 category/cluster/verified 组合筛选和工厂详情，`relatedFactories` 仅返回同一 published 产业带的其他 published 工厂、按 `published_at DESC, id DESC` 排序并限制 10 条。published 工厂关联 draft 产业带时仍可见，但公开 `cluster=null` 且不返回 related，避免泄露 draft 数据。
- 主要文件：新增 `apps/api/src/factories` 的 NestJS controllers/module、Drizzle service 与严格 DTO mapper，注册到 `AppModule` 并复用 M1-T2 冻结 route contracts；A-5 仅公开 WGS-84 location、双语 address、英语可翻译字段和 CDN 图片 URL，不返回 objectKey、GCJ-02 或后台校验人。扩展 `apps/api/e2e/public-catalog.e2e.ts` 并新增 mapper/cursor 单测；未修改数据库 migration、Payload、冻结 wire shape 或生成的 API client。
- 验证：`pnpm api:generate` 后生成产物无变化，`pnpm api:generate:check`、`pnpm lint`、`pnpm typecheck`、`pnpm test:unit`（262/262）、`pnpm test:e2e`（24/24，真实 PostGIS + Redis + Nest listener）、`pnpm build`、`pnpm api:runtime:check` 与 `git diff --check` 全部通过。e2e 覆盖 A-3 draft/不存在产业带 404、published/draft 隔离、category/cluster/verified 筛选、同时间 cursor、draft cluster 引用隐藏、完整 A-5 字段、related 排除当前/排除 draft/稳定排序/10 条上限、Zod 400、404 和生成客户端真实调用。
- 遗留/人工项：无；本任务未部署 staging 或 production，缓存/限流、A-6 与 MAP-* 仍按后续 M1 任务包实现。

## 2026-07-24 ｜ M1-T4c（公开 API：search）

- 内容：实现 PRD A-6、F-3.2/F-3.3 与 G-1/G-2/G-4/G-6；新增 categories/clusters/factories 三组各最多 5 条的统一搜索，英文采用 FTS 优先并以 trigram 补位，纯中文采用 trigram、两字中文采用转义后的 ILIKE prefix/contains，混合查询同时检索中英列。类目 aliases 通过共享搜索列反向命中关联产业带/工厂，cluster/factory 仅返回具有 `published_at` 的 published 数据。
- 主要文件：新增 `apps/api/src/search` 的 NestJS module/controller/service 与查询分类辅助逻辑，复用并导出现有冻结 search route contract；产业带搜索结果实时聚合公开工厂数，Point 以 WGS-84 GeoJSON 返回。扩展 `apps/api/e2e/public-catalog.e2e.ts` 和搜索辅助单测；未修改数据库 migration、Payload、公开 wire shape 或生成的 API client。
- 验证：`pnpm api:generate` 后生成产物无变化；`pnpm api:generate:check`、`pnpm --filter @chinasupply/api db:check`、`pnpm lint`、`pnpm typecheck`、`pnpm test:unit`（269/269）、`pnpm test:e2e`（25/25，真实 PostGIS + Redis + Nest listener）、`pnpm build`、`pnpm api:runtime:check`、`pnpm format:check` 与 `git diff --check` 全部通过。e2e 覆盖 `led`/`socks`、拼写模糊补位、`sofa` alias、`家具`、`LED灯`、draft 隔离、每组 5 条上限、FTS 优先与稳定同分排序、实时 factoryCount、WGS-84 Point、Zod 400 及生成客户端真实调用；本地已预热 fixture 的 `sofa` 调用为 3.33ms。
- 遗留/人工项：真实数据规模下搜索 p95/500ms 基线按批准计划由 M1-T9 验证；G-11 缓存与 Redis 多实例限流由 M1-T6 实现。本任务未部署 staging 或 production。

## 2026-07-24 ｜ M1-T5（地图 API）

- 内容：实现 PRD MAP-1、MAP-2、MAP-3 与 G-1/G-2/G-4/G-6；新增 published 产业带点、bbox 内产业带边界和工厂点 FeatureCollection，固定轻量 properties、实时 factoryCount、category/cluster/verified 筛选、draft 隔离、5000 点上限及 `meta.truncated`。按确认结论将 MAP-2 容差冻结为 zoom `<10` 使用 `0.01°`、`10–11` 使用 `0.002°`、`≥12` 原始精度，并同步 PRD v1.3 与 `AGENTS.md`。
- 主要文件：新增 `apps/api/src/map` 的 NestJS module/controller/service、Drizzle PostGIS 查询和严格 Zod mapper，注册既有 MAP-* route contracts；扩展 `apps/api/e2e/public-catalog.e2e.ts` 及地图单测。bbox 使用原始 geometry 相交语义，简化结果用 `ST_Multi` 保持 MultiPolygon wire shape；published 工厂关联 draft 产业带时返回 `clusterId=null`。未修改 migration、Payload、Web、Mobile、冻结 Zod/OpenAPI shape 或生成客户端。
- 验证：`pnpm api:generate:check`（生成产物零漂移）、`pnpm --filter @chinasupply/api db:check`、`pnpm lint`、`pnpm typecheck`、`pnpm test:unit`（279/279，其中 API 59/59）、`pnpm test:e2e`（29/29，真实 PostGIS + Redis + Nest listener）、`pnpm build`、`pnpm api:runtime:check`、`pnpm format:check` 与 `git diff --check` 全部通过。e2e 覆盖三档简化、bbox/组合筛选、draft/空 boundary 隔离、严格属性、Zod 400、Orval 客户端真实调用，以及 5001 点查询返回 5000 点并标记 truncated。
- 遗留/人工项：MAP-* Cache-Control、Cloudflare 缓存规则、Redis 多实例限流和 purge 仍按 M1-T6 实现；MAP-1 gzip 与 MAP-3 真实数据量 p50/p95 仍按 M1-T9 验证。本任务未部署 staging 或 production。

## 2026-07-24 ｜ M1-T6（缓存与限流）

- 内容：实现 PRD G-11 与 MAP-* 的缓存和限流约定；搜索与三个 MAP 路由分别按可信 `clientIp` 独立限制为 60 次/分钟，基于 NestJS Throttler 与自建 ioredis Lua/sorted-set 原子滚动窗口在多实例间共享配额，Redis 故障失败关闭为标准 `INTERNAL` 500。MAP 成功响应返回 `public, max-age=0, s-maxage=3600`，所有错误响应返回 `no-store`，搜索不设置共享缓存 TTL；同步 PRD v1.4 与 `AGENTS.md`。
- 主要文件：新增 `apps/api/src/rate-limit`、`apps/api/src/cache`、Redis/Testcontainers e2e 和 Cloudflare purge 客户端单测；更新搜索/MAP controller、全局异常/边缘代理错误缓存头、API 远程环境校验及运维文档。Cloudflare staging 已新增唯一 active Cache Rule `ChinaSupply staging MAP API cache`，精确匹配 `api-staging.chinasupply.ai` 的 `/api/v1/map/` 前缀，启用缓存资格并以 `bypass_by_default` 尊重源站缓存头；未执行 purge，也未暴露 purge HTTP 路由。
- 验证：`pnpm api:generate:check`、`pnpm --filter @chinasupply/api db:check`、`pnpm lint`、`pnpm typecheck`、`pnpm test:unit`（285/285）、`pnpm test:e2e`（32/32，真实 PostGIS + Redis，覆盖 60/61、路由/IP 隔离、多实例共享、窗口恢复、伪造 X-Forwarded-For 无效、Redis 故障 500、缓存头）、`R2_MEDIA_BUCKET=chinasupply-dev-media pnpm build`、`pnpm api:runtime:check`、`pnpm format:check` 与 `git diff --check` 全部通过；Cloudflare 控制台复核规则为 1 条 active，未覆盖其他 ruleset。
- 遗留/人工项：staging 只从 `main` 部署，当前分支合并前无法让 Railway origin 返回本任务新增的缓存头；合并并成功部署后仍须完成 Cloudflare Trace、MAP MISS→HIT、合法 query 隔离、搜索 DYNAMIC/BYPASS 与真实 429 smoke。上述外部门禁完成前，开发计划 M1-T6 保持未勾选；用户写接口尚未落地，后续 M3/M5 复用同一 Redis storage 并按用户 ID 限流。MAP 性能门禁仍按 M1-T9 执行。

## 2026-07-24 ｜ M1-T6（人工门禁确认）

- 内容：产品负责人确认 M1-T6 外部门禁可以放行，将开发计划中的 M1-T6 状态更新为已完成；本次不修改缓存、限流或 purge 的运行时代码，对应 PRD G-11、MAP-*。
- 主要文件：仅更新 `ChinaSupply.AI开发计划.md` 的 M1-T6 复选框，并追加本条不可变开发日志。
- 验证：复核 M1-T6 原交付已合并至 `main`；运行 `pnpm format:check` 与 `git diff --check`。
- 遗留/人工项：M1-T6 无；MAP 性能门禁仍按 M1-T9 执行，publish/unpublish 的 prefix purge 接线仍按 M5-T3 执行。

## 2026-07-24 ｜ M1-T7（基础导入管道）

- 内容：实现 PRD F-9.1 与 G-1/G-2/G-3/G-8/G-10 的 `import:clusters` / `import:factories` 管道：固定 CSV/JSON v1 契约，CLI 上传私有 R2 后入 BullMQ，Worker 逐行 Zod 校验、解析 region ID 与 category/cluster slug、按实体转换 GCJ-02、生成共享 search_text、按 slug 事务 upsert，并把成功/失败行与原因写回 R2；重跑覆盖导入字段和关联但保留 ID、publish 与 verified 状态。
- 主要文件：新增 `packages/schemas/src/imports.ts`、`apps/api/src/imports`、两个 CLI 入口、四份 `docs/import-templates` 模板及 `docs/operations/data-imports.md`；API/Worker 使用官方 S3-compatible AWS SDK 访问私有桶，本地/e2e 可显式覆盖 MinIO endpoint；`packages/geo` 增加 Node 编译入口并保留 React Native 条件导出。未增加 HTTP/OpenAPI 接口、数据库 migration 或 Payload 表。
- 验证：`pnpm api:generate:check`、`pnpm --filter @chinasupply/api db:check`、`pnpm install --frozen-lockfile`、`pnpm lint`、`pnpm typecheck`、`pnpm test:unit`（298/298）、`pnpm test:e2e`（33/33，真实 PostGIS + Redis + MinIO）、`pnpm build`、`pnpm api:runtime:check`、`pnpm mobile:check`（Expo Doctor 18/18、iOS/Android export）、`pnpm format:check` 与 `git diff --check` 通过。导入 e2e 对 clusters/factories 各验证 100 行含 10 行脏数据，均为 90 入库/10 报告；第二次运行均为 0 新增/90 更新且无重复。首次全套 e2e 的既有 Redis 停机用例触发一次 15 秒超时，随后单独复跑及最终全套复跑均通过。
- 遗留/人工项：按任务范围未向 staging 或 production 上传文件、入队或写入数据；M1-T8 起数据人员需使用已交付模板和 SOP 在 staging 录入真实/合成数据。Production 数据迁移仍受 M5-T8a 人工门禁约束。

## 2026-07-24 ｜ M1-T8（种子与合成数据）

- 内容：交付 10 个跨品类真实产业带和每带 5 家的 50 家真实工厂 canonical 数据，只向 staging 导入且全部保持 `draft + unverified`；新增完整 Zod/引用预检、regions/categories 幂等 upsert、复用 M1-T7 R2 → BullMQ → Worker 的 clusters/factories 顺序导入，以及固定 PRNG/算法版本的 5,000/20,000 工厂点生成器，对应 PRD F-9.1、G-3、G-8、G-10。
- 主要文件：新增 `data/staging/real-seed/` 四类数据、`apps/api/src/seeds/` 与两个 CLI、`data/load/manifest.json`、`data-sources.md` 和 `docs/operations/data-verification-sop.md`；合成文件只写被忽略的 `.generated/load/`，manifest SHA-256 分别为 `edb179078c2d4e78f2d487d3b663924922b443b25af81e0c95b7237cd30f4667`（5,000）与 `ba9a3e42bf33828f820877d8a3046cb68fefe4db046da7c9719eb771c4ec3e8f`（20,000）。未修改 HTTP API、OpenAPI、数据库 migration 或生成 client。
- 验证：`pnpm api:generate:check`、`pnpm --filter @chinasupply/api db:check`、`pnpm lint`、`pnpm typecheck`、`pnpm test:unit`（306/306）、`pnpm test:e2e`（34/34，PostGIS + Redis + MinIO，seed 首轮 10/50 insert、次轮 10/50 update、ID 不变、报告零失败）、`R2_MEDIA_BUCKET=chinasupply-dev-media pnpm build`、`pnpm format:check` 和 `git diff --check` 通过。staging 最终作业为 clusters `w1E1kLMUbHXwbZlJLHdKd`（`staging/imports/clusters/w1E1kLMUbHXwbZlJLHdKd/report.json`，0 insert/10 update/0 failed）及 factories `GHeTHElyyGo4zzc5ip7yi`（`staging/imports/factories/GHeTHElyyGo4zzc5ip7yi/report.json`，0 insert/50 update/0 failed）；只读查询确认 canonical slug 精确为 10/50、无 synthetic、全部 draft/unverified，公开 clusters/factories/MAP 均返回 0 条。
- 遗留/人工项：50 家工厂仍须在 M2 `/ops` 按 SOP 逐条核验制造主体、厂区级坐标、中英文地址、主营产品、来源与联系方式后，才可由 Admin 写入 verified 留痕并单独 publish；当前 Nominatim 无法确认厂区对象的候选落点不得作为核验通过依据。合成数据仅供 M1-T9 本地/隔离负载测试，禁止进入 production。

## 2026-07-24 ｜ M1-T8（CI 慢速运行器测试预算修订）

- 内容：GitHub Actions 的 20,000 行确定性生成测试耗时 5.095 秒，超过 Vitest 默认 5 秒；经批准将该用例组超时预算显式设为 15 秒，不修改生成算法、manifest 或任何运行时代码。
- 主要文件：仅修改 `apps/api/test/generate-load-data.test.ts` 的测试超时，并追加本条日志。
- 验证：`CI=true pnpm --filter @chinasupply/api test:unit`、`pnpm format:check` 与 `git diff --check`。
- 遗留/人工项：M1-T8 原人工数据核验门禁不变。

## 2026-07-24 ｜ M1-T9（API e2e 与负载基线）

- 内容：补齐 A-4 公开 factories 列表同时间戳 cursor 翻页一致性 e2e；新增完全隔离的 Testcontainers + k6 基线，复用 M1-T8 生成器装载 10 个 published clusters 与 5,000 个 published synthetic factories，验证 MAP-1 gzip、MAP-3 5,000 点 p50/p95 和搜索 p50/p95，对应 PRD G-4/G-5/G-11、A-1/A-4/A-6、MAP-1/MAP-3、N-1/N-5。负载夹具仅替换测试应用的限流 Guard，真实 Redis 多实例 429 仍由独立 e2e 覆盖。
- 主要文件：新增 `apps/api/scripts/run-load-tests.ts`、`apps/api/load/` k6 场景、手动 `.github/workflows/api-load.yml`、`docs/operations/api-load-testing.md` 与 `docs/performance/m1-t9-baseline.md`；根命令新增 `pnpm test:load`。未修改 HTTP API、Zod/OpenAPI、生成 client、数据库 migration 或生产限流配置。
- 验证：`pnpm test:load` 最终通过——MAP-1 gzip 806 B；MAP-3 5,000 点 p50 163.53 ms / p95 196.46 ms（阈值 <500 ms）；搜索 `q=led` p50 78.81 ms / p95 139.84 ms（阈值 <300 ms），HTTP 检查零失败。首次负载执行已跑完两段 k6，但因 k6 2.0 summary-export 结构变化在合并报告解析处失败，兼容新旧结构后完整复跑通过。`pnpm api:generate:check`、`pnpm lint`、`pnpm typecheck`、`pnpm test:unit`（307/307）、`pnpm test:e2e`（35/35，真实 PostGIS + Redis + MinIO/BullMQ）、`R2_MEDIA_BUCKET=chinasupply-dev-media pnpm build`、`pnpm format:check` 与 `git diff --check` 均通过。
- 遗留/人工项：本次只建立本地隔离 synthetic 基线，未向 staging 或 production 写入数据；手动 GitHub workflow 合并后可按需运行并保留精确 commit artifact，M5-T8 仍须用批准的真实数据量与部署环境复测 N-1。

## 2026-07-24 ｜ M2-T1a（Web 地图底座）

- 内容：实现 PRD G-1/G-2、MAP-1、F-1.1、F-11.2 与 N-6；Web 首页替换为覆盖中国 bbox `[73,18,135,54]` 的全屏 MapLibre 地图，使用现有生成客户端读取 MAP-1 envelope，并按 Feature `color` 渲染带白色描边的产业带圆点。加入键盘可操作的缩放/指南针控件、英语加载/错误/重试状态，以及首次静态渲染即出现的 MapTiler、OpenStreetMap attribution 和 Free plan logo；未实现 M2-T1b/T1c 的 boundary、工厂聚合、防抖/abort、筛选、点位点击或详情卡片。
- 主要文件：Web 精确锁定 `maplibre-gl@6.0.0`；新增 `apps/web/src/app/(frontend)/map/` 地图组件、图层配置和静态 attribution，更新首页、响应式全屏样式、英语 messages 与 Next Image 远程 logo 配置；新增 `@chinasupply/config/map/style` 共享浅色商务 style，运行时注入受环境约束的 MapTiler key，仓库无真实 key、无托管 style URL；未修改 API、Zod、OpenAPI 或生成客户端。
- 验证：`pnpm lint`、`pnpm typecheck`、`pnpm test:unit`（Web 44/44、config 32/32，全部 workspace 通过）、`pnpm build`、`pnpm api:generate:check`（生成产物零漂移）、`pnpm format:check` 与 `git diff --check` 全部通过；Playwright production-build 夹具确认 1440×900 全屏 canvas、静态 attribution/logo、缩放/指南针控件、错误与 Retry 状态及无运行时控制台错误。
- 遗留/人工项：受 MapTiler key 的本地域名/Origin 限制，本任务未冒用不受限 key，无法完成真实 MapTiler staging 视觉冒烟；按批准计划由 M2-T8 在正式 staging 域名完成。无其他未满足的 M2-T1a 验收项。

## 2026-07-24 ｜ M2-T1a（地图数据与真实底图修复）

- 内容：修复 PRD MAP-1、G-11、F-1.1/F-11.2 与 N-6 的两个独立运行时故障：公开、无凭据且经 CDN 缓存的 MAP 只读接口改用 `Access-Control-Allow-Origin: *`，使 Vercel Preview 不再因固定 staging Origin 显示 “Industrial cluster data could not be loaded.”；在已合并的 `v3-openmaptiles` TileJSON 端点修复基础上，为 Next/Turbopack 产物显式配置同版本 MapLibre module Worker，避免默认空 Worker URL 令矢量瓦片与 GeoJSON 永久停在 loading。
- 主要文件：`apps/api/src/http-application.ts` 新增 MAP GET/HEAD/OPTIONS 专用无凭据 CORS delegator，并由单元/e2e 测试锁定；`apps/web/scripts/prepare-maplibre-worker.mjs` 在 dev/build 前从精确锁定的 `maplibre-gl@6.0.0` 复制 Worker 与 shared module 到忽略的静态目录，`industrial-map.tsx` 在创建地图前调用 `setWorkerUrl`；共享 style 测试明确锁定 `tiles/v3-openmaptiles/tiles.json` 及 landcover/landuse/water/transportation/boundary/place source-layer，禁止回退到 `/tiles/v4/`。未修改 API wire contract、Zod/OpenAPI 或生成客户端。
- 验证：`pnpm lint`、`pnpm typecheck`、`pnpm test:unit`（324/324）、`pnpm --filter @chinasupply/api test:e2e`（35/35，真实 PostGIS + Redis）、`pnpm build`、`pnpm api:generate:check`、`pnpm format:check` 与 `git diff --check` 全部通过。Playwright 打开 production build，以固定 MAP-1 response 隔离后端、真实请求 staging 授权的 MapTiler 资源：module Worker 与 shared module 均 200、OpenMapTiles TileJSON 200、7 个矢量瓦片和 4 个 glyph 均 200，错误遮罩消失，1440×900 截图确认水域、行政线、英文地名及 `color` 业务圆点实际渲染。
- 遗留/人工项：本 PR 合并并完成 Web/API staging 部署后，需在 `https://staging.chinasupply.ai` 再做同一视觉 smoke；Cloudflare 可能在最长 1 小时内继续命中旧 MAP 响应中的固定 CORS header，未经发布授权未执行 purge，可等待 TTL 到期或由获授权人员在部署后 purge MAP cache。M2-T8 的正式 staging 全链路 smoke 门禁不变。

## 2026-07-24 ｜ M2-T1b（地图分层加载）

- 内容：实现 PRD G-1/G-2、MAP-2/MAP-3 与 F-1.2/F-1.3/F-1.7；Web 地图在 `moveend` 后防抖 500ms 生成规范化 WGS-84 bbox，zoom ≥8 加载产业带 boundary 半透明色块，zoom ≥10 加载最多 5000 个工厂点并由 MapLibre 聚合，点击聚合点自动放大；移动开始、query key 替换和卸载时均通过生成 client 的 `AbortSignal` 取消旧请求。MAP-3 当前成功响应 `meta.truncated=true` 时显示 “Zoom in to see all factories”，移动/占位刷新期间隐藏陈旧提示，断网继续显示数据错误与重试而非空白。
- 主要文件：扩展 `apps/web/src/app/(frontend)/map/` 的 viewport/debounce、MAP-2/MAP-3 source 与图层、聚合点击和 truncated 状态；更新英语 i18n 与响应式提示条样式，新增分层加载单测。未修改 API、Zod/OpenAPI、Payload、生成 client、筛选 UI、详情卡片或 PostHog 接线。
- 验证：`pnpm lint`、`pnpm typecheck`、`pnpm test:unit`（Web 52/52，全部 workspace 通过）、`pnpm build`、`pnpm api:generate:check`、`pnpm format:check` 与 `git diff --check` 全部通过。production-build 浏览器验收使用固定 TileJSON 和 MAP fixture，在 1440×900 WebGL2 Chrome 中确认 MAP-2 首次请求 zoom=8、MAP-3 首次请求 zoom=10、zoom 11 在途 MAP-2/MAP-3 均被 abort、zoom 12 成功替换、5000 点形成可见聚合、聚合点击触发新 viewport 请求，truncated 在刷新期间隐藏并于成功响应后恢复；无 map/data/Next 错误遮罩。
- 遗留/人工项：M2-T8 仍须提交仓库内固定 MSW/style/tile Playwright 套件并在正式 staging 域名完成真实 MapTiler smoke；M2-T3 才接类目筛选，`map_moved` 埋点等待 M3-T6 Consent 与 `packages/analytics` 接线。本任务未访问 staging/production、未使用真实 MapTiler key、未执行 Cloudflare purge。

## 2026-07-24 ｜ M2-T1c（地图卡片交互）

- 内容：实现 PRD F-1.4/F-1.5；点击产业带点或 boundary 时先用 MAP-1/MAP-2 的名称与实时 factoryCount 即时显示卡片，点击工厂点时先用 MAP-3 的名称与 verified 状态即时显示卡片，随后分别通过生成客户端调用 A-2/A-5 补全封面/图片与主营产品。地图点击按工厂聚合、工厂点、产业带点、boundary 的顺序解析；无效属性不请求详情，空白、聚合点或关闭按钮清除选择，详情失败保留即时字段并提供重试。
- 主要文件：新增 `apps/web/src/app/(frontend)/map/map-selection.ts` 与 `map-selection-card.tsx`，扩展地图点击接线、响应式侧栏/底部卡片、英语 i18n 和严格 R2 CDN 图片 origin；新增卡片交互单测。详情链接已指向冻结的 `/clusters/[slug]` 与 `/factories/[slug]`；未修改 Payload、API、Zod/OpenAPI、生成客户端、筛选或 analytics。
- 验证：`pnpm lint`、`pnpm typecheck`、`pnpm test:unit`（Web 59/59；全部 workspace 338/338）、`pnpm build`、`pnpm api:generate:check`、`pnpm format:check` 与 `git diff --check` 全部通过；单测覆盖 MAP 属性解析/拒绝、重叠点击优先级、即时 skeleton、A-2/A-5 图片与主营产品、无图、verified/unverified、错误重试、关闭按钮及冻结 href。production build 成功；本地浏览器因按规范保留占位 MapTiler key，在 style 初始化前进入地图错误态，浏览器通道无法改写 MapLibre Worker 的 TileJSON 请求，因此未伪造固定 tile 视觉通过。
- 遗留/人工项：M2-T4/M2-T5 实现前详情目标路由暂时返回 404；M2-T8 仍须提交仓库内固定 MSW/style/tile Playwright 套件，并在正式 staging 域名完成真实 MapTiler 与端到端卡片交互 smoke。本任务未访问 staging/production、未使用真实或不受限 MapTiler key、未部署预览。

## 2026-07-24 ｜ M2-T6（前置 staging 数据门禁 Admin API）

- 内容：按批准的任务重排前置实现 ADM-1/ADM-3 的 GET+PATCH、ADM-5 factory verify、ADM-2/ADM-4 publish/unpublish，并由 API 自身验证 Clerk session token 与 `metadata.role=admin`；工厂未 verified 时拒绝 publish，普通 PATCH 统一重建 `search_text` 并将工厂恢复为 unverified，保留历史核验时间与人员留痕。publish/unpublish 幂等保留首次 `published_at`，状态提交后同步 purge `/api/v1/map/`，purge 失败返回标准 INTERNAL 且重复状态操作会再次清理以完成收敛，对应 PRD G-4/G-6/G-7、ADM-1~5、F-7.2/F-7.3 与 MAP-*。
- 主要文件：新增 `apps/api/src/admin`、`apps/api/src/auth` 与 MAP cache invalidation service，API 直接依赖 Clerk Backend SDK；Admin 列表按 `updated_at DESC, id DESC` cursor 分页，详情返回冻结 Admin DTO，cluster/factory PATCH 事务校验 region/category/cluster 引用并维护关联表。同步调整开发计划与 `AGENTS.md`，将 M2-T6/T7 和真实 staging 数据门禁前置；未增加数据库 migration、未修改 Zod/OpenAPI wire shape或生成客户端、未实现 Create/ADM-6、未操作 staging/production 数据。
- 验证：`pnpm install --frozen-lockfile`、`pnpm lint`、`pnpm typecheck`、`pnpm test:unit`（345/345）、`pnpm test:e2e`（40/40，真实 PostGIS + Redis + MinIO）、`R2_MEDIA_BUCKET=chinasupply-dev-media pnpm build`、`pnpm api:runtime:check`、`pnpm api:generate:check`（生成产物零漂移）、`pnpm format:check` 与 `git diff --check` 全部通过。新增 e2e 覆盖 401/403、draft Admin cursor、PATCH/搜索列、verify 首次与最近时间、未核验发布拒绝、首次发布时间保留、公开 draft 隔离，以及 purge 失败后状态持久化与安全重试。
- 遗留/人工项：本任务合并至 `main` 并完成 Railway staging 部署后，仍需以真实 Clerk admin token 验证 401/403、状态流转和 Cloudflare MAP purge；M2-T7 `/ops` 与 2 个产业带 + 10 家工厂的独立人工 SOP 核验/发布尚未执行，禁止在它们完成前直接改写 staging 数据状态。

## 2026-07-24 ｜ M2-T7（前置 staging 数据门禁 /ops）

- 内容：实现 PRD F-7.2 与 ADM-1~5 的最小 `/ops` 运营界面；经既有 Clerk admin 服务端门禁后，客户端使用 Clerk session token 调用生成的 Admin API client，提供产业带/工厂列表、详情编辑、factory verify、publish/unpublish、状态与核验标识、失败重试。产业带 publish 与 factory verify 前必须由独立复核人确认已完成 SOP；未核验工厂的 publish 控件保持禁用，保存工厂修改后明确提示重新核验。
- 主要文件：新增 `apps/web/src/app/(frontend)/ops/ops-dashboard.tsx`、响应式样式和表单数据转换，替换原 `/ops` 鉴权探针页并增加英语 i18n；新增静态组件/表单测试和 i18n 契约测试。新增 `docs/operations/reviews/staging/TEMPLATE.md`，供人工复制为 `YYYY-MM-DD-m2-smoke.md` 并记录 2 个产业带、10 家工厂、分阶段发布及 unpublish→publish 验收；未创建虚假的已完成记录，未实现新建、ADM-6 上传、地图选点或 M5 后台增强，未修改 API、Zod/OpenAPI 或生成客户端。
- 验证：`pnpm lint`、`pnpm typecheck`、`pnpm test:unit`（351/351，其中 Web 65/65）、`R2_MEDIA_BUCKET=chinasupply-dev-media pnpm build`、`pnpm api:generate:check`（生成产物零漂移）、`pnpm format:check` 与 `git diff --check` 全部通过。Web 测试覆盖表单转换与拒绝、列表状态、verified/unverified、独立复核确认、未核验发布禁用、操作错误与 Retry；既有权限测试继续覆盖匿名、非 admin 与 admin 路由门禁。
- 遗留/人工项：合并并完成 staging 部署后，仍需获授权的独立复核人用真实 Clerk admin 账户逐条核验与修正数据，按模板先发布 1 个产业带 + 3 家工厂冒烟，再扩至 2 + 10，并实测 MAP purge、A-2/A-5/A-6、MAP-1/2/3、draft/unverified 隔离及 unpublish→publish 收敛。现有候选工厂缺少可直接确认的厂区级坐标证据，本任务未发布任何 staging 数据；M2 staging 真实数据门禁保持未勾选，M2-T2 继续受其阻塞。

## 2026-07-25 ｜ M2 staging 真实数据门禁（证据预检，阻塞）

- 内容：按 M1-T8 数据核验 SOP 对 M2-T2 前置的 staging 真实数据门禁进行只读预检；核对 A-1/A-4、MAP-1/2/3 的 draft 隔离，复查古镇、德化、永康三组边界与工厂来源，并记录候选坐标差异。未执行 ADM-5、ADM-2/4 或任何 production 操作。
- 主要文件：新增 `docs/operations/reviews/staging/2026-07-25-m2-smoke.md`，更新 `data-sources.md`；记录已登录 Admin 访问 `/ops` 时 count formatter 函数跨 Server/Client 边界导致 HTTP 500，以及现有候选不足 2+10 厂区级坐标证据。
- 验证：staging `/health/live`、`/health/ready`、A-1、A-4、MAP-1/2/3 实际请求完成；公开列表和地图均为 0，draft 隔离符合预期。Vercel serverless 日志确认 `/ops` 错误根因。`pnpm lint`、`pnpm typecheck`、`pnpm test:unit`（351/351）、`pnpm build`、`pnpm api:generate:check`、`pnpm format:check` 与 `git diff --check` 通过。
- 遗留/人工项：门禁未通过且未勾选。需先以独立 PR 修复并部署 `/ops` 序列化缺陷，再补齐同一产业带每组 5 家工厂的厂区级坐标证据，由独立 Admin 复核后执行 1+3、2+10、缓存 purge 和 unpublish→publish；M2-T8 fixture Playwright 与真实 MapTiler smoke 仍不被替代。

## 2026-07-25 ｜ M2-T7（/ops Server/Client 序列化修复）

- 内容：修复 `/ops` 将数量格式化函数从 Server Component 传入 Client Component 所导致的运行时 500；`OpsLabels` 现在只包含可序列化字符串，产业带与工厂数量在客户端通过既有 `Operations.dashboard` ICU 消息格式化，保留英语单复数规则，对应 PRD G-2、G-7、F-7.2 与 ADM-1~5。
- 主要文件：修改 `apps/web/src/app/(frontend)/ops/page.tsx` 与 `ops-dashboard.tsx`，移除跨 RSC 边界的 `clusterCount`/`factoryCount` 函数；更新 `ops-dashboard.test.tsx` 并新增 `ops-page.test.tsx`，锁定 labels JSON 可序列化及 1/多数量渲染。未修改鉴权、Admin API、Zod/OpenAPI、生成客户端或数据状态。
- 验证：`pnpm lint`、`pnpm typecheck`、`pnpm test:unit`（353/353，其中 Web 67/67）、`pnpm build`、`pnpm api:generate:check`（生成产物零漂移）、`pnpm format:check` 与 `git diff --check` 全部通过。
- 遗留/人工项：本修复合并并完成 Vercel staging 部署后，仍须以真实 Clerk Admin 会话确认 `/ops` 不再 500、列表为 10 个产业带/50 家工厂且详情、编辑、verify/publish 控件与鉴权正常；数据继续保持 draft/unverified，独立 SOP 复核、1+3、2+10 与缓存收敛门禁尚未执行。M2-T8 保持未完成且不被本次修复替代。

## 2026-07-25 ｜ M2 staging 真实数据门禁（固定顺序穷举，阻塞）

- 内容：在 `/ops` 修复部署并通过真实 Clerk Admin 冒烟后，按冻结顺序穷举 Guzhen、Dehua、Yongkang、Chenghai、Dongguan、Nantong、Jinjiang、Shunde、Yiwu、Shenzhen 的现有关联数据；核对官方制造主体、地址、产品、联系方式、OSM boundary、地图 POI 与坐标系，使用 `packages/geo` 将已确认的高德 GCJ-02 POI 转为 WGS-84。仅 Dongguan 形成 1 个产业带 + 5 家工厂的完整证据候选，其余组均存在至少一个不可接受的办公/门店落点、错误实体或无可追溯厂区 POI，未达到冻结的 2+10 门槛；未执行 ADM-5、ADM-2/4 或 production 操作，对应 PRD G-1/G-2/G-8、F-7.2、ADM-2/4/5、A-2/A-5/A-6 与 MAP-1/2/3。
- 主要文件：扩充 `docs/operations/reviews/staging/2026-07-25-m2-smoke.md`，记录 10 组逐组结论、Dongguan OSM relation 3464319、5 家工厂的官方证据、地图对象 ID、GCJ-02 原值和 WGS-84 转换值，以及其他 6 组新增拒绝证据；未修改公开 API、Zod/OpenAPI、生成客户端、业务代码或 staging 数据状态。
- 验证：PR #36 GitHub CI 与 Vercel Preview 通过并已合并；staging Vercel 部署 `dpl_G8zhzfHoi3hrPZ9awKxC2MqNSLDj` READY；真实 Admin `/ops` 显示 10 个 draft 产业带和 50 家 draft/unverified 工厂，详情、编辑、verify/publish 控件加载正常且部署后无新 error 日志；`packages/geo` 现有 `gcj02ToWgs84` 完成 OPPO、vivo、Delta、Luxshare 四个 POI 的转换。文档提交前运行 `pnpm format:check` 与 `git diff --check`。
- 遗留/人工项：冻结候选集中不存在第二个完整的 5 家合格工厂组，故门禁按失败规则保持未勾选，全部数据保持 draft/unverified，1+3、2+10、缓存 purge 和 unpublish→publish 未运行。若要继续，必须先批准独立的源数据修复/替换计划，不能在本门禁内换厂、重关联或降低厂区坐标标准；M2-T2 继续受阻，M2-T8 fixture Playwright 与真实 MapTiler smoke 保持未完成且不被替代。
## 2026-07-25 ｜ M2-T7（/ops PATCH CORS 修复）

- 内容：修复 staging `/ops` 保存表单时 Admin PATCH 的浏览器预检被拦截；受信 Web Origin 的 CORS 方法显式包含 GET/HEAD/POST/PUT/PATCH/DELETE/OPTIONS，公开 MAP 跨域只读策略继续严格限制为 GET/HEAD/OPTIONS，对应 PRD G-6/G-7、F-7.2 与 ADM-1/ADM-3。
- 主要文件：修改 `apps/api/src/http-application.ts` 的按请求 CORS 配置，并更新 `apps/api/test/http-application.test.ts` 锁定公开 MAP 与受信 Web 两套允许方法。未修改 Admin 契约、鉴权、Zod/OpenAPI、生成客户端或数据状态。
- 验证：真实 staging Admin 会话捕获到 PATCH 预检 204 但 `Access-Control-Allow-Methods: GET,HEAD,POST`，浏览器未发送有效 PATCH，东莞 boundary 保持未写入；修复后 `pnpm lint`、`pnpm typecheck`、`pnpm test:unit`（全 workspace 通过）、`pnpm build`、`pnpm api:generate:check`、`pnpm format:check` 与 `git diff --check` 通过。
- 遗留/人工项：本修复合并并完成 Railway staging 部署后，须重新通过 `/ops` 保存一条 draft 并确认预检允许 PATCH，再继续 M2 source remediation；尚未执行任何 verify/publish，M2 staging 真实数据门禁与 M2-T8 均保持未完成。

## 2026-07-25 ｜ M2 staging（源数据修复/替换）

- 内容：按批准的有限例外修复 Dongguan 与 Nantong 两组 canonical draft：写入两个可追溯的 WGS-84 MultiPolygon boundary，修正十家工厂的双语主体/地址、主营产品、官网与厂区级坐标，并在南通同产业带原位以 Xinyi、Jinkanghong、Nanshing 替换三条无法形成完整证据链的 draft；同步来源台账、旧值→新值证据和门禁规则，对应 PRD G-1/G-2/G-3/G-8、F-7.2 与 ADM-1/ADM-3。
- 主要文件：更新 `data/staging/real-seed/clusters.json`、`factories.json`、`data-sources.md`、`docs/operations/reviews/staging/2026-07-25-m2-source-remediation.md`、开发计划与 `AGENTS.md`；新增 real-seed 回归测试。staging 的相同内容仅通过真实 Clerk Admin `/ops` 保存，未运行 seed/import、未写状态字段、未修改 API 契约、数据库结构或生成客户端。
- 验证：canonical 仍为 10 个产业带/50 家工厂、每带 5 家、slug 唯一、图片为空；`pnpm lint`、`pnpm typecheck`、`pnpm test:unit`、`pnpm build`、`pnpm api:generate:check`、`pnpm format:check` 与 `git diff --check` 通过，PR #37 GitHub CI、API e2e、构建和 Vercel Preview 通过。staging 回读确认两个 boundary 与十家工厂内容精确匹配 canonical，全部 `draft + unverified` 且发布/核验审计字段为空；A-1/A-4/A-6、MAP-1/2/3 继续隔离为零，三条抽查公开详情为 404。
- 遗留/人工项：独立 Admin 尚须逐项目视复核两条 boundary、十个厂区落点及官方证据，并给出冻结授权声明；在此之前禁止 ADM-5 与 publish/unpublish。1+3、2+10、A-2/A-5/A-6、MAP-1/2/3、Cloudflare 缓存和 unpublish→publish 收敛尚未执行，M2 staging 门禁及 M2-T8 均保持未完成。

## 2026-07-25 ｜ M2-T6（代理协议与 MAP 缓存清理修复）

- 内容：修复真实 staging Admin publish 已提交状态后，MAP 缓存清理因 Railway 代理内层协议为 HTTP 而拒绝构造公开 HTTPS prefix 的问题；Admin 状态路由现在优先使用单值 `X-Forwarded-Proto`，并拒绝多值、逗号分隔及非 HTTP(S) 协议，对应 PRD G-6、ADM-2/ADM-4 与 MAP-* 缓存主动清理。
- 主要文件：修改 `apps/api/src/admin/admin.controller.ts` 的请求 origin 解析，新增 `apps/api/test/admin-controller.test.ts`，并让 `apps/api/e2e/admin.e2e.ts` 以真实代理头断言 purge origin 为 `https://api-staging.chinasupply.ai`。未放宽 Fastify 全局 `trustProxy`，未修改鉴权、公开 API、Zod/OpenAPI、生成客户端、数据库结构或 production。
- 验证：真实 `/ops` 中十家目标工厂完成 ADM-5 后，Dongguan 首次 publish 状态已持久化，但 API 返回 500；Railway 日志精确定位 `Remote API origin must be a public HTTPS origin`。修复后 `pnpm lint`、`pnpm typecheck`、`pnpm test:unit`、`pnpm build`、`pnpm api:generate:check` 与 `git diff --check` 通过；`pnpm --filter @chinasupply/api exec vitest run --config vitest.e2e.config.ts e2e/admin.e2e.ts` 5/5 通过。`pnpm format:check` 首次发现两处格式差异，Prettier 修复后复跑通过。
- 遗留/人工项：须在本修复合并并完成 Railway staging 部署后，通过 `/ops` 重试幂等状态操作，确认 Cloudflare purge 成功，再继续 1+3、2+10 与 unpublish→publish 收敛；M2-T8 保持未完成且不被替代。

## 2026-07-25 ｜ M2-T6（Cloudflare prefix purge 格式修复）

- 内容：修复 Cloudflare zone purge 请求把缓存 prefix 序列化为带 `https://` 的完整 URL 而被 provider 拒绝的问题；内部仍用严格 HTTPS URL 校验目标，但发送给 Cloudflare 时按官方格式转换为 `host/path`，对应 PRD G-6、ADM-2/ADM-4 与 MAP-* 主动缓存清理。
- 主要文件：修改 `apps/api/src/cache/cloudflare-cache-purge.service.ts` 与 `apps/api/test/cloudflare-cache-purge.test.ts`，锁定 `api-staging.chinasupply.ai/api/v1/map/` 请求体。未改变公开 API、Admin wire contract、Zod/OpenAPI、生成客户端、数据库结构、鉴权或 production。
- 验证：修复前真实 `/ops` unpublish 已提交状态但返回 500；Railway 日志显示 origin 已正确为 HTTPS，失败点转为 Cloudflare provider request。Cloudflare 官方文档确认 2025-04 起全部 plan 均支持 prefix purge，API 请求体示例不含 scheme；使用 Railway 相同 staging zone/token 和正确 `host/path` 做限定 MAP prefix 的诊断 purge，返回 HTTP 200、`success=true`。
- 遗留/人工项：须在本修复合并并完成 Railway staging 部署后，通过 `/ops` 重跑 publish/unpublish 并确认成功回包、公开 MAP 即时收敛与缓存年龄重置，再继续 1+3、2+10；M2-T8 保持未完成且不被替代。

## 2026-07-25 ｜ M2 staging 真实数据门禁（2+10 发布与收敛）

- 内容：接收独立 Admin 的冻结 SOP 复核声明后，仅通过 staging `/ops` 对 Dongguan、Nantong 两个产业带关联的十家工厂执行 ADM-5，并按 1+3、2+10 顺序发布；验证 A-2/A-5/A-6、MAP-1/2/3、draft/unverified 隔离、cluster/factory unpublish→publish、首次 `publishedAt` 保留及 Cloudflare MAP 缓存清理，对应 PRD G-1/G-2/G-6/G-7/G-8、F-1.2/F-1.3/F-1.4/F-1.5/F-7.2、A-2/A-5/A-6、MAP-1/2/3 与 ADM-2/ADM-4/ADM-5。
- 主要文件：完成并更新 `docs/operations/reviews/staging/2026-07-25-m2-smoke.md`，勾选 `ChinaSupply.AI开发计划.md` 中的 M2 staging 人工数据门禁，并机械修复 `data-sources.md` 的 Markdown 格式漂移；未修改公开 API、Zod/OpenAPI、生成客户端、数据库结构或业务代码，未操作 production。
- 验证：十家工厂均记录同一真实 Clerk Admin 的 `verifiedBy` 及完整 `verifiedAt/lastVerifiedAt`；1+3 与 2+10 的公开列表、详情、搜索和 MAP 数量均符合预期，两个 boundary 为有效 WGS-84 MultiPolygon，图片为空时地图卡片显示中性占位。Dongguan 与 OPPO 分别完成 unpublish→消失→publish→恢复，原始 `publishedAt` 不变；purge 后首个 MAP 请求为 `MISS`，重复请求为 `HIT` 且 `Age` 增长。真实 staging 地图在桌面右侧和 390×844 窄屏底部展示产业带/工厂卡片、verified、产品、冻结详情链接与 attribution。`pnpm lint`、`pnpm typecheck`、`pnpm test:unit`、`pnpm build`、`pnpm api:generate:check` 均通过；`pnpm format:check` 首次发现 `data-sources.md` 的既有空行格式漂移，Prettier 修复后与 `git diff --check` 一并复跑通过。
- 遗留/人工项：M2-T8 的固定 fixture Playwright 套件与正式真实 MapTiler smoke 仍未完成、未勾选且不被本次有限真实数据 smoke 替代；PR #35 仍需用户最终审阅和合并。

## 2026-07-25 ｜ M2-T1a（地图首次布局尺寸 hotfix）

- 内容：修复 PRD F-1 地图在容器布局完成前初始化时可能使用 MapLibre `400×300` 回退尺寸的问题；应用现在只在容器宽高均大于 0 后创建地图，并由单一应用层 ResizeObserver 在后续有效尺寸变化时调用 `map.resize()`。关闭 MapLibre 内部 resize 跟踪，避免两个 observer 重复触发 move 事件和 viewport 请求。
- 主要文件：新增 `apps/web/src/app/(frontend)/map/map-container-size.ts`，调整 `industrial-map.tsx` 的初始化、resize 与卸载生命周期；新增 `map-container-size.test.ts`，覆盖零尺寸拒绝、首次非零通知、后续尺寸变化和 observer 清理。未修改地图数据、API、Zod/OpenAPI、生成客户端、筛选、卡片或 analytics。
- 验证：Web `lint`、`typecheck` 与 70/70 单测通过；`pnpm lint`、`pnpm typecheck`、`pnpm test:unit`、`pnpm build`、`pnpm api:generate:check`、`pnpm format:check` 和 `git diff --check` 全部通过。production build 在 1280×720 无缓存首次加载时，容器、canvas CSS 尺寸和 canvas attributes 均为 1280×720；强制容器先为 0×0 时不创建 canvas，恢复为 1280×720 后约 42ms 自动创建同尺寸 canvas，无用户交互；后续调整为 1024×600 时 canvas 自动同步。
- 遗留/人工项：PR 合并部署 staging 后仍需复测真实首次落地；M2-T8 的固定 fixture Playwright 与正式 staging MapTiler smoke 保持未完成且不被本 hotfix 替代。

## 2026-07-25 ｜ M2-T2（Web 搜索）

- 内容：实现 PRD F-3.1~F-3.6、G-2、F-11.3、N-4/N-6 的 Web 产品反查：300ms 防抖调用 A-6，按类目/产业带/工厂分组并支持完整键盘操作；类目结果联动 MAP-* 筛选，产业带/工厂结果 flyTo 后复用现有卡片补全详情；无结果时按 A-7 `sortOrder` 展示前 5 个一级类目。补齐 `packages/analytics` consent-aware 门面，默认或未授权时完全 no-op，`search_performed` 在包内完成邮箱/电话脱敏及 100 字符截断；未安装或加载 PostHog SDK。
- 主要文件：新增 `apps/web/src/app/(frontend)/map/map-search*` 搜索控制器、结果视图与纯函数模型，扩展 `industrial-map.tsx` 的 category 查询参数、筛选清理和搜索定位动作，新增响应式样式与英语 i18n；将 `packages/analytics` 从占位包完善为带类型、测试和可注入 capture adapter 的共享包。A-6/A-7/MAP-* schema、OpenAPI 和 Orval 生成产物未修改。
- 验证：`pnpm lint`、`pnpm typecheck`、`pnpm test:unit`（378/378）、`pnpm test:e2e`（40/40，真实 PostGIS + Redis + MinIO/BullMQ）和 `pnpm build` 均通过；浏览器使用临时只读 HTTP fixture 验证三组结果、键盘选择产业带卡片、无结果前 5 类目、类目筛选与零 PostHog 资源。Staging A-6 验证 `led`/`socks`/`sofa`/`家具` 均 200 且命中相关类目/实体，101 字符为 400 `VALIDATION_ERROR`；5 次热请求中位数分别为 232/222/216/215ms，存在 595~1325ms 的跨网冷连接/抖动尖峰，M1-T9 隔离负载基线仍为搜索 p95 139.84ms。
- 遗留/人工项：分支部署到 PR Preview/staging 后需使用真实 MapTiler 与 Clerk 配置复核完整地图 flyTo 和卡片视觉；当前本地外部服务为占位配置，浏览器中底图与 Clerk SDK 无法加载。Staging 跨网偶发延迟尖峰需在正式环境按既有负载 SOP 持续观察；Consent banner 与真实 PostHog 初始化仍按计划留在 M3-T6。

## 2026-07-25 ｜ M2-T3（一级类目筛选 Chips）

- 内容：实现 PRD G-2、F-1.6/F-1.7 的 Web 一级类目单选 chips；A-7 按 `sortOrder` 提供常驻一级类目与颜色，首项 All categories 清除筛选。界面选择立即高亮并清理旧图层/卡片/truncated 状态，MAP-1/2/3 在 500ms 防抖后应用最终 category slug，快速切换取消旧请求；A-6 命中的二级类目保留精确 slug，不扩大为父类筛选，并显示可关闭标签。
- 主要文件：新增 `map-category-chips.tsx`，扩展 `industrial-map.tsx`、`map-search.tsx` 与纯函数模型，补齐单行横向滚动、响应式状态样式、英语 i18n 和 Web 单测。A-7、MAP-* schema、OpenAPI、Orval 生成客户端、URL 状态与 analytics 均未修改；M2-T8 Playwright 未提前实现或勾选。
- 验证：`pnpm lint`、`pnpm typecheck`、`pnpm test:unit`（全 workspace 通过，Web 85/85）、`R2_MEDIA_BUCKET=chinasupply-dev-media pnpm build`、`pnpm api:generate:check`、`pnpm format:check` 与 `git diff --check` 全部通过。浏览器使用临时只读 HTTP fixture 在 1280×720 与 390×844 验证一级顺序、颜色、All 清除、快速 Electronics→Lighting 仅发最终请求、二级 `home-furniture` 精确参数与横向滚动；窄屏 chips clientWidth 306px、scrollWidth 437px，未越出视口。
- 遗留/人工项：本地 MapTiler/Clerk 外部资源无法加载，故真实地图视口触发的 MAP-2/MAP-3 与真实 MapTiler 视觉仍需在 PR Preview/staging 复核；M2-T8 的固定 MSW/style/tile fixture Playwright、正式 staging MapTiler smoke 与 Lighthouse SEO 门禁保持未完成。

## 2026-07-25 ｜ M2-T4（产业带详情页）

- 内容：实现 PRD F-2、A-2/A-3、G-1/G-2/G-5、N-2/N-4/N-6 的 Web `/clusters/[slug]`；A-2/A-3 首页并行进入完整 SSR HTML，页面按 900 秒按需 ISR，metadata 提供本地化 title/description、canonical、英语 hreflang、Open Graph 与可选封面。详情依次呈现名称/城市、禁用收藏占位、小地图、主营产品、实时 factoryCount、可选 stats、安全 Markdown 和无限工厂列表；无效或未发布 slug 返回 404，服务故障返回 noindex 错误页并可在恢复后清理该 slug 缓存重试。新增 consent-aware `cluster_viewed` analytics 事件，未授权或未配置适配器时继续完全 no-op。
- 主要文件：新增 `apps/web/src/app/(frontend)/clusters/[slug]/` 的数据缓存、metadata、页面/状态、MapLibre 预览、Markdown、工厂无限列表、响应式样式与精确缓存重试；更新英语 i18n、Web 测试与 `react-markdown@10.1.0` 依赖，扩展 `packages/analytics` 及测试。未修改 API、Zod/OpenAPI、Orval 生成客户端、Payload 或 Mobile；工厂卡链接按计划指向后续 M2-T5 的 `/factories/[slug]`。
- 验证：`pnpm install --frozen-lockfile --offline`、`pnpm lint`、`pnpm typecheck`、`pnpm test:unit`（全 workspace 392/392，Web 92/92）、`pnpm build`（`/clusters/[slug]` 输出为 `●` 按需 SSG）、`pnpm api:generate:check`、`pnpm format:check` 与 `git diff --check` 全部通过。production build 使用固定 A-2/A-3 HTTP fixture 验证首个响应 `Cache-Control: s-maxage=900`、SSR 首页 20 张卡、键盘加载不透明 cursor 后共 22 张、404、5xx 恢复重试、boundary 与 centroid 降级、地图错误重试、attribution，以及 1280px/390×844 响应式无横向溢出；Lighthouse 13.4.1 SEO 为 100。
- 遗留/人工项：收藏仍为批准的禁用 UI 占位，真实登录回跳与 A-8 留 M3-T1；工厂详情链接须待 M2-T5 落地。M2-T8 的仓库内固定 Playwright/style/tile fixture 与正式 staging MapTiler smoke 仍未完成且不被本任务替代；本任务未访问 staging/production、未部署或执行 Cloudflare purge。

## 2026-07-25 ｜ M2-T4（产业带详情硬 404 修复）

- 内容：修复 Web `/clusters/[slug]` 在按需 ISR 下把不存在或未发布产业带的 `notFound()` 结果缓存为 HTTP 200 的软 404；移除 slug 校验前的路由级流式 loading 边界，使 A-2 返回 400/404 时先完成 `notFound()` 判定，同时保留有效页面的 900 秒 SSR/ISR，对应 PRD A-2、F-2.2 与 N-2。
- 主要文件：删除 `apps/web/src/app/(frontend)/clusters/[slug]/loading.tsx`，清理其专用 skeleton 样式和英语文案；扩展 `cluster-detail.test.tsx`，锁定该动态段不得在 `notFound()` 判定前重新引入路由级流式边界。未修改页面数据请求、`generateStaticParams()`、`revalidate`、API、Schema、生成客户端、Payload 或 Mobile。
- 验证：固定 A-2/A-3 HTTP fixture + production build 下，不存在 slug 与 draft slug 均为首次 `404/MISS`、重复请求 `404/HIT`；published slug 保持首次 `200/MISS`、重复请求 `200/HIT`，响应仍含 `Cache-Control: s-maxage=900` 与 `x-nextjs-prerender: 1`。`pnpm lint`、`pnpm typecheck`、`pnpm test:unit`（全 workspace 通过，Web 93/93）、`pnpm build`（`/clusters/[slug]` 仍为 `●` 按需 SSG）、`pnpm api:generate:check`、`pnpm format:check` 与 `git diff --check` 全部通过。
- 遗留/人工项：PR 合并并完成 Preview/staging 部署后，仍需对真实不存在 slug、真实 draft slug 与 published slug 复核相同状态矩阵；M2-T8 的固定 Playwright/style/tile fixture 与正式 staging MapTiler smoke 保持未完成且不被本修复替代。

## 2026-07-25 ｜ M2-T5（工厂详情页）

- 内容：实现 PRD F-4、F-6.2/F-6.3、A-5 与 N-2 的 Web `/factories/[slug]`；A-5 通过 React request cache 复用并按 900 秒按需 ISR，提供 canonical、英语 hreflang、描述与可选首图 Open Graph metadata。页面展示核验月份与来源、可访问图片轮播、按存在性渲染的工厂信息、不可交互 WGS-84 MapLibre 点位、双语地址复制、实际联系方式、平台化导航与相关工厂轨道；400/404 为硬 404，服务故障返回本地化 `noindex` 重试页。
- 主要文件：新增 `apps/web/src/app/(frontend)/factories/[slug]/` 的数据缓存、metadata、页面/状态、轮播、联系方式、地址复制、MapLibre 小地图、导航回落、相关工厂与响应式样式；补齐英语 i18n 和 Web 单测。Web 新增 `@chinasupply/geo` 运行依赖并在独立 dev 前构建共享包；扩展 `packages/analytics` 的 `factory_contact_clicked` 与 `navigation_clicked` consent-aware 事件，payload 仅含工厂 ID、slug、交互类型、导航提供商与平台。未修改 API、Zod/OpenAPI、Payload、Mobile 或生成产物。
- 验证：`pnpm install --frozen-lockfile --offline`、`pnpm lint`、`pnpm typecheck`、`pnpm test:unit`（全 workspace 通过，Web 105/105、analytics 9/9）、`pnpm build`（`/factories/[slug]` 输出为 `●` 按需 SSG）、`pnpm api:generate:check`、`pnpm format:check` 与 `git diff --check` 全部通过。固定 A-5 HTTP fixture + production build 验证 published `200/HIT`、missing `404/HIT`、5xx 本地化 `noindex` 错误页、`Cache-Control: s-maxage=900`、canonical/hreflang、零图片隐藏、地图错误重试与强制 attribution；1280px 和 390×844 均无横向溢出，桌面导航矩阵与地址复制反馈正常，Lighthouse 13.4.1 SEO 为 100。
- 遗留/人工项：PR Preview/staging 仍须使用真实 MapTiler key 复核点位和底图加载，并在真实 iOS/Android 环境复核高德/百度 App URI 回落；F-6.4 Release 包真机落点复验仍属于 M4-T5。M2-T8 的仓库内固定 Playwright/style/tile fixture 与正式 staging MapTiler smoke 保持未完成且不被本任务替代；本任务未访问 staging/production、未执行 Cloudflare purge，也未接入收藏、登录回跳或真实 PostHog SDK。

## 2026-07-25 ｜ M2-T8（Web Playwright）

- 内容：为 PRD F-1.2~F-1.7、F-2、F-3、F-4、A-2/A-5/A-6/A-7、MAP-1/2/3 与 N-5 建立 Chromium Playwright 核心路径；fixture 套件通过 Next test proxy 同时拦截浏览器与 App Router 服务端 fetch，覆盖首次地图、MAP-1 点选、卡片 skeleton/补全、失败重试、搜索 flyTo、产业带/工厂详情、防抖、AbortSignal 与 truncated。真实 staging 套件独立验证 MAP-1、MapTiler TileJSON/矢量瓦片、Canvas、强制 attribution 和 Dongguan 搜索卡片，不进入 PR CI。
- 主要文件：新增 `apps/web/playwright.config.ts`、`playwright.staging.config.ts` 与 `apps/web/e2e/` 的类型安全数据、Orval MSW handler、固定 MapTiler 资源、请求探针和两套测试；精确加入 `@playwright/test@1.62.0` 及根命令。`apps/web/next.config.ts` 仅在 `PLAYWRIGHT_TEST=1` 启用 testProxy；CI 新增独立 `Web Playwright` job、失败产物上传并纳入 `CI Gate`，同步更新 workflow 契约、Turbo 环境输入、忽略规则和 CI/CD 文档。将桌面 truncated 状态条移到搜索控件下方，避免被高层级搜索框遮挡；未修改 API、Zod/OpenAPI、Orval 生成产物、Mobile、鉴权或收藏。
- 验证：`pnpm test:web:e2e` 5/5 通过且无真实外网依赖；`PLAYWRIGHT_STAGING_BASE_URL=https://staging.chinasupply.ai pnpm test:web:e2e:staging` 1/1 通过，真实 MAP-1、MapTiler TileJSON/瓦片和 Dongguan 路径成功；CI workflow 契约 6/6 通过。`pnpm lint`、`pnpm typecheck`、`pnpm test:unit`（407/407）、`pnpm build`、`pnpm api:generate:check`、`pnpm format:check` 与 `git diff --check` 全部通过。
- 遗留/人工项：无。

## 2026-07-25 ｜ M2-T9（Streets v4 2D 底图迁移）

- 内容：将共享 MapLibre 底图从 Planet v3 schema 迁移到 MapTiler Planet v4，保留完整道路、交通、POI 与街道细节，英文名优先并以当地名称 fallback；建筑仅在 zoom ≥15 显示淡化 2D footprint，固定 pitch=0 并禁止 `fill-extrusion`。产业带 fill 统一插入首个底图 symbol layer 之前，边界线与业务点保持在标签之上，对应 PRD F-1.2~F-1.5、F-2.1、F-4.1 与 F-11.2。同步冻结 Local/Staging Free 仅供测试评估、commercial production 必须 Flex 的 M5-T9 门禁。
- 主要文件：重写 `packages/config/map/chinasupply-light.json`，新增 Planet v4 schema manifest 与样式来源说明，扩展 `packages/config/test/map-style.test.mjs` 的 schema/tileset 一致性、完整 Streets 细节、托管 style 禁令、2D 与 validator 护栏；首页、产业带小地图与工厂小地图共享 label anchor 和 pitch 约束，Playwright 升级为固定 v4 TileJSON/PBF/glyph/sprite fixture 及 staging 资源响应护栏。MapTiler Cloud 已创建 `ChinaSupply Streets v4 Evaluation`，但 Free 实时界面将 Download style 标为 Flex 功能，因无法同包取得并确认 `LICENSE.txt`，仓库没有复制或派生官方 Streets 图层定义。
- 验证：`pnpm --filter @chinasupply/config test:unit`（35/35）、`pnpm test:web:e2e`（5/5，禁止意外外网）、`pnpm lint`、`pnpm typecheck`、`pnpm test:unit`、`pnpm build`、`pnpm mobile:check`（expo-doctor 18/18，iOS/Android export 通过）、`pnpm install --frozen-lockfile --offline`、`pnpm api:generate:check`、`pnpm format:check` 与 `git diff --check` 均通过。记录了迁移前真实 staging v3 基线：1280×720 下 12 个 MapTiler 请求、响应体合计 5,039,391 bytes。
- 遗留/人工项：分支尚未合并部署，因此未伪造 v4 staging 验收；合并后需确认 Planet v4 TileJSON/PBF/glyph/三组 sprite 全部 200，人工检查中国全境、zoom 10 产业带与 zoom 14–15 工厂场景，记录迁移后请求数/传输量，并在美国网络完成 Lighthouse LCP `<2.5s`。上述门禁通过前 M2-T9 保持未勾选；commercial production 仍必须先升级 Flex、确认授权与账单限额、创建三平台受限 production keys。

## 2026-07-26 ｜ M2-T9（验收状态确认）

- 内容：PR #48 已合并到 `main`；Owner 明确接受 Streets v4 2D 底图实现与全部自动化验收，要求将开发计划中 M2-T9 勾选，同时将真实 staging 视觉/资源与 Lighthouse 检查延后由 Owner 复核，对应 PRD F-1.2~F-1.5、F-2.1、F-4.1 与 F-11.2。
- 主要文件：勾选 `ChinaSupply.AI开发计划.md` 的 M2-T9，并更新 `docs/operations/m2-t9-streets-v4.md` 明确延后复核不得被记录为已通过；未修改地图代码、样式、测试或生产配置。
- 验证：确认 `main` 包含 PR #48（merge commit `1632d1e`），该 PR 的 CI Gate、lint/typecheck/unit、Build、API e2e、Web Playwright、Mobile 与 Vercel Preview 全部通过；本次仅为文档状态变更。
- 遗留/人工项：Planet v4 TileJSON/PBF/glyph/三组 sprite 真实 staging 200 复核、中国全境/zoom 10 产业带/zoom 14–15 工厂场景人工检查、迁移后请求数/传输量和美国网络 Lighthouse LCP `<2.5s` 均尚未执行，由 Owner 稍后完成；M5-T9 的 Flex、商业授权、账单限额与三平台 production keys 门禁不变。

## 2026-07-26 ｜ M3-T1（Clerk Web 接线）

- 内容：实现公开 Clerk Web 邮箱验证码/Google 登录注册入口与产业带收藏按钮回跳，登录和新用户注册均回到原 `/clusters/{slug}`；已登录收藏仍明确留给 M3-T4，对应 PRD G-2、G-7、F-2.3 与 F-8.1。
- 主要文件：新增 `/sign-in/[[...sign-in]]`、`public-auth-routes.ts` 和产业带 `cluster-save-action.tsx` 客户端小岛；将全局 ClerkProvider 默认路径切换到公开登录，保留 `/ops/sign-in` 独立强制回跳与 admin 二次校验，Payload `cms_users` 边界不变；同步英语 i18n、Web/Clerk 环境文档和单测。未修改 API、Zod/OpenAPI、生成客户端、数据库或 Payload schema。
- 验证：Web 定向单测 113/113 通过；`pnpm lint`、`pnpm typecheck`、`pnpm test:unit`、`pnpm build`、`pnpm test:web:e2e`（5/5）与 `pnpm api:generate:check` 通过，production build 显示 `/clusters/[slug]` 仍为 `●` 按需 SSG/ISR。Playwright 首次发现过宽 Clerk matcher 会触发外网 handshake，收窄为仅 `/sign-in` 与 `/ops` 后复跑通过且继续零外网。`pnpm format:check` 首次发现两个测试文件格式差异，Prettier 修复后复跑通过。
- 遗留/人工项：未操作外部 Clerk Dashboard 或 staging。授权人员仍须启用公开注册、邮箱验证码与 Google sign-up/sign-in，保留 `metadata.role` claim，并完成新/旧邮箱用户、Google 新用户、原产业带回跳、直接登录 fallback 及 `/ops` 三权限真实 smoke；完成前 M3-T1 保持未勾选。M3-T1 期间产生的 Clerk 用户留待 M3-T2 Webhook 和 M3-T3 回填。

## 2026-07-26 ｜ M3-T1（Clerk staging 配置与验收）

- 内容：在 staging Clerk Development instance 关闭 Restricted mode、开放自助注册，启用注册/登录邮箱验证码并关闭邮箱密码与邮件链接；确认 Google sign-up/sign-in 使用 Clerk Development shared credentials，保留 `metadata = {{user.public_metadata}}` session claim 与管理员 `role=admin` 边界。Owner 明确要求“直接通过”，据此勾选 M3-T1；该人工接受不被记录成未执行测试的通过证据，对应 PRD G-2、G-7、F-2.3 与 F-8.1。
- 主要文件：更新 `ChinaSupply.AI开发计划.md`、Web/环境运维文档与外部账号台账，记录当前 Clerk staging 状态和人工豁免；未提交 Clerk/OAuth 密钥，未修改 API、Zod/OpenAPI、生成客户端、数据库、Payload schema、Webhook、用户回填、收藏 API 或账户页。M3-T1 实现已由 PR #50 合并到 `main`。
- 验证：现有邮箱用户和新邮箱用户均以验证码完成登录/注册；直接 `/sign-in` 回到 `/`；从产业带 Save 发起后回到相同 `/clusters/dongguan-electronic-information`，已登录按钮保持禁用并提示等待 M3-T4；现有 Google 用户 OAuth 成功；匿名 `/ops` 回到 `/ops/sign-in?redirect_url=%2Fops`，普通用户进入 `/ops/forbidden`，带 `metadata.role=admin` 的管理员进入 `/ops`。合并后 canonical staging 的 `/sign-in` 返回 HTTP 200 并命中 `/sign-in/[[...sign-in]]`，匿名 `/ops` 返回 307 管理员登录回跳，产业带详情返回 HTTP 200 与 `x-nextjs-prerender: 1`；PR #50 的 CI Gate 与 Vercel 检查已通过。本次文档收尾的 `pnpm format:check` 与 `git diff --check` 通过。
- 遗留/人工项：未使用第二个、此前从未注册此 Clerk instance 的 Google 身份执行“Google 新用户”smoke；Owner 于 2026-07-26 明确豁免并接受交付，勾选状态不代表该场景已通过。M3-T1 期间产生的 Clerk 用户仍须由 M3-T2 Webhook 和 M3-T3 回填接续；生产 Google custom credentials 不属于本任务。

## 2026-07-26 ｜ M3-T2（Clerk Webhook 与删除生命周期）

- 内容：实现 PRD G-3/G-4/G-6/G-7、A-11、F-8.2/F-8.4 与 N-3；API 通过 Nest/Fastify raw body 和 `@clerk/backend/webhooks` 验证 Svix 签名，在单一 PostgreSQL 事务内以 `svix-id` 去重并同步 `user.created`/`user.updated`/`user.deleted`。用户删除保留 `users` tombstone、硬删 favorites，迟到的 created/updated 不清除 `deleted_at`；新增要求同步用户存在且未删除的 `UserAuthGuard`，留给 M3-T4 用户路由接入。
- 主要文件：新增 `apps/api/src/webhooks/`、`apps/api/src/auth/user-auth.guard.ts` 与 `apps/api/e2e/webhooks.e2e.ts`；HTTP 入口启用 raw body，远程 API 环境要求 `CLERK_WEBHOOK_SECRET` 而 Worker 不要求；共享 Webhook Zod schema 校验声明的主邮箱确实存在。现有 Admin 鉴权行为、数据库 schema/migration、A-8～A-10 路由、Web/Mobile 和生成 API client wire shape 均未改变。
- 验证：`pnpm install --frozen-lockfile`、`pnpm api:generate:check`、`pnpm lint`、`pnpm typecheck`、`pnpm test:unit`（424/424）、`pnpm test:e2e`（45/45，含真实 Standard Webhooks 签名、raw body、并发重放、用户更新、收藏硬删、tombstone 与 401）、`pnpm build`、`pnpm mobile:check`（Expo Doctor 18/18、TypeScript、配置、iOS/Android export）、`pnpm format:check` 与 `git diff --check` 全部通过。
- 遗留/人工项：未操作 Clerk/Railway 或 staging。合并前须把真实 `CLERK_WEBHOOK_SECRET` 仅加入 Railway API；部署后须在 Clerk Development instance 创建只订阅 created/updated/deleted 的 staging endpoint，并用一次性用户完成真实生命周期 smoke。上述门禁通过前 M3-T2 保持未勾选；M3-T3 仍须回填 Webhook 启用前已有用户。

## 2026-07-26 ｜ M3-T2（staging 配置与生命周期验收）

- 内容：完成 PRD A-11、F-8.2、F-8.4 与 N-3 的外部门禁并勾选 M3-T2：在 Clerk Development instance 创建 staging lifecycle endpoint，仅订阅 `user.created`、`user.updated`、`user.deleted`；将 signing secret 仅配置到 Railway API，并在 PR #52 合并后的 staging 版本完成一次性用户 created → updated → deleted 真实 smoke。
- 主要文件：更新 `ChinaSupply.AI开发计划.md` 与 `docs/operations/environments.md` 的验收状态和运维证据；未修改 API 代码、数据库 schema/migration、生成产物、Web 或 Mobile。首次验收时受控工具输出显示过 signing secret，按泄露立即在 Clerk 轮换、更新 Railway API 并重新部署；Worker 始终未配置该变量，临时密钥与一次性用户资料均已清理。
- 验证：PR #52 的 main CI、Staging CMS/Core migration 与 Release Gate 全部成功；Railway API/Worker 运行合并提交且为 `SUCCESS`，`/health/ready` 确认 PostgreSQL/Redis 为 `up`。Clerk 三类 lifecycle 投递均为 `Succeeded`；数据库确认 created/updated 的邮箱、姓名与 `locale`，deleted 后 users 行保留且 `deleted_at` 非空、favorites 为 0、三类 `webhook_events` receipt 各一条。轮换后再次创建并删除第二个一次性用户，确认新 secret 部署可接收事件并生成 tombstone；两名 Clerk 用户随后均已删除。
- 遗留/人工项：Svix 对已轮换旧 secret 的自动 24 小时兼容窗口尚未自然结束，无需人工操作；M3-T3 仍须回填 endpoint 启用前已有的 Clerk 用户。

## 2026-07-26 ｜ M3-T3（Clerk 用户回填命令）

- 内容：实现一次性 `clerk:sync-users` 命令，通过 Clerk Backend API 以 500 条/页拉取现有用户并在单一事务内只插入缺失的核心 `users` 行，对应 PRD G-3/G-8、3.5、A-11、F-8.2/F-8.4。命令保留数据库默认 `locale=en`，冲突时不覆盖 active 用户或 tombstone，不删除数据库孤立行、不修改 favorites，也不伪造 webhook receipt；Local 无确认参数、Staging 必须精确 `--confirm-staging`，Production 在连接外部服务前拒绝。
- 主要文件：新增 API 编译入口与 `users` 共享 Clerk 字段映射/分页回填逻辑，Webhook 改为复用相同主邮箱和姓名规则；新增独立 Zod CLI 环境切片、根/API 命令、单元与 PostGIS Testcontainers e2e，并补充 API/环境运维说明。未修改数据库 schema/migration、HTTP API、OpenAPI/Orval 生成产物、Web 或 Mobile。
- 验证：`pnpm lint`、`pnpm typecheck`、`pnpm test:unit`（431/431）、`pnpm test:e2e`（47/47，含 M3-T3 2/2）、`pnpm build`、`pnpm api:generate:check`、`pnpm format:check` 与 `git diff --check` 全部通过；额外使用编译后的 CLI 确认 production guard 返回失败且未尝试连接占位 Clerk/PostgreSQL。
- 遗留/人工项：尚未从 CI 通过的已提交代码执行 staging 首次回填、第二次幂等复跑和聚合数据库核对，因此 M3-T3 暂不勾选；完成后须在同一 PR 补录无 PII 的 aggregate evidence。

## 2026-07-26 ｜ M3-T3（staging 用户回填验收）

- 内容：在 PR #54 的精确实现提交通过 CI Gate 后，对 staging Clerk Development instance 与核心 `users` 表执行 M0-M2 存量用户回填并勾选 M3-T3，对应 PRD G-3/G-8、3.5、A-11、F-8.2/F-8.4；未清空或重建 Clerk 用户，未访问 production。
- 主要文件：更新 `ChinaSupply.AI开发计划.md` 与环境运维文档，记录无 PII 的回填、幂等复跑和聚合核对证据；未修改实现代码、数据库 schema/migration、HTTP API、OpenAPI/Orval、Web 或 Mobile。
- 验证：PR #54 提交 `541ee9a` 的 CI Gate、Build、API e2e、Web Playwright、Mobile、lint/typecheck/unit 全部成功。回填前 Clerk=4、core active=0、tombstone=2、missing=4；首次命令 `fetched=4/inserted=4/existing=0`，复跑 `fetched=4/inserted=0/existing=4`。聚合 postflight 为 core=6、active=4、tombstone=2、missing=0、extra active=0，4/4 主邮箱与规范化姓名匹配，审核管理员为 active；postflight favorites=0、既有 webhook receipts=5。全过程未输出或记录密钥、数据库 URL、邮箱、姓名或完整用户清单。
- 遗留/人工项：无。

## 2026-07-26 ｜ M3-T4（收藏与账户 API 接线）

- 内容：实现 PRD G-4/G-5/G-6/G-7/G-11、A-8/A-9/A-10、F-5.3 与 F-8.3/F-8.4；新增需登录的收藏列表、published 目标幂等收藏/取消、本地账户资料更新与 Clerk 账户删除请求。收藏按 `created_at DESC, id DESC` 使用不透明 cursor，已收藏目标下架后返回 `target=null`；首次收藏 draft/不存在目标统一 404。用户写接口经现有 Redis store 按用户、按路由限制 60 req/min，Redis 命令增加 3 秒超时以保持故障时快速 fail-closed。
- 主要文件：新增 `apps/api/src/favorites/`、`apps/api/src/account/`、`apps/api/src/rate-limit/user-throttler.guard.ts` 与 `apps/api/e2e/account-favorites.e2e.ts`；扩展公开 Cluster/Factory service 的批量 summary 复用入口并在 AppModule 注册新模块。未修改数据库 schema/migration、Zod/OpenAPI wire shape、Orval 生成客户端、Web、Mobile 或 Payload。
- 验证：`pnpm lint`、`pnpm typecheck`、`pnpm test:unit`（API 122/122）、`pnpm build`、`pnpm api:generate:check`、`pnpm format:check` 与 `git diff --check` 全部通过；`pnpm test:e2e` 最终 56/56 通过。首次完整 e2e 的既有 Redis 停机 fail-closed 用例超过 15 秒，增加 3 秒 `commandTimeout` 后定向 3/3、完整 56/56 复跑通过；新增 M3-T4 e2e 9/9 覆盖鉴权、cursor、draft 隔离、并发幂等、用户隔离、PATCH 校验、Clerk 成败边界和 429。
- 遗留/人工项：自动测试仅使用 Clerk mock，未删除真实 staging/production 用户；真实账户删除 → Clerk `user.deleted` webhook → 本地 tombstone/收藏硬删除的跨系统回放与 staging 生命周期验收留给 M3-T8，或须使用另行明确授权的一次性测试账号。

## 2026-07-26 ｜ M3-T5（收藏页与账户页）

- 内容：实现 PRD G-2/G-5/G-7/G-8、F-5.1/F-5.2/F-5.3 与 F-8.3/F-8.4 的 Web 交互；新增登录可见的 Factory/Industrial clusters 双 Tab 收藏页、匿名登录引导、产业带/工厂详情收藏入口、账户邮箱/英语偏好/登出/二次确认删除，以及 Map/Saved/Account 共享导航。认证请求统一按需附加 Clerk bearer token，401 清空私有缓存并登出；favorites query key 包含 Clerk user ID，创建后 upsert + invalidate，取消采用 cancel + 乐观移除 + 失败回滚 + 成功 invalidate。
- 主要文件：新增 `apps/web/src/app/(frontend)/favorites/`、`account/`、共享导航和 `apps/web/src/auth/protected-api.ts`，替换产业带收藏占位并为工厂详情补充收藏；新增英语 i18n、metadata 与 17 项定向测试。`target=null` 仅显示不可用状态并允许取消，不暴露内部目标 ID；A-8～A-10、Zod/OpenAPI、Orval 生成产物、数据库、Payload 与 Mobile 均未修改。
- 验证：`pnpm lint`、`pnpm typecheck`、`pnpm test:unit`（全 workspace 456/456，Web 130/130）、`pnpm build`（`/favorites`、`/account` 静态输出，clusters/factories 保持按需 SSG）、`pnpm test:web:e2e`（固定 MSW/style/tile fixture 5/5）、`pnpm api:generate:check`、`pnpm format:check` 与 `git diff --check` 全部通过。
- 遗留/人工项：PR Preview/staging 仍需使用真实 Clerk 普通账号复核产业带/工厂收藏、双 Tab 取消、英语偏好保存、登出与窄屏导航；真实账户删除 → Clerk `user.deleted` webhook → 本地 tombstone/favorites 硬删除的破坏性跨系统验收按计划留给 M3-T8 的一次性 staging 账号，不在本任务操作 production 或删除现有账号。

## 2026-07-26 ｜ M3-T6（Web Consent）

- 内容：实现 PRD F-11.3、F-1.7 与 N-4；买家公共页面首次访问显示非阻塞 Consent banner，选择以 `chinasupply.analytics-consent.v1` 持久化并可从公共导航随时更改。只有明确同意后才动态导入 `posthog-js@1.407.2`；unknown、denied、撤回、存储/SDK 故障与排除路由均保持 fail-closed。六类冻结事件只允许手工 capture，补齐 `factory_viewed` 与初始视口后 500ms 防抖的 `map_moved`，共享层按成功发送结果执行 10 秒节流。
- 主要文件：扩展 `packages/analytics` 的 consent subscription、factory/map 类型与故障隔离；新增 `apps/web/src/analytics/` PostHog 适配器和 Consent 控制器、公开布局 Provider/设置入口及工厂/地图 tracker；补充英语 i18n、固定 PostHog Playwright fixture、单元/E2E 测试和 Web/analytics/环境运维文档。`/ops/**`、`/sign-in/**` 与 Payload `/admin/**` 不展示入口且不会从已保存同意触发首次 SDK 加载；未修改 API、数据库、Payload schema、Mobile、法律页面或生成产物。
- 验证：`pnpm install --frozen-lockfile --offline`、`pnpm lint`、`pnpm typecheck`、`pnpm test:unit`（全 workspace 通过，Web 140/140、analytics 14/14）、`pnpm build`、`pnpm test:web:e2e`（固定 MSW/style/tile/PostHog fixture 8/8）、`pnpm api:generate:check`、`pnpm format:check` 与 `git diff --check` 全部通过。Playwright 覆盖 unknown/deny 零 PostHog 请求、拒绝 reload 持久化、grant 后才发送、revoke 后停止，以及 1280×720/390×844 和键盘交互。
- 遗留/人工项：实现 PR 合并部署后，仍须在 canonical staging 用干净浏览器分别验证 unknown、deny、grant、revoke，并在 PostHog Live Events 核对六个命名事件、搜索邮箱/电话脱敏、`map_moved` 十秒节流及无额外自动事件；完成后用同一 T 编号验收 PR 补录证据并勾选 M3-T6。本实现任务未访问 staging/production，因此开发计划保持未勾选。

## 2026-07-26 ｜ M3-T7（Privacy 与 Terms 法律页面实现）

- 内容：实现 PRD F-11.1 的公开 `/privacy` 与 `/terms` 英语法律页面，使用共享 Server Component、`generateMetadata`、语义化长文结构、固定章节锚点和响应式样式；两页披露 `HUANG SOURCING LTD`、Company No. `17241958`、英格兰和威尔士注册地、官方登记地址与 Owner 指定邮箱。Privacy 按 ICO right-to-be-informed checklist 覆盖产品实际数据处理，Terms 明确免费目录、Verified 边界、买方尽调责任、可接受使用、免责声明、`£100` 责任上限及英格兰和威尔士法律。注册页与 Consent 面板加入固定法律入口，两页互链并可返回地图。
- 主要文件：新增 `apps/web/src/app/(frontend)/privacy/`、`terms/`、共享 `legal/` 组件、`apps/web/src/legal/legal-routes.ts`、英语 `Legal` i18n 文案及单元/Playwright/staging smoke 测试；更新 `docs/operations/m0-t0-external-accounts.md` 并新增 `docs/operations/m3-t7-legal-pages.md`，记录 Companies House/GOV.UK/ICO 事实源、Owner 审批门禁和待补 staging 证据。未修改 API、数据库、Payload、Mobile、`/about`、sitemap、production 或 M3-T6 状态。
- 验证：`pnpm lint`、`pnpm typecheck`、`pnpm test:unit`（全 workspace 475/475，Web 144/144）、`pnpm build`、`pnpm test:web:e2e`（固定 MSW/style/tile/PostHog fixture 10/10）、`pnpm api:generate:check`、`pnpm format:check` 与 `git diff --check` 全部通过；构建将 `/privacy`、`/terms` 均标记为 `○` 静态预渲染路由，本地 production server curl 确认两页均为 HTTP 200 且输出准确 title、公司名称和更新时间。Playwright 覆盖桌面、390px、键盘章节导航、公司披露、更新时间、互链及 Consent Privacy 入口，且没有意外外部请求。
- 遗留/人工项：法律英文稿仍须 Owner 在 Draft PR 中逐字审阅并明确批准；批准提交 CI 全绿并合并后，须等待 `main` staging release gate 与 Vercel 部署，再对 canonical staging 两条 URL、metadata、正文、注册页和 Consent 入口执行定向 smoke 并回填证据。上述门禁完成前不部署、不勾选 M3-T7；Companies House 当前 SIC 为 `99999 - Dormant Company`，Owner 须在 production 前与会计或法律顾问确认运营与申报状态。M3-T6 状态不变。

## 2026-07-26 ｜ M3-T7（法律正文批准与 staging 验收）

- 内容：Owner 明确表示“已审阅，批准，请继续”，批准提交 `41d2033` 的精确英文 Privacy Policy 与 Terms of Use；批准记录已写入 PR #58，且在无后续正文变更的情况下转为 Ready 并合并为 `09f8325`。完成 PRD F-11.1 的 canonical staging 上线验收并勾选 M3-T7；M3-T6 状态不变。
- 主要文件：更新 `ChinaSupply.AI开发计划.md`、`docs/operations/m0-t0-external-accounts.md` 与 `docs/operations/m3-t7-legal-pages.md`，记录批准提交、PR 评论、main CI、两类 migration、Release Gate、Vercel deployment ID 和逐项 smoke 证据；未修改 Web 实现、API、数据库 schema、Payload、Mobile、`/about`、sitemap 或 production。
- 验证：PR #58 的 CI Gate、lint/typecheck/unit、Build、API e2e、Web Playwright 与 Vercel Preview 全绿；精确 merge commit `09f8325` 的 main CI run `30228435413`、Staging CMS migration、Staging Core migration 与 Staging Release Gate 全部成功。Vercel deployment `dpl_8KnMsdcVHuTd7kUWvq2oG8HcN1Ls` 为 `READY`、Production target，并绑定 `staging.chinasupply.ai`。定向 staging Playwright 4/4 通过；curl 确认 `/privacy`、`/terms`、`/sign-in` 均 HTTP 200 且无法律页认证重定向，两页 title、公司披露、邮箱与互链正确；干净 Chromium 确认 Consent 首次 banner 与重开 settings 均链接 `/privacy`。验收文档变更另以 `pnpm --filter @chinasupply/config test:unit`（36/36）、`pnpm format:check` 与 `git diff --check` 验证通过。
- 遗留/人工项：Companies House 当前 SIC 仍为 `99999 - Dormant Company`，Owner 须在 production 前与会计或法律顾问确认实际运营与申报状态；production 法律页复核与部署仍属 M5-T9，`/about` 和 sitemap 仍属 M5-T7。本任务未访问或修改 production。

## 2026-07-26 ｜ M3-T6（Web Consent staging 验收与收尾）

- 内容：完成 PRD F-11.3、F-1.7 与 N-4 的 canonical staging 真实验收并勾选 M3-T6。基于精确 `main` 提交 `4e15af6`、成功 CI run `30228961672` 与 Vercel deployment `dpl_6cRPprpt6s5Wb5kgXEQxHMZLNvwR`，确认 unknown/denied/revoked 零 PostHog 请求、grant 后才加载、搜索 PII 脱敏、`map_moved` 十秒节流、六类冻结事件计数/字段白名单、同一匿名会话及排除路由零首次加载；无自动事件。
- 主要文件：新增 `docs/operations/m3-t6-analytics-consent-acceptance.md`，更新 `ChinaSupply.AI开发计划.md`、`docs/operations/environments.md` 与 `docs/operations/m0-t0-external-accounts.md` 的完成状态和非敏感证据。未修改 Web 实现、公共 API、schema、类型、analytics 事件接口、Mobile、数据库或 Payload；未记录项目 key、请求体、headers 或匿名标识符。
- 验证：`pnpm lint`、`pnpm typecheck`、`pnpm test:unit`（全仓 475/475）、`pnpm build`、`pnpm test:web:e2e`（10/10）、`pnpm test:web:e2e:staging`（4/4）、`pnpm api:generate:check`、最终 `pnpm format:check` 与 `git diff --check` 全部通过。两次中间 `pnpm format:check` 分别发现新增验收 Markdown 和更新后的账号台账表格未按 Prettier 排版；定向格式化对应文档后完整复跑通过。
- 遗留/人工项：M3-T6 staging 验收无遗留；合成验收事件按计划保留在 staging PostHog 项目中。production PostHog 配置、Consent 与事件复验仍属 M5-T9，本任务未访问或修改 production。

## 2026-07-26 ｜ M3-T8（账户、收藏与 Webhook e2e）

- 内容：完成 PRD G-4/G-5/G-7、A-8/A-10/A-11、F-5.3、F-8.2/F-8.4 与 N-5 的 M3 收口 e2e；保留并复核 Webhook 并发/串行重放、收藏并发重复创建与重复删除、`created_at DESC, id DESC` 同时间戳 cursor 翻页覆盖，新增贯通 `DELETE /me` → Clerk 删除调用 → 签名 `user.deleted` webhook → users 软删/favorites 硬删 → 重放无副作用 → 旧 JWT 返回 401 的单一全流程场景。
- 主要文件：扩展 `apps/api/e2e/webhooks.e2e.ts`，在真实 PostGIS/Redis Testcontainers 和 Nest/Fastify HTTP 入口下串联账户删除生命周期并核对 webhook receipt 唯一性；更新 `apps/api/e2e/account-favorites.e2e.ts` 的任务归属描述，未修改业务实现、数据库 schema/migration、Zod/OpenAPI、Orval 生成产物、Web、Mobile 或 Payload。勾选 `ChinaSupply.AI开发计划.md` 的 M3-T8。
- 验证：定向 `pnpm --dir apps/api exec vitest run e2e/account-favorites.e2e.ts e2e/webhooks.e2e.ts --config vitest.e2e.config.ts` 15/15 通过；`pnpm test:e2e` 9 个文件、57/57 通过；`pnpm lint`、`pnpm typecheck`、`pnpm test:unit` 与 `pnpm build` 全部通过。第一次使用带 `--` 的定向命令实际误触发全量套件，其中既有 `api-health.e2e.ts` 曾因 Testcontainers 等待端口绑定超时失败；正确的定向命令和随后正式全量命令均复跑通过。
- 遗留/人工项：无。测试中的 Clerk 删除调用为受控 mock，Webhook 使用真实签名验证与本地容器数据库；本任务未访问或删除 staging/production 账户，M3-T2 已完成的真实 staging Clerk Webhook 生命周期验收不被重复执行。

## 2026-07-26 ｜ M4-T0（地图体验诊断任务批准与计划入列）

- 内容：经 Owner 明确批准，将 M4-T0 作为 M4-T1 前置门禁写入冻结开发计划，定义 F-1.1~F-1.7、F-11.2 与 N-1/N-6 的三缩放级诊断、稀疏/密集数据对照、至少两套方向稿和 Owner 选择记录验收要求；未扩大 PRD V1 功能范围。
- 主要文件：更新 `ChinaSupply.AI开发计划.md` 至 v1.2，将 Next Action 改为 M4-T0 并增加任务边界、产物与门禁；同步 `AGENTS.md` 的冻结 PRD 版本和当前执行起点。未修改 PRD、技术栈、Web/Mobile/API 代码、共享地图 style、schema、生成产物或任何外部环境。
- 验证：`pnpm lint`、`pnpm typecheck`、`pnpm test:unit`（全仓 475/475）、`pnpm build`、`pnpm format:check`、`git diff --check` 和定向文档一致性检查全部通过。首次定向计数断言同时命中 v1.2 变更摘要与正式任务条目而失败；改为只计数未勾选的 M4-T0 任务行后通过，文档无缺失。
- 遗留/人工项：M4-T0 诊断和方向稿尚未执行，复选框保持未勾选；待产物与 Owner 选择记录同 PR 交付后才可进入 M4-T1。M0-T0 中 Apple Developer、Google Play Console 及商店标识保留仍是未完成的人工前置。

## 2026-07-27 ｜ M2-T7 修复（南通工厂坐标与核验 SOP）

- 内容：按 PRD G-1/G-7、ADM-3/ADM-4/ADM-5 逐条以 Google Earth 高分辨率卫星影像和 WGS-84 地理配准 Sentinel-2 影像比较南通 5 家工厂的当前点与 `gcj02ToWgs84` 参考点。仅 `nantong-jinkanghong-textile` 的修正点压在连续厂房屋面，已通过 staging `/ops` 从 `[121.02599,32.09873]` 改为 `[121.0214538,32.1006772]`；其余 4 条两个候选均落在道路、停车区、农田或水域，坐标保持原值。5 条均通过 `/ops` unpublish 并恢复 `Draft + Unverified`，未执行 ADM-5 或重新发布；MAP 缓存已随状态流转 purge。
- 主要文件：新增 `docs/operations/reviews/staging/2026-07-27-nantong-coordinate-remediation.md` 逐条证据与 Owner attestation 门禁；更新 `data-sources.md` 的旧值→新值、GCJ-02 漏转换原因和影像判读；补强 `docs/operations/data-verification-sop.md` 的偏移坐标源清单、`packages/geo` 转换留痕和“pin 必须压在厂房建筑轮廓”规则。`apps/api/src/admin/admin.service.ts` 在工厂资料变更时清空旧核验审计字段，并保证历史 unverified 记录重新 ADM-5 时生成新的 `verifiedAt`；e2e 覆盖旧时间戳失效、PATCH 清空和重新核验。未修改东莞记录、地图 style、转换函数、production 或开发计划复选框。
- 验证：定向 Admin API PostGIS/Redis e2e 5/5 通过；`pnpm test:e2e` 9 个文件、57/57 通过；`pnpm lint`、`pnpm typecheck`、`pnpm test:unit`（全仓 475/475）、`pnpm build`、`pnpm format:check`、`pnpm api:generate:check` 与 `git diff --check` 全部通过。staging postflight 确认产业带公开工厂列表为 0、5 条公开详情均 404、MAP-3 为 0 features 且首次 MISS/二次 HIT，证明下架与 purge 后缓存已收敛。
- 遗留/人工项：Owner 尚未签署重新核验，5 条继续保持 `Draft + Unverified`。Jinkanghong 待 Owner 接受后才可执行 ADM-5 并重新发布；Luolai、Xinyi、Violet、Nanshing 仍须取得可追溯且压在对应厂房屋面的新坐标证据，不能使用任一已审候选。Owner 完成 attestation 前不得勾选任何任务完成状态；未触碰 production。

## 2026-07-27 ｜ M2-T7 修复（南通坐标签署与 staging 收口）

- 内容：Owner 在 Codex 任务中明确回复“签署通过”，接受 `nantong-jinkanghong-textile` 的屋顶级修正坐标，并否决 Luolai、Xinyi、Violet、Nanshing 的两个已审候选。待 PR #63 合并且审计字段修复部署后，仅通过 staging `/ops` 对 Jinkanghong 执行 ADM-5 与 publish；新的 `verifiedAt`/`lastVerifiedAt` 均为 `2026-07-27T05:13:42.340Z`，`verifiedBy=user_3Gr8DpJw59xwHVz70XQ2VCm6yma`。其余四条未核验、未发布，继续 `Draft + Unverified`；未修改任何坐标、状态或数据于 production。
- 主要文件：更新 `docs/operations/reviews/staging/2026-07-27-nantong-coordinate-remediation.md` 的 Owner attestation、精确部署、ADM-5、发布与 MAP purge postflight；同步 `data-sources.md` 的最终 staging 状态。实现 PR #63 已合并为 `ddc5c3d`，本验收记录不修改业务代码、schema、生成产物、东莞数据、地图 style 或 `packages/geo`。
- 验证：PR #63 精确 attestation 提交 `8b05161` 的 CI Gate 全绿；main CI run `30238836667`、CMS migration、core migration 与 Staging Release Gate 全部通过。Railway API/Worker 对应部署均在精确 merge commit 达到 `SUCCESS`。公开 API 确认 Jinkanghong 详情 HTTP 200、`verified=true`、修正坐标及全新的匹配核验时间，产业带列表只返回该记录；其余四条详情均 HTTP 404。MAP-3 首次 `MISS`、二次 `HIT`，仅返回 Jinkanghong 一个 feature，坐标正确且 `meta.truncated=false`。
- 遗留/人工项：Luolai、Xinyi、Violet、Nanshing 仍须另行取得可追溯且压在对应厂房建筑轮廓上的新坐标证据，在此之前必须保持 `Draft + Unverified`。本修复不是开发计划中的独立编号任务，因此未勾选任何开发计划复选框；production 未触碰。

## 2026-07-27 ｜ M2-T9 修复（官方 Streets v4 快照）

- 内容：纠正“MapTiler 内置 Streets v4 Style JSON 需 Flex 才可读取”的错误判断，通过公开文档化的 `GET /maps/streets-v4/style.json` 与受限 staging Web key 下载官方 159 层样式；key 只在内存中使用并立即替换为 token，未打印或提交。共享底图保留官方道路、交通、POI 与标签层，仅按冻结约束移除唯一 `Building 3D`、将 2D 建筑限制为 zoom ≥15 且淡化、固定零 pitch/bearing，并把 31 个名称表达式统一为英文优先、当地名称 fallback。对应 PRD F-1.2~F-1.5、F-2.1、F-4.1、F-11.2 与 N-1。
- 主要文件：`packages/config/map/chinasupply-light.json` 替换为去 key 的官方快照 2D 派生版，metadata 记录上游端点、获取日期与 SHA-256；`style.js` 将首个 symbol anchor 更新为官方 `Ferry labels`；`map-style.test.mjs` 锁定 158 层、资源 token、Planet v4 source-layer、官方道路/交通/POI、英文 fallback、z15+ 2D 建筑与 MapLibre validator。同步更正冻结技术栈、`AGENTS.md`、MapTiler 账号台账和 M2-T9 运维记录。未修改 API/schema、MAP-* 数据流、Web 交互、Mobile 业务页或线上数据。
- 验证：`pnpm lint`、`pnpm typecheck`、`pnpm test:unit`（全仓 476/476，config 37/37）、`pnpm build`、`pnpm mobile:check`（Expo Doctor 18/18，iOS/Android export 通过）、`pnpm test:web:e2e`（10/10）、`pnpm api:generate:check`、`pnpm format:check` 与 `git diff --check` 全部通过。受限 key 的 style/TileJSON/PBF/glyph/三组 sprite JSON+PNG 全部 HTTP 200；MapLibre 6.0.0 同源实渲染 158 底图层 + 2 测试业务层，22 个 MapTiler response 全 200、零 MapLibre error、零 extrusion，boundary fill 精确位于首个 symbol 之前，截图人工复核通过。
- 遗留/人工项：修复 PR 合并、staging release gate 和 Vercel canonical 部署后，仍须在真实页面复核全国/城市/街道视觉、业务图层层级及美国网络 LCP `<2.5s`；本分支未部署 staging/production，也未触发 Cloudflare purge。M5-T9 的 production Flex、商业授权、账单上限与三平台 production key 门禁不变。

## 2026-07-27 ｜ M2-T9 修复（PR Preview 受限域诊断）

- 内容：在 PR #66 的 Vercel Preview 部署就绪后复核真实页面。Preview canvas、业务 MAP-1 点与 attribution 可见，但底图瓦片为空；定向网络诊断确认原因是受限 Web key 不允许 `*.vercel.app` Origin，Planet v4 TileJSON 返回 HTTP 403。同时 staging API 的 categories CORS 只允许 `https://staging.chinasupply.ai`，因此 Preview 无法代替 canonical staging 真实验收。按最小权限约定未放宽 MapTiler allowlist 或 API CORS。
- 主要文件：本记录仅追加 `开发日志.md`；未修改样式、测试、环境变量、Vercel、MapTiler、Cloudflare、API CORS 或线上数据。
- 验证：PR #66 精确提交 `710ae53` 的 CI Gate、lint/typecheck/unit、Build、API e2e、Mobile PR checks、Web Playwright 与 Vercel 全部通过。`PLAYWRIGHT_STAGING_BASE_URL=<preview> pnpm test:web:e2e:staging` 按设计拒绝非 canonical Origin 并返回配置错误；随后的只读浏览器诊断观察到 MapTiler 请求为 1 个 TileJSON 403、3 组 sprite JSON 200、3 组 sprite PNG 200 与 1 个 logo 200，错误日志中 key 已脱敏。
- 遗留/人工项：待 PR #66 合并且同一提交通过 staging release gate、canonical alias 切换后，在 `https://staging.chinasupply.ai` 复跑专用 staging Playwright 与人工视觉验收。当前同源合成渲染的 22/22 MapTiler 200 证据仍有效，但不冒充 canonical 部署证据。

## 2026-07-27 ｜ M2-T9 修复（官方 Streets v4 视觉一致性）

- 内容：Owner 明确确认修订冻结规则，撤销“统一英文优先”和“建筑仅 zoom ≥15”两项正规化。共享底图恢复官方 Streets v4 的全部标签表达式、默认相机，以及 `Building` 在 zoom 12–15 的原始 fill/opacity/outline，并通过移除该层的 `maxzoom: 15` 在 z15+ 延续同款 2D footprint 代替唯一的 `Building 3D`。除运行时密钥占位符、ChinaSupply 来源元数据和 3D→2D 转换外，底图视觉逐字段保持官方快照原值；对应 PRD F-1.2~F-1.5、F-2.1、F-4.1、F-11.2 与 N-1。
- 主要文件：更新 `packages/config/map/chinasupply-light.json` 与 `packages/config/test/map-style.test.mjs`，新增官方 2D 派生语义摘要护栏；同步修订 `ChinaSupply.AI技术栈-最终冻结版.md`、`ChinaSupply.AI开发计划.md`、`AGENTS.md`、地图 README 与 M2-T9 运维记录。未修改 API/schema、MAP-* 数据流、Web/Mobile 业务交互、密钥或线上环境。
- 验证：官方快照逐字段语义审计通过；`@chinasupply/config` 37/37、全仓 `pnpm test:unit` 476/476 通过；`pnpm lint`、`pnpm typecheck`、`pnpm build`、`pnpm mobile:check`（Expo Doctor 18/18，iOS/Android export 通过）、`pnpm test:web:e2e`（串行复跑 10/10）、`pnpm format:check`、`pnpm api:generate:check` 与 `git diff --check` 全部通过。首次 Web E2E 与 API 生成漂移检查并行执行时，构建短暂读到生成器正在清理的目录而失败；生成检查完成后串行复跑通过，生成产物无漂移。
- 遗留/人工项：本分支未部署 staging/production。PR 合并并完成 canonical staging 发布后，仍须在全国/城市/街道层级人工复核官方标签、z12–15 建筑、z15+ 平面建筑、业务覆盖层层级及美国网络 LCP `<2.5s`；M5-T9 的 production Flex 与三平台 production key 门禁不变。

## 2026-07-27 ｜ M4-T0（地图体验诊断与方向稿）

- 内容：完成 PRD F-1.1~F-1.7、F-11.2 与 N-1/N-6 的 canonical staging 地图体验诊断、全国/城市/街道三缩放层级标注、稀疏真实数据与 30 产业带/200 工厂设计 fixture 密集态对照，以及 Direction A/B 两套方向稿和比较表。Owner `huangsourcing-ux` 在同一 PR #65 明确回复 `Keep baseline`，因此勾选 M4-T0 并解除 M4-T1 前置门禁。
- 主要文件：`docs/design/map-experience/` 新增原始截图、标注图、密集态图、方向稿、固定 fixture、capture manifest、诊断、比较与 Owner 决策记录；更新 `ChinaSupply.AI开发计划.md` 与 `AGENTS.md` 的执行状态。本任务未修改 Web/Mobile 业务代码、共享 style JSON、API/schema、生成产物或线上数据。
- 验证：PR #65 的 lint/typecheck/unit、Web/API build、API e2e、Web Playwright、CI Gate 与 Vercel Preview 全部通过；收口提交再运行 `pnpm format:check`、`git diff --check` 和定向文档一致性检查。
- 遗留/人工项：Owner 同时指出当前手工底图与官方 Streets v4 差异过大，已在独立 M2-T9 修复 PR #66/#67 中完成官方快照修正并合入 `main`；该代码修正不计入 M4-T0 完成范围。M0-T0 的 Apple Developer、Google Play Console 与商店标识保留仍未完成。

## 2026-07-27 ｜ M2-T9 修复（Web glyph 预热与持久缓存）

- 内容：在完全保留官方 Streets v4 字体栈、标签表达式和共享样式语义的前提下，修复 Web glyph 请求约 `2,094ms` 才发起、复访地图资源约 `5,159ms` 才完成且字体未有效持久缓存的问题。新增无 key 的 `chinasupply-glyph://` 内部协议，只接管 MapTiler fonts PBF；按全国 zoom 4、产业带 zoom 10、工厂 zoom 14 动态提取当前样式的 `6/9/8` 个字体栈，并在 MapLibre 动态加载期间预热 `0-255.pbf`。同路径 Promise 合并并发，Cache Storage 使用绑定官方快照 SHA 的版本名、30 天 TTL、96 项 FIFO 上限和旧版本清理；不可用或失败时回退 `force-cache`，预热失败不阻断地图且正式请求可重试。对应 PRD F-1.2~F-1.5、F-2.1、F-4.1、F-11.2 与 N-1。
- 主要文件：新增 `apps/web/src/app/(frontend)/map/map-glyph-cache.ts` 和完整单测；全国、产业带详情、工厂详情三个地图入口接入预热、协议注册与 glyph-only `transformRequest`。扩展固定资源 Playwright fixture、地图核心 E2E 和 canonical staging 性能附件采集；更新 `docs/operations/m2-t9-streets-v4.md`。`packages/config/map/chinasupply-light.json` 未修改，SHA-256 仍为 `dcb579f69e17e1153e2e7e02112dd49815ce6a4d10e9aabf84cb6a2d24f5703f`；未修改 API/schema、环境变量、Mobile 或基础设施。
- 验证：`pnpm lint`、`pnpm typecheck`、`pnpm test:unit`（全 workspace 通过，Web 154/154、glyph cache 10/10、config 37/37）、`pnpm build`、`pnpm mobile:check`（Expo Doctor 18/18，iOS/Android export 通过）、`pnpm test:web:e2e`（最终 11/11）、`pnpm format:check`、`pnpm api:generate:check` 与 `git diff --check` 全部通过；核心 glyph E2E 单 worker 连续重复 3/3 通过。一次中间全量 E2E 在地图 ready 后过早读取首个固定瓦片记录而 10/11，改为显式等待瓦片记录后正式全量复跑通过；一次将同一 Next test-mode 用例三 worker 并行的额外压力运行因测试框架上下文竞争而 0/3，按单 worker重复验证后通过。
- 遗留/人工项：本分支未访问或修改 staging/production。合并并完成 canonical staging 部署后，仍须在相同美国网络条件下复核首次 glyph 发起 `<500ms`、暖访问远端 glyph 请求为 `0`、最后地图资源完成 `≤2.5s`，并核对全国、产业带和工厂三类地图 ready、attribution 与业务图层；不得以本地 fixture 或 Preview 域替代。Mobile 保持原共享样式，原生缓存能力留待 M4-T1 单独验收。

## 2026-07-27 ｜ M4-T1a（App 地图数据源与图层）

- 内容：实现 PRD G-1/G-2/G-4/G-6、MAP-1/MAP-2/MAP-3、F-1.1/F-1.2、F-1.3 数据图层部分、F-1.7 数据刷新部分、F-11.2 与 N-6；App 根路由改为无需登录的公开 MapLibre 地图，使用平台隔离 MapTiler key 注入共享 Streets v4 2D style，初始相机覆盖中国 bbox `[73,18,135,54]`，全量显示按一级类目 `color` 着色的产业带点，zoom ≥8 加载 boundary fill/line，zoom ≥10 加载工厂点。原生视口停止 500ms 后生成 WGS-84 bbox，移动开始即取消 MAP-2/3 查询；断网/底图失败显示 i18n 错误与 Retry，MapTiler + © OpenStreetMap contributors attribution 自首次渲染始终可见。
- 主要文件：新增 `apps/mobile/src/features/map/` 的产品地图屏、图层/空数据配置、视口规范化/防抖及 14 项定向测试，新增 `apps/mobile/src/lib/mobile-map-style.ts` 与 2 项测试锁定 iOS/Android key 选择；更新根路由、i18n、MapLibre Jest mock、Mobile README 与认证兼容入口，并删除 M0-T5b 的离线 spike 页面、fixture 与演示 style。未修改 API、Zod/OpenAPI、Orval 生成产物、共享 Streets style、Web、数据库或线上数据。
- 验证：`pnpm lint`、`pnpm typecheck`、`pnpm test:unit`（全仓 493 项，Mobile 42/42）、`R2_MEDIA_BUCKET=chinasupply-dev-media pnpm build`、`pnpm api:generate:check`、`pnpm mobile:check`（Expo Doctor 18/18、iOS/Android Metro export）、`pnpm format:check` 与 `git diff --check` 全部通过。首次把 `format:check` 与会清理并重建生成目录的 `api:generate:check` 并行执行时，Prettier 因中途文件短暂不存在返回 ENOENT；新增 key 测试后的首次格式检查又准确报告该单文件未格式化。生成稳定并执行 Prettier write 后均已串行复跑通过，工作树无生成漂移。
- 遗留/人工项：M4-T1 父项保持未勾选；M4-T1b 仍须实现工厂聚合/点击放大、产业带与工厂选择、底部卡片补全、truncated 提示、`map_moved` 节流及交互验收。当前未触发 EAS、未访问 staging/production，合并后仍需使用真实受限 iOS/Android key 与 canonical staging API 做双端模拟器/真机地图视觉 smoke；Apple/Google 商店标识和物理真机门禁仍由人工完成。

## 2026-07-27 ｜ M4-T1a 修复（Mobile Jest Linux CI 挂起）

- 内容：定位并修复 PR #69 的 Mobile 单测在 GitHub Ubuntu runner 上 CPU 100% 且不退出的问题。`AppMapScreen` 的 500ms 视口防抖组件测试使用 Jest modern fake timers 时同时伪造了 React Native 调度器所需的 `nextTick`、`queueMicrotask` 与 `setImmediate`，导致 Linux 测试环境清理阶段循环；测试现仅伪造宏定时器并保留三项微任务原语为真实实现，产品代码、500ms 行为和 PRD 范围均未改变。
- 主要文件：仅更新 `apps/mobile/src/features/map/app-map-screen.test.tsx` 的定时器配置并补充 Linux runner 原因注释；未修改 App 运行时代码、API/schema、生成产物、共享地图样式、CI 工作流或线上环境。
- 验证：在全新 Linux + Node 22.23.1 容器中先稳定复现原用例 60 秒超时、Jest CPU 100%，隔离确认其余组件/地图配置测试通过；修复后同一用例约 2.7 秒通过，构建 `@chinasupply/schemas` 后 Linux Mobile 全套 42/42 在 4.7 秒完成。本机 `CI=true pnpm --filter @chinasupply/mobile test:unit`（42/42）、`pnpm lint`、`pnpm typecheck`、`pnpm test:unit`（全仓 493/493）、`R2_MEDIA_BUCKET=chinasupply-dev-media pnpm build`、`pnpm mobile:check`（Expo Doctor 18/18、iOS/Android Metro export）、`pnpm api:generate:check`、`pnpm format:check` 与 `git diff --check` 全部通过。一次加速复现只安装 Mobile 依赖而未先构建 schemas，导致 Linux 全套最后 1 个既有 workspace 测试报模块缺失；补齐与仓库脚本一致的 schemas build 后 42/42 通过。
- 遗留/人工项：待本提交推送后由 GitHub Actions 的 Ubuntu x64 runner 完成 CI Gate 复验；M4-T1b、真实受限 key 与 canonical staging 双端 smoke 等原人工项不变。

## 2026-07-27 ｜ M4-T1b（App 聚合、卡片与交互，待双端 smoke）

- 内容：完成 PRD F-1.3～F-1.5、F-1.7 的 App 工厂聚合、点击聚合放大、MAP-1/MAP-2/MAP-3 点击选择、底部详情补全卡片、MAP-3 截断提示和 `map_moved` 共享 facade 接线。聚合参数固定为 radius 50、max zoom 14；点击优先级为工厂聚合、工厂点、产业带点/边界、空白地图；详情 CTA 在 M4-T2b/T2c 前保持明确禁用。未接入 PostHog RN、未改变 Consent 语义、未新增详情路由，M4-T1b 与父项 M4-T1 因双端 smoke 未完成而保持未勾选。
- 主要文件：新增 `apps/mobile/src/features/map/map-selection.ts`、`map-selection-card.tsx` 及其测试；更新 `app-map-screen.tsx`、`map-config.ts`、MapLibre/Jest mocks、Mobile i18n/README；Mobile 新增 `@chinasupply/analytics` workspace 依赖；同步更新 `ChinaSupply.AI开发计划.md` 与 `AGENTS.md`。未修改 API、Zod/OpenAPI、生成客户端、共享 Streets v4 样式、Web、数据库或线上数据。
- 验证：`pnpm install --frozen-lockfile --offline`、`pnpm lint`、`pnpm typecheck`、`pnpm test:unit`（全仓 505/505，Mobile 54/54）、`R2_MEDIA_BUCKET=chinasupply-dev-media pnpm build`、`pnpm api:generate:check`、`pnpm mobile:check`（Expo Doctor 18/18、iOS/Android Metro export）、`pnpm format:check` 与 `git diff --check` 全部通过；生成产物无漂移。
- 遗留/人工项：本机 Xcode Simulator 列表中没有 booted 设备，Android `adb devices -l` 也没有连接的 emulator/device，无法使用真实受限平台 key 与 canonical staging API 完成 iOS Simulator、Android Emulator smoke。因此本 PR 以 Draft 交付，M4-T1b、父项 M4-T1 及 Next Action 均不推进；待双端验证底图/attribution、聚合放大、产业带/工厂卡片、关闭/重试及无崩溃后方可勾选并把 Next Action 更新为 M4-T2a。未触发 EAS，未部署 staging/production，未修改线上数据。

## 2026-07-27 ｜ M4-T1b 收口（双端 Simulator/Emulator smoke）

- 内容：使用真实平台受限 MapTiler key 与 canonical staging API 完成 PRD F-1.3～F-1.5、F-1.7 的 iOS Simulator 和 Android Emulator 验收：底图与 attribution、产业带/工厂卡片、工厂聚合放大、关闭、A-5 失败轻量态及 Retry 恢复均通过且无原生崩溃。勾选 M4-T1b 与父项 M4-T1，并将 Next Action 推进至 M4-T2a。干净 iOS prebuild 同时发现已锁定的 `@clerk/expo` 未注册 Expo config plugin，补齐后 Clerk iOS/Android 插件均被 Expo Doctor 和原生构建识别。
- 主要文件：更新 `apps/mobile/src/config/expo-config.ts` 及其测试以注册 `@clerk/expo`；在 `apps/mobile/README.md`、`docs/adr/0001-mobile-version-matrix.md` 记录双端版本、构建、交互证据和警告边界；同步更新 `ChinaSupply.AI开发计划.md`、`AGENTS.md`。未修改 API/schema、生成客户端、共享 Streets v4 样式、Web、线上数据或运行时地图相机逻辑；测试期的临时相机与失败注入均已撤销。
- 验证：Xcode 26.6 的 iPhone 17 Pro / iOS 26.5 完成 clean prebuild、CocoaPods（98 dependencies、99 pods）、`ChinaSupplyAIStaging` Simulator build（24.9 秒）和交互 smoke；Android Emulator 36.6.11 / API 36 完成 Gradle staging build（548 tasks）和同路径 smoke。`pnpm lint`、`pnpm typecheck`、`pnpm test:unit`（全仓 505/505，Mobile 54/54）、`R2_MEDIA_BUCKET=chinasupply-dev-media pnpm build`、`pnpm api:generate:check`、`pnpm mobile:check`（Expo Doctor 18/18、iOS/Android Metro export）、`pnpm format:check` 与 `git diff --check` 全部通过。
- 遗留/人工项：M4-T1b 无。物理真机、Apple/Google 商店账号与标识、production Flex 及三平台 production key 仍按既有 M0/M5 发布门禁由人工完成，不阻塞本任务；未触发 EAS，未部署 staging/production，未修改线上数据。

## 2026-07-27 ｜ M4-T2a（App 搜索）

- 内容：完成 PRD F-3.1～F-3.6 的 App 原生搜索，并补齐 F-1.6 一级类目 chips；实现 100 字符上限、trim + 2 字符请求门槛、300ms 搜索防抖、Categories/Industrial clusters/Factories 分组、加载/失败重试/空态前 5 个热门类目、analytics 成功响应去重、500ms 类目 MAP 筛选防抖、旧请求取消与旧类目防闪现、一级/二级精确筛选态，以及产业带 zoom 9、工厂 zoom 13 的 700ms 定位和即时详情卡片。`map_moved` 使用实际已应用 slug；Mobile analytics 继续为 consent-aware 网络 no-op。
- 主要文件：新增 `apps/mobile/src/features/map/map-search-model.ts`、`map-search.tsx`、`map-category-chips.tsx`、`map-search-results.tsx` 及纯函数/组件测试；更新 `app-map-screen.tsx`、Mobile i18n、MapLibre/API/analytics Jest mocks、Mobile README、移动版本矩阵、`ChinaSupply.AI开发计划.md` 与 `AGENTS.md`。未修改 API/schema、Orval 生成产物、Web、共享地图样式、详情路由、依赖或线上数据。
- 验证：`pnpm lint`、`pnpm typecheck`、`pnpm test:unit`（全仓 525/525，Mobile 18 suites、74/74）、`R2_MEDIA_BUCKET=chinasupply-dev-media pnpm build`、`pnpm mobile:check`（Expo Doctor 18/18、iOS/Android export）、`pnpm api:generate:check`、`pnpm format:check` 与 `git diff --check` 全部通过。iPhone 17 Pro Simulator（iOS 26.5）与 `diaoyouji_api_36` Android Emulator（API 36）使用 preview 平台受限 key + canonical staging API 完成 `led`、`socks`、`sofa`、`家具`、无结果、失败/Retry、类目 MAP 筛选、产业带/工厂定位与卡片、attribution 和无崩溃 smoke；A-6 warm 样本均 `<500ms`，101 字符直接请求返回 400。PR #71 的生成漂移、lint/typecheck/unit、Mobile PR checks、Web/API build、API e2e、Web Playwright、CI Gate 与自动 Vercel Preview 全部通过。
- 遗留/人工项：M4-T2a 无。详情 CTA 仍须由 M4-T2b/T2c 原生路由启用；真实 Mobile PostHog adapter + Consent、永久 Maestro 套件、物理真机、Apple/Google 商店账号与标识、production Flex/keys 仍按既有后续任务或人工发布门禁执行。本任务未触发 EAS，未部署 staging/production，未修改线上数据。

## 2026-07-27 ｜ M4-T2b（App 产业带详情，待真实数据 smoke）

- 内容：实现 PRD F-2.1、A-2/A-3、G-1/G-2/G-5、N-4/N-6 的 Expo Router `/clusters/[slug]` 产业带详情；地图产业带卡片 CTA 已启用，工厂 CTA 保持禁用至 M4-T2c。详情使用单个 `FlatList`，包含禁用收藏占位、共享 Streets v4 静态 boundary/centroid 小地图、主产品、实时 factoryCount、可选 stats、安全原生 Markdown、20 条 cursor 工厂分页与 ID 去重；补齐 loading/404/服务错误/空列表/首屏失败/续页失败/地图失败的返回或重试动作。A-2 成功后仅调用一次共享 `cluster_viewed` facade，Mobile 未配置 adapter/Consent 时仍为网络 no-op；F-2.3 真实收藏与登录回跳留给 M4-T3a/T3b。
- 主要文件：新增 `apps/mobile/src/app/clusters/[slug].tsx` 与 `apps/mobile/src/features/clusters/` 的详情屏、小地图、Markdown renderer、模型及测试；地图选择卡接入产业带路由；更新 Mobile i18n、Jest mocks/config、README、移动版本矩阵、`ChinaSupply.AI开发计划.md` 与 `AGENTS.md`；Mobile 直接锁定 `mdast-util-from-markdown@2.0.3`。未修改 API、Zod/OpenAPI、Orval 生成产物、Web、共享地图样式、staging/production 数据或配置。
- 验证：`pnpm install --frozen-lockfile`、`pnpm lint`、`pnpm typecheck`、`pnpm test:unit`（Mobile 21 suites、89/89）、`pnpm build`、`pnpm mobile:check`（Expo Doctor 18/18、iOS/Android Hermes export）与 `git diff --check` 均通过。iPhone 17 Pro Simulator（iOS 26.5）和 `diaoyouji_api_36` Android Emulator（API 36）使用 preview 平台受限 MapTiler key + canonical staging API 完成搜索→地图卡片→详情、boundary/attribution、5 家工厂滚动、禁用 CTA、返回地图、直接深链及无原生崩溃 smoke；Xcode Debug 构建 18.0 秒，Android Gradle 548 tasks / 22 秒。Markdown/raw HTML/安全 URL、bounds、stats、cursor/去重、可选区块及各级错误重试均有定向单测。
- 遗留/人工项：canonical staging 当前仅 2 个 published 产业带，二者 `description/stats` 均为空且最大 `factoryCount=5`，无法以真实数据验收 Markdown 和 A-3 第二页 cursor。按任务门禁，M4-T2b 保持未勾选并以 Draft PR 交付；须经现有 Admin API + `/ops` 审核流程补足可验收 published 数据后再做双端复验。物理真机、Apple/Google 商店账号与标识、production Flex/keys 仍按既有发布门禁由人工完成；未触发 EAS，未部署 staging/production，未修改线上数据。

## 2026-07-28 ｜ M4-T2c（App 工厂详情，待真实数据 smoke）

- 内容：实现 PRD F-4、A-5、G-1/G-2、N-4/N-6 的 Expo Router `/factories/[slug]` 工厂详情；启用地图工厂卡片和产业带工厂列表进入口，支持直接深链、无历史返回地图、loading/404/服务失败 Retry。详情按存在性渲染核验时间、来源、图片分页轮播、主产品、认证/MOQ/成立年份/员工规模，使用共享 Streets v4 的 zoom 14 WGS-84 点位小地图；双语地址与 WeChat 使用 Expo Clipboard，Website/Email/Phone 采用安全外链，relatedFactories 可连续进入同路由。`factory_viewed` 每个身份仅调用一次，四种联系方式接入 `factory_contact_clicked` facade；Mobile 无 adapter/Consent 时仍为网络 no-op。F-6 只显示禁用占位，未接入真实导航；未提前实现收藏。
- 主要文件：新增 `apps/mobile/src/app/factories/[slug].tsx` 及 `apps/mobile/src/features/factories/` 的详情屏、小地图、模型与定向测试；地图选择卡和产业带工厂卡接入工厂路由；新增 SDK 54 兼容的 `expo-clipboard@~8.0.8` 并同步 Jest mock、lockfile、Mobile i18n/README、移动版本矩阵、`ChinaSupply.AI开发计划.md` 与 `AGENTS.md`。未修改 API、schema、OpenAPI/Orval 生成客户端、Web、共享地图样式或线上数据。
- 验证：`pnpm install --frozen-lockfile`、`pnpm lint`、`pnpm typecheck`、`pnpm test:unit`（全仓 558/558，Mobile 23 suites、107/107）、`pnpm build`、`pnpm mobile:check`（Expo Doctor 18/18，iOS/Android Hermes export）、`pnpm api:generate:check`、`pnpm format:check` 与 `git diff --check` 全部通过。CocoaPods 安装 Expo Clipboard 8.0.8 后，Xcode 26.6 的 iPhone 17 Pro / iOS 26.5 Simulator 与 Gradle 安装的 `diaoyouji_api_36` / API 36 Emulator 使用 preview 平台受限 MapTiler key + canonical staging API 完成直接深链、A-5、点位地图/attribution、Website 外跳、related 跳转、返回及无原生崩溃 smoke；Android 另完成英文地址复制反馈和断网服务失败→恢复网络后 Retry，logcat 无 fatal exception。图片零/单/多、Unverified、所有可选信息、Email/Phone/WeChat、安全 URL、复制失败、地图失败/Retry 及埋点去重均由定向单测覆盖。
- 遗留/人工项：canonical staging 当前仅 6 家 published 工厂，全部 verified，均无图片/认证/MOQ/成立年份/员工规模/Email/Phone/WeChat，仅 Website 联系方式，5 家有 relatedFactories；因此上述缺失分支仅有 fixture 证据，不满足已批准的真实数据 smoke 门禁。M4-T2c 保持未勾选并以 Draft PR 交付，须经现有 Admin API + `/ops` 审核流程补足 published 数据后再做双端复验；M4-T2b 既有数据门禁不变。物理真机、Apple/Google 商店账号与标识、production Flex/keys 仍按发布门禁由人工完成；未触发 EAS，未部署 staging/production，未修改线上数据。

## 2026-07-28 ｜ M4-T2c 修复（slug 防御与缓存对齐）

- 内容：根据 PR #73 复审补齐工厂详情的防御纵深；`normalizeFactorySlug` 现与产业带详情使用相同的小写 kebab-case 正则和 160 字符上限，拒绝字面路径穿越、百分号变体、空格/大写与超长参数；非法 slug 只进入未找到态，A-5 React Query 以 `enabled: false` 禁用。工厂详情 `staleTime` 从 5 分钟调整为 900 秒，与 Web 工厂详情和 Mobile 产业带详情统一；冻结需求没有支持原 5 分钟差异的业务依据。
- 主要文件：更新 `apps/mobile/src/features/factories/factory-detail-model.ts` 及其模型/屏幕测试；未修改 API/schema、Orval 模板或生成产物、联系方式、地图、staging 数据与 M4-T2b/T2c 勾选状态。
- 验证：定向 slug/缓存/路由测试 18/18；`pnpm lint`、`pnpm typecheck`、`pnpm test:unit`（全仓 559/559，Mobile 23 suites、108/108）、`pnpm build`、`pnpm mobile:check`（Expo Doctor 18/18，iOS/Android Hermes export）、`pnpm api:generate:check`、`pnpm format:check` 与 `git diff --check` 全部通过。
- 遗留/人工项：本次不修改生成客户端的路径编码策略；`safeHttpUrl` 共享化以及 `tel:` 号码白名单规范化应作为后续独立安全工具任务，后者还需含 Phone 的受审核数据和双端拨号器验证；不阻塞本次复审。既有 staging 真实数据 smoke 缺口不变，M4-T2c 继续保持未勾选。

## 2026-07-28 ｜ M4-T2c 收口（门禁校正与 URL 安全加固）

- 内容：按 Owner 批准的开发计划 v1.3，以 canonical staging 双端主路径 smoke + 固定 fixture/自动化分支覆盖收口 M4-T2b/T2c（PRD F-2/F-4、A-2/A-3/A-5、G-5/G-10），Next Action 推进至 M4-T3a；Orval 统一对所有路径参数执行单次 URI 组件编码，Web 移除 slug 手工编码；Mobile 抽取共享 HTTP(S) 外链工具，并将 Phone 严格规范化为可选首位 `+`、3–15 位 ASCII 数字的 `tel:` URI。非法号码仍展示，但不可点击且不触发联系方式埋点。未修改 OpenAPI、Zod schema、API wire contract、线上数据或 PRD V1 功能范围。
- 主要文件：更新 `packages/api-client/orval.config.ts`、共享 fetcher、生成客户端及 API Client 测试，使相对生成路径在运行时保留可选部署前缀；新增 `apps/mobile/src/lib/external-url.ts` 及测试，产业带 Markdown 与工厂 Website/Source/Contact 共用 HTTP(S) 校验；工厂联系方式组件对非法 Phone 降级为只读文本。同步修订 `ChinaSupply.AI开发计划.md`、`AGENTS.md` 与 Mobile 版本矩阵：真实授权图片留 M5-T1 ADM-6，审核 Phone/Email/WeChat 与双端拨号器留 M5-T2，21+ 同产业带真实 cursor 仅在 M5 数据自然满足时非阻塞回归，Unverified 由 fixture 验收。
- 验证：`pnpm api:generate:check` 通过且生成产物稳定；`pnpm lint`、`pnpm typecheck` 均为 9/9 workspace 通过；`pnpm build` 5/5 tasks 通过；`pnpm mobile:check` 的 Expo Doctor 18/18、配置检查和 iOS/Android Hermes export 通过；API Client 3 files / 8 tests、Mobile 24 suites / 122 tests 通过，API 单 worker 34 files / 122 tests 通过；`pnpm format:check` 与 `git diff --check` 通过。默认并发 `pnpm test:unit` 两次均触发既有 M1-T8 20,000 行负载测试的 15 秒超时（并发耗时 20.7/39.0 秒，隔离同用例 1.01 秒、API 单 worker 2.75 秒）；第二次另有既有地址复制反馈断言仅在并发高负载下未观察到瞬时文案，Mobile 隔离全套随后通过。未修改这些历史测试或放宽超时。
- 遗留/人工项：M5-T1 须用获授权真实图片完成 presign→PUT→HEAD→PATCH→A-5 CDN URL；M5-T2 须经 `/ops` 审核并发布 Phone/Email/WeChat 样本，再由人工在 iOS/Android 验证系统拨号器。若 M5 canonical staging 自然形成同产业带 21+ 家 published 工厂，再补真实第二页 cursor 回归；不得为测试伪造或错配数据。本任务未运行 import、未部署、未修改 staging/production，也未提前新增 Admin Create/Upload 端点。

## 2026-07-28 ｜ M4-T3a（App 认证与账户页，待真实 Google 成功 smoke）

- 内容：实现 PRD F-8.1/F-8.3/F-8.4 与 A-9/A-10；建立只显示 Map/Account 的 Expo Router Tabs 并保持地图匿名可用，使用 Clerk 邮箱验证码完成注册/登录及 email-code MFA/client trust，以 `useSSO` 接入 Google browser OAuth，回调固定为 staging 的 `chinasupply.staging://sso-callback`。Account 展示主邮箱和唯一 English locale，Bearer 调用 A-9，登出/删除/401 共享 session cleanup；A-10 使用内联二次确认，成功后即使 Clerk 本地 sign-out 异常也清理并返回公开 Map。普通用户 API guard 在完整验证 JWT 后接受缺少 `azp` 的原生 session token，浏览器 token 仍须匹配 Web origin；Admin guard 保持严格。未修改 schema、OpenAPI、生成客户端、production 配置、收藏、Apple 登录、密码登录或 Mobile PostHog/Consent。
- 主要文件：新增 `apps/mobile/src/app/(tabs)/`、`apps/mobile/src/features/account/`、`apps/mobile/src/lib/mobile-protected-api.ts` 及定向测试；重写 `apps/mobile/src/features/auth/sign-in-screen.tsx`，更新加密 Clerk token cache、英语 i18n、Jest 环境和 SDK 54 依赖 `expo-auth-session~7.0.11`/`expo-web-browser~15.0.11`。更新 `apps/api/src/auth/admin-auth.guard.ts`、`user-auth.guard.ts` 及测试；同步 Mobile README、环境契约、版本矩阵、开发计划与 `AGENTS.md`。staging Clerk allowlist 仅加入精确 App 回调；staging API deployment `59b50dfa-f6e1-4e9a-a8fb-f8a5799d96e6` 成功，production 未配置或部署。
- 验证：`pnpm lint`、`pnpm typecheck` 均 9/9 workspace 通过；`pnpm test:unit` 全仓 595/595（Mobile 27 suites / 141 tests，API 34 files / 123 tests）；`pnpm build` 5/5 tasks；`pnpm mobile:check` 的 Expo Doctor 18/18、配置校验及 iOS/Android Hermes export；`pnpm api:generate:check`、`pnpm format:check` 与 `git diff --check` 全部通过。Xcode 26.6 的 iPhone 17 Pro / iOS 26.5 Simulator 与 `diaoyouji_api_36` / API 36 Emulator 分别使用独立一次性 Clerk 测试邮箱和官方测试码完成注册→locale 保存→登出→既有用户登录→App DELETE；两端均只读确认 Clerk 用户已不存在、core user 为 tombstone、favorites 为 0 且近期 `user.deleted` webhook 成功。Google 浏览器真实打开/取消在双端无错误，成功/取消/失败由单测覆盖；验收截图、一次性邮箱、token 与临时自动化日志均未保留。
- 遗留/人工项：当前没有可用的非管理员 Google 测试身份，未能完成计划要求的双端真实 Google 成功登录与登出；模拟成功不能替代真实 OAuth，因此 M4-T3a 保持未勾选、PR 保持 Draft、Next Action 仍为 M4-T3a。iOS 物理真机、Android 物理真机、商店账号/标识与 production 凭据仍按既有人工/发布门禁处理。Android 冷启动出现一次可恢复 React Host soft diagnostic，双端仍有既有共享 MapLibre style warning；均未导致认证、账户或地图崩溃。

## 2026-07-28 ｜ M4-T3a 收口（Clerk verifier 与 Admin guard 回归保护）

- 内容：根据 Owner 对 `/ops` 信任边界的复审，补齐 `createClerkTokenVerifier` 和 Admin guard 的安全回归测试；默认及显式 `web-only` 必须调用 Clerk `verifyToken` 并携带 `authorizedParties: [WEB_ORIGIN]`，`web-or-native` 必须不携带该选项，Admin guard 必须只传 token、不传 policy。结合既有 `hasAllowedUserAuthorizedParty` 五组合与 User guard `"web-or-native"` 断言，现已锁住原生用户无 `azp` 可用、Web 用户 origin 精确匹配、Admin 仍严格拒绝无 `azp` 的完整组合。Owner 明确确认当前行为正确、缺口仅为回归保护，并批准补测后勾选 M4-T3a；开发计划 Next Action 推进至 M4-T3b。
- 主要文件：仅修改 `apps/api/test/admin-auth.guard.test.ts` 增加 Clerk SDK mock、两项 verifier policy 测试及 Admin guard 单参数断言；同步更新 `ChinaSupply.AI开发计划.md`、`AGENTS.md`、Mobile README 与移动版本矩阵的收口状态。未修改运行时认证代码、API/schema、OpenAPI、生成客户端、staging/production 配置或数据。
- 验证：Admin/User guard 定向 2 files / 12 tests 通过；`pnpm lint`、`pnpm typecheck` 均 9/9 workspace，`pnpm test:unit` 全仓 597/597（API 34 files / 125 tests，Mobile 27 suites / 141 tests），`pnpm build` 5/5 tasks，`pnpm mobile:check` 的 Expo Doctor 18/18、配置检查和 iOS/Android Hermes export，`pnpm api:generate:check`、`pnpm format:check` 与 `git diff --check` 全部通过。首次 `format:check` 准确报告新增测试文件未格式化，执行仓库 Prettier 后复跑通过。
- 遗留/人工项：真实非管理员 Google 成功 session 因无可用测试身份仍未执行且不记为已通过；Owner 已明确批准该验收边界，不再阻塞 M4-T3a。物理真机、商店账号/标识与 production 凭据仍按既有人工/发布门禁处理。

## 2026-07-28 ｜ M4-T3b（App 收藏）

- 内容：实现 PRD F-5.1～F-5.3、F-2.3 并复用 A-8 生成客户端；Tabs 调整为 Map → Saved → Account，新增带安全 `returnTo` 白名单的独立登录页、按 Clerk 用户隔离的 cursor 收藏缓存、产业带/工厂详情收藏按钮，以及默认 Factories、可切换 Industrial clusters 的 Saved 列表。POST 按幂等结果 upsert，DELETE 乐观移除并支持失败回滚，所有请求注入 Bearer，401 复用统一 session cleanup；Saved 聚焦时重验以同步跨设备变更。未修改 API、schema、OpenAPI、生成客户端、Web、canonical 内容或 production。
- 主要文件：新增 `apps/mobile/src/features/favorites/`、`apps/mobile/src/features/auth/sign-in-route.tsx`、`apps/mobile/src/app/sign-in.tsx` 与 `apps/mobile/src/app/(tabs)/saved.tsx`；更新 Tabs、产业带/工厂详情、`SignInScreen`、英语 i18n、Mobile README 和移动版本矩阵。Saved 覆盖图片/占位、地区、产品、工厂认证、`target=null`、分页继续入口、首屏/续页错误、Retry、取消成功/失败状态。
- 验证：`pnpm test:unit` 全仓 614/614（Mobile 31 suites / 158 tests）通过，覆盖未登录引导、安全回跳、两类列表、不可公开目标、cursor 去重/边界、幂等创建、乐观删除/回滚、用户缓存隔离、Bearer 和 401；`pnpm lint`、`pnpm typecheck`、`pnpm build`、`pnpm mobile:check`、`pnpm api:generate:check`、`pnpm format:check` 与 `git diff --check` 均通过。iPhone 17 Pro / iOS 26.5 Simulator 与 `diaoyouji_api_36` / API 36 Emulator 使用真实受限 MapTiler key + canonical staging API 完成匿名 Saved→邮箱登录自动回跳、两类详情收藏、同一临时账户双端同步、列表详情、两类取消、失败→Retry、重新聚焦跨端同步为空及 401 cleanup；Android Debug 原生构建 549 tasks 成功，无 fatal exception。
- 遗留/人工项：临时收藏和 Clerk 测试账户均已清理；M4-T3b 无未满足验收项。物理真机、Apple/Google 商店账号与标识、production Flex/keys 及 M4-T6 Maestro 仍按既有阶段门禁由人工或后续任务完成；未触发 EAS 或部署。

## 2026-07-29 ｜ M4-T3b 修复（Mobile 测试退出与 CI 超时）

- 内容：修复 M4-T3b favorites 渲染测试断言完成后仍等待 5 分钟的问题。根因是两处测试 QueryClient 只将 query GC 设为 `Infinity`，触发收藏/取消 mutation 后仍保留 React Query 默认 5 分钟回收计时器；现将 mutation GC 同步设为 `Infinity`，使 Jest 无活动句柄退出。把 React Native 调度所需的 `nextTick`、`queueMicrotask`、`setImmediate` 全局加入 Jest `fakeTimers.doNotFake`，并移除 Map 测试中 10 处局部重复配置。为 CI 的可执行 jobs 增加 5～10 分钟上限，quality/mobile 按历史约 2～3 分钟基线设为 6 分钟；可复用 migration job 在被调用工作流内设为 15 分钟。Next Action 保持 M4-T4。
- 主要文件：更新 `apps/mobile/jest.config.js`、favorites/Map 测试、`.github/workflows/ci.yml` 与 `release-migrations.yml`；配置测试锁定全局 fake timer 例外、各 CI job 上限和 migration 上限。未修改 Mobile 运行时代码、API/schema、生成客户端、产品功能或环境数据。
- 验证：修复前 `saved-screen.test.tsx --detectOpenHandles` 的 6/6 断言约 1 秒完成但进程在 49.71 秒人工终止时仍未退出；修复后 favorites 两组 10/10 在 5.71 秒完整退出，全 Mobile `--detectOpenHandles` 31 suites / 158 tests 在 5.24 秒完整退出，CI 同款 `pnpm --filter @chinasupply/mobile test:unit` 在 7.64 秒退出。`pnpm test:unit` 全仓 615/615 在 17.30 秒退出；`pnpm lint`、`pnpm typecheck`、`pnpm build`（5/5）、`pnpm mobile:check`（Expo Doctor 18/18、配置检查、iOS/Android Hermes export）、`pnpm api:generate:check`、`pnpm format:check` 与 `git diff --check` 全部通过。
- 遗留/人工项：无功能或数据验收项；本修复不改变 App 运行时，无需重复原生 smoke。未部署 staging/production；GitHub-hosted runner 的实际 job 时长由本 PR checks 复核。

## 2026-07-29 ｜ M4-T4（App Explore tab）

- 内容：实现 PRD F-10.1 并复用 A-7、A-1 与现有产业带详情；Tabs 固定为 Map → Explore → Saved → Account，新增 `/explore` 一级类目双列网格和保留 tab bar 的 `/explore/[slug]` cursor 产业带列表。类目页按 API 顺序使用 color 与九种 Font Awesome 6 icon 映射，null/未知 icon 回落 `shapes`；列表仅接受一级类目 exact slug，以 20 条页大小原样传递不透明 cursor、跨页按 ID 去重、滚动续页并覆盖首屏/续页 Retry。产业带卡支持封面/颜色占位、名称、地区、简介、主产品、factory count 和既有详情往返。未新增登录、搜索、地图筛选、导航、Analytics，也未修改 API/schema/OpenAPI、生成客户端、Web 或线上数据。
- 主要文件：新增 `apps/mobile/src/app/(tabs)/explore/` 与 `apps/mobile/src/features/explore/` 的路由、屏幕、分页模型、icon 映射及 4 组定向测试；更新 Tabs、英语 i18n、Jest vector-icon mock，并把 `@expo/vector-icons@15.1.1` 与其 SDK peer `expo-font~14.0.12` 声明为 Mobile 直接依赖。同步更新 Mobile README、移动版本矩阵、`ChinaSupply.AI开发计划.md` 与 `AGENTS.md`，将 M4-T4 勾选并把 Next Action 推进至 M4-T5。
- 验证：`pnpm install --frozen-lockfile`、`pnpm api:generate:check`、`pnpm lint`、`pnpm typecheck`、`pnpm test:unit`（8/8 tasks、全仓 639/639；Mobile 35 suites / 182 tests）、`R2_MEDIA_BUCKET=chinasupply-dev-media pnpm build`（5/5 tasks）与 `pnpm mobile:check`（Expo Doctor 18/18、配置检查、iOS/Android Hermes export）通过；最终 `pnpm format:check` 与 `git diff --check` 通过。Xcode 26.6 在 iPhone 17 Pro / iOS 26.5 Simulator 以 21.2 秒构建启动，Android Gradle 在 `diaoyouji_api_36` / API 36 Emulator 完成 549 tasks、15 秒安装；双端使用真实受限平台 key + canonical staging API 通过四 tabs、九类 color/icon、Electronics/Home Textiles 非空列表、Dongguan 详情往返、Lighting 真实空态且无 App 崩溃。Android 另以真实飞行模式完成 uncached Furniture 失败→恢复网络→Retry，包级 logcat 无 App fatal exception。
- 遗留/人工项：canonical staging 当前没有任一类目超过 20 个 published 产业带，因此第二页 cursor、跨页重复与续页失败/Retry 仅由固定 fixture/自动化覆盖，未为验收修改 canonical 数据；iOS Simulator 缺少受支持的独立断网控制，iOS 错误/Retry 使用固定自动化覆盖，真实断网恢复已在 Android Emulator 完成，不阻塞 F-10.1。物理真机、M4-T5 导航、M4-T6 Maestro、Mobile PostHog/Consent、EAS/production 与商店人工门禁仍按后续任务执行。

## 2026-07-29 ｜ M4-T5（导航 Deep Link，待 Release 真机门禁）

- 内容：完成 PRD F-6.3、G-1/G-2、N-5 的 App 工厂导航代码阶段；iOS 按 Google → Apple → 高德 → 百度、Android 按 Google → 高德 → 百度展示可访问链接，直接把 A-5 WGS-84 `[lng, lat]` 与工厂名称交给 M0-T9 冻结的 `@chinasupply/geo/navigation`。Apple/Google 仅作一次 HTTPS 系统交接，高德/百度 App URI 被拒后立即打开 Web URL，双重失败显示本地化可恢复错误；每次点击仅调用一次只含 `factoryId`、`slug`、`provider`、`platform` 的 `navigation_clicked` facade，未接入 Mobile PostHog adapter/Consent。F-6.4 未完成，M4-T5 与 Next Action 均保持未推进。
- 主要文件：新增 `apps/mobile/src/features/factories/factory-navigation.ts` 及测试，启用 `factory-detail-screen.tsx` 导航按钮并更新 i18n/Jest analytics mock；新增 `docs/operations/m4-t5-navigation-release-validation.md` 的 13 项真机矩阵与本地 smoke 边界，同步 Mobile README、移动版本矩阵、开发计划和 `AGENTS.md`。未修改 API/schema、OpenAPI/Orval 生成物、数据库、Web 导航、原生 scheme/package queries、canonical 数据或 production 配置。
- 验证：导航 helper/工厂详情定向 Mobile 2 suites / 24 tests、Geo 29/29、Analytics 14/14 通过；`pnpm lint`、`pnpm typecheck`、`pnpm test:unit`（全仓 648/648，Mobile 191）、`R2_MEDIA_BUCKET=chinasupply-dev-media pnpm build`（5/5）、`pnpm api:generate:check`、`pnpm format:check` 与 `git diff --check` 通过。`pnpm mobile:check` 首次仅因 Expo Doctor 的 React Native Directory 外部服务返回异常停在 17/18，立即复跑后 18/18、配置检查及 iOS/Android Hermes export 全部通过；补写本地 smoke 表后首次 `format:check` 准确报告新表格未格式化，执行 Prettier 后复跑通过。Preview 环境下，iPhone 17 Pro / iOS 26.5 的本地 Release Simulator build 显示 4 个按钮，Apple Maps 系统外跳及未安装高德 → Safari `amap.com` OPPO 终点回落通过；`diaoyouji_api_36` / API 36 的本地 Release APK 显示 Google/高德/百度且无 Apple，Google Maps 收到 `22.7707860,113.7771620`，未安装高德时 ActivityManager 记录 App URI 拒绝后 HTTPS 交给 Chrome，双端返回 App 无崩溃。canonical A-5 复核坐标仍为 `[113.7771621452, 22.7707857278]`。
- 遗留/人工项：F-6.4 的 13 项 Release 物理真机矩阵全部 Pending：iOS 安装态 4 项、Android 安装态 3 项及双端 Google/高德/百度未安装 Web 回落 6 项，均须记录构建、设备/系统、地图 App 版本、审核人/date，并确认路线规划终点误差 `<50m`。当前 iPhone Offline 且无 Android 真机；Android fresh Emulator 的 Chrome 停在首次条款页，本次未代替用户接受条款，因此只认定 OS 级 Web handoff，不认定 Android Web 页面渲染。未触发 EAS/Production Build/Submit，未实现 M4-T6、Mobile Consent/PostHog 或 M4-T7。

## 2026-07-29 ｜ M4-T5 收口（Owner 真机验收确认）

- 内容：Owner 明确确认已完成 PRD F-6.4 的 13 项 Release 物理真机矩阵，iOS 的 Apple/Google/高德/百度安装态、Android 的 Google/高德/百度安装态，以及双端 Google/高德/百度未安装 Web 回落均进入已填充终点的路线规划或批准的等效页面，所有终点误差 `<50m`；Owner 同时明确批准勾选 M4-T5。开发计划已勾选本项并把 Next Action 推进到 M4-T6。
- 主要文件：更新 `docs/operations/m4-t5-navigation-release-validation.md`、Mobile README、移动版本矩阵、`ChinaSupply.AI开发计划.md` 与 `AGENTS.md`，把此前代码阶段的 Pending 门禁更新为 Owner 验收通过。此次仅记录验收结论，未修改 Mobile 运行时代码、API/schema、Orval 生成物、数据库、Web、原生标识、canonical 数据或 production 配置。
- 验证：本次为文档收口，无运行时代码变化；运行 `pnpm format:check` 与 `git diff --check` 验证文档格式和补丁完整性。F-6.3 的全量代码/自动化与本地 smoke 结果已记录在上一条 M4-T5 日志及实现 PR #79。
- 遗留/人工项：Owner 未随本次确认提供设备型号/系统、Release 构建产物标识、地图 App 版本、逐项实测距离或外部证据链接，仓库仅如实记录 Owner 于 2026-07-29 的验收结论并将这些字段标为“已由 Owner 验证、未转录”，未补造数据。按 Owner 最新验收决定，F-6.4 与 M4-T5 不再阻塞；未触发 EAS/Production Build/Submit，M4-T6 Maestro 为下一任务。

## 2026-07-29 ｜ M4-T6（Maestro 核心路径自动化）

- 内容：为 PRD F-1、F-2、F-3、F-4、F-5、F-6.3、F-8.1/F-8.3/F-8.4 与 A-2/A-5/A-8/A-10 建立 canonical staging 双端 Maestro 门禁；主流程覆盖清空状态后地图真实 ready、匿名 Saved、Dongguan 搜索→产业带→OPPO 工厂、动态 Clerk 测试用户注册及登录回跳、两类收藏/取消、iOS/Android 导航按钮集合、登出、同账户重登和二次确认删除。失败后的 `onFlowComplete` 会重新进入账户并删除已创建用户；精确 canonical OPPO 名称、WGS-84 坐标和双平台完整导航 URL 由既有 Mobile/Geo 单测断言。未修改 API/schema、OpenAPI/生成客户端、canonical 内容、EAS、CI 或 production。
- 主要文件：新增 `apps/mobile/.maestro/` 主流程和认证、搜索、删除/清理子流程，新增根命令 `pnpm test:mobile:e2e -- --udid=<device-id>` 及外部 CLI 启动脚本；Map 只在底图与当前 MAP 请求均完成时暴露 `app-map-ready`，Saved 未登录动作暴露 `saved-signed-out-action`。同步 Mobile README、环境运维说明、开发计划和 `AGENTS.md`，勾选 M4-T6 并把 Next Action 推进到 M4-T7；`.maestro-artifacts/` 已忽略且本地测试产物已删除。
- 验证：6 个 Maestro YAML 全部通过 `maestro check-syntax`；同一最终工作树的本地 staging Release App 在 iPhone 17 Pro / iOS 26.5 Simulator 与 `diaoyouji_api_36` / Android API 36 Emulator 分别完整跑绿，覆盖启动、地图 ready、搜索/详情、匿名/登录 Saved、两类收藏/取消、平台按钮集合、登出/重登和删除账户。Railway + Clerk 只读聚合核验确认 7 个 M4-T6 测试用户全部从 Clerk 删除、7 个 core tombstone、0 个 active core user、0 个 favorites，成功删除 webhook receipt 数覆盖全部测试用户；未保留邮箱、token、截图或原始日志。`pnpm lint`、`pnpm typecheck`、`pnpm test:unit`（全仓 649/649，Mobile 36 suites / 192 tests）、`pnpm build`（5/5）及 `pnpm mobile:check`（Expo Doctor 18/18、配置检查、iOS/Android Hermes export）均通过。
- 遗留/人工项：M4-T6 验收项无遗留；M4-T5 已完成的 F-6.4 物理真机 `<50m` 落点验收未重复执行。未触发 EAS Build/Submit、部署或 production 操作；M4-T7 商店合规为下一任务。

## 2026-07-29 ｜ M4-T7（商店合规与法律入口）

- 内容：按 Owner 批准的离线验收口径完成 PRD F-8.1/F-11.1/F-11.4；iOS 接入 Clerk 原生 Sign in with Apple，并通过 `EXPO_PUBLIC_APPLE_SIGN_IN_ENABLED` 与 Google 同显同隐，Production 未启用时配置校验失败，Android Google 不受影响。登录/注册与登录后 Account 均提供环境感知的 Privacy Policy/Terms of Use 内置浏览器入口和失败 Retry；Expo 配置固化 Apple entitlement、无追踪 App Privacy manifest、required-reason API 原因、Android 禁用备份与存储/悬浮窗/震动权限移除规则。完成 Apple App Privacy、Google Data Safety 和账户删除路径的离线声明包，并按批准定义勾选 M4-T7、把 Next Action 推进至 M4-T8；M0-T0、M4-T8 和 M4 出口保持未完成。
- 主要文件：新增 `apps/mobile/src/features/legal/legal-links.tsx`、`apps/mobile/src/config/legal-urls.ts` 及测试，更新 `sign-in-screen.tsx`、`account-screen.tsx`、Mobile i18n、环境契约与 Expo 原生配置；加入 Expo SDK 54 兼容的 `expo-apple-authentication~8.0.8`。新增 `docs/operations/m4-t7-store-compliance.md` 离线商店声明包，并同步修订 PRD F-8.1/F-11.4、开发计划 v1.4、`AGENTS.md`、Mobile README 与 M0-T0 外部账号说明。未修改业务 API、数据库、共享 schema、生成客户端、Mobile PostHog/Consent、广告、ATT、定位或 production。
- 验证：`pnpm install --frozen-lockfile`、`pnpm lint`、`pnpm typecheck`、`pnpm test:unit`（8/8 tasks、全仓 663/663；Mobile 38 suites / 205 tests）、`pnpm build`（5/5）、`pnpm mobile:check`（Expo Doctor 18/18、配置检查、iOS/Android Hermes export）与 `pnpm api:generate:check` 全部通过；最终 `pnpm format:check` 与 `git diff --check` 通过。Expo introspection、iOS/Android 非 clean prebuild、CocoaPods 和最终原生 manifest 检查确认 Apple entitlement、八类 collected data、四类 required-reason API、Android `allowBackup=false` 及五项权限移除规则；iPhone 17 Pro / iOS 26.5 Simulator Release 与 `diaoyouji_api_36` / API 36 Emulator release APK 使用 canonical staging 页面完成匿名和登录后 Privacy/Terms 打开、无额外权限提示及流程末删除临时账户，Android 同时确认只显示 Google、不显示 Apple。
- 遗留/人工项：未使用 Apple Developer/Google Play Console，未保留正式 Bundle ID/package，未配置 Clerk Native Application/Apple connection 或 Apple capability 控制台，未录入 App Store Connect/Play Console 声明，未用真实 Apple 身份完成成功登录，也未发布 TestFlight/Play 内测包；以上均为 M4-T8 硬门禁，因此 M4 出口仍阻塞。M5-T10 还须以 Production 最终二进制复核原生清单、权限和商店答案一致。临时 Preview 环境、自动化邮箱、截图与 Maestro 原始日志已删除；未触发 EAS、部署或 production 操作。

## 2026-07-29 ｜ M4-T8（商店门禁迁移与正式标识切换）

- 内容：按 Owner 明确批准将 Apple Developer/Google Play Console、正式标识符保留、Apple capability 与 Clerk Production/Native Application 前置迁到 M5-T9，将商店隐私/Data Safety/账户删除表单、真实 Apple 成功登录、TestFlight/Play 内测和 Production Submit 迁到 M5-T10；PRD 与开发计划升级为 v1.5，M4-T8 仅作为门禁迁移任务收口，M4 出口只依据已完成的 App P0 功能、Maestro、双端核心路径与导航真机验收，Next Action 推进到 M5-T1。对应 F-11.4；未新增 V1 功能或修改 API 契约。
- 主要文件：将 Mobile Production iOS Bundle ID/Android package 候选从已废弃的 `ai.chinasupply.app` 改为尚未保留的 `ai.chinasupply.mobile`，URL scheme 保留 `chinasupply`；EAS production profile 固定 `EXPO_PUBLIC_APP_ENV=production`，缺少正式 API、Clerk、Apple、MapTiler 或 Sentry 变量时继续由现有 Zod 门禁拒绝而不回退 Local。同步更新 `ChinaSupply.AI产品PRD.md`、`ChinaSupply.AI开发计划.md`、`AGENTS.md`、Mobile README/ADR、M0-T0 外部账号说明和 M4-T7 离线合规包。未修改业务 API、数据库、schema、Orval 生成客户端、路由或用户功能。
- 验证：Mobile 标识、MapTiler 身份、Expo/EAS 配置与环境门禁定向 4 suites / 19 tests 通过；两份 EAS workflow 均通过 `eas workflow:validate`。`pnpm lint`、`pnpm typecheck` 均 9/9 workspace，`pnpm test:unit` 8/8 tasks、全仓 665/665（Mobile 38 suites / 207 tests），`pnpm build` 5/5，`pnpm mobile:check` 的 Expo Doctor 18/18、配置检查及 iOS/Android Hermes export，`pnpm api:generate:check`、`pnpm format:check` 与 `git diff --check` 全部通过。
- 遗留/人工项：未访问 Apple Developer、App Store Connect 或 Google Play Console，未接受协议、付款或 2FA，`ai.chinasupply.mobile` 尚未在任一平台验证/保留；未配置 Apple/Clerk Production、未录入商店表单、未完成真实 Apple 登录、未创建 tag、未触发 EAS Build/Submit、未发布 TestFlight/Play 内测包。以上均按 v1.5 保持为 M5-T9/M5-T10 的未完成硬门禁；若新标识任一平台不可用，必须停止并先修订批准文档，不得自行改名。

## 2026-07-29 ｜ M5-T1（Admin Create 与 R2 上传链，实现阶段）

- 内容：实现 PRD ADM-1/ADM-3 的产业带与工厂 Create、ADM-6 五分钟 R2 预签名 PUT，以及 G-7/G-10/G-11、N-3/N-5 的媒体引用与 Admin 写接口加固。Create 在事务中校验 region/category/cluster 引用、生成 21 位 ID、共享搜索列与关联表，产业带固定为 draft、工厂固定为 draft + unverified，且必须先无图创建；PATCH 仅接受当前媒体 bucket、当前环境 prefix、当前实体路径下，经 R2 HEAD 权威确认存在、MIME 为 JPEG/PNG/WebP 且实际大小为 1 byte–10 MB 的对象。Create、PATCH、verify、publish/unpublish 与 presign 共用按 `adminUserId` 的 Redis 写限流，Admin GET 不受写限流；未新增硬删除或修改冻结 wire shape。
- 主要文件：更新 `apps/api/src/admin/` 的 controller/service/module，新增 `apps/api/src/media/media-object-storage.service.ts`、`apps/api/src/rate-limit/admin-throttler.guard.ts` 与公开媒体环境切片校验；直接锁定 `@aws-sdk/s3-request-presigner@3.1094.0`。扩展 `apps/api/e2e/admin.e2e.ts`，以固定 MinIO 镜像覆盖 Create → presign → PUT → PATCH/HEAD → verify/publish → A-5 CDN URL、严格对象隔离与限流；新增 `docs/operations/m5-t1-admin-uploads.md` 作为 canonical staging 真实图片验收模板。
- 验证：`pnpm lint` 与 `pnpm typecheck` 均 9/9 workspace 通过；`pnpm test:unit` 8/8 tasks、全仓 670/670（API 36 files / 129 tests，Mobile 38 suites / 207 tests）；`pnpm test:e2e` 9 files / 63 tests，PostGIS、Redis 与 `minio/minio:RELEASE.2025-04-22T22-12-26Z` 全部通过；`pnpm build` 5/5 tasks、`pnpm api:generate:check` 生成产物稳定、`pnpm format:check` 与 `git diff --check` 通过。
- 遗留/人工项：实现 PR 合并并由更新后的 main 通过 CI、CMS/Core migration、Staging Release Gate 与 Railway 部署后，仍须使用 Owner 提供的获授权真实图片和指定 staging 工厂，完成 Admin presign → PUT → PATCH、`/ops` 重新 verify、A-5 与 CDN HEAD 验收，并在独立 `codex/m5-t1-staging-acceptance` PR 记录授权来源、SHA-256、字节数和非敏感响应证据。当前未修改 staging/production、未记录任何凭据或完整 presigned URL，M5-T1 保持未勾选，Next Action 仍为 M5-T1。
