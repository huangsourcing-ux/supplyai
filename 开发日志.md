# ChinaSupply.AI 开发日志

> 记录规则见 `AGENTS.md` 第 10 节：每个任务包完成时在本文件末尾追加一条，与代码同一提交交付；只追加，不修改历史条目。

<!-- 在此行之下追加日志条目，格式：

## YYYY-MM-DD ｜ M?-T?（任务名）
- 内容：
- 主要文件：
- 验证：
- 遗留/人工项：

-->

## 2026-07-22 ｜ M0-T1（Monorepo）

- 内容：使用 create-turbo 基线建立 pnpm + Turborepo 单仓，落实 apps 与共享 packages 目录，提供统一质量命令和 `packages/config` 的 ESLint、TypeScript、Tailwind 基础预设；为 PRD G-2、G-6 的跨端共享结构建立工程基础，未实现业务功能。
- 主要文件：新增 `package.json`、`pnpm-workspace.yaml`、`turbo.json`、`pnpm-lock.yaml`、`apps/*` 占位目录、`packages/*` 工作区和 `packages/config` 配置、构建与结构测试；初始化本地 `main` Git 仓库。
- 验证：`pnpm lint`、`pnpm typecheck`、`pnpm test:unit`（4/4 通过）、`pnpm build`、`pnpm format:check` 均通过；`pnpm list --recursive --depth -1` 正确识别 6 个共享包。
- 遗留/人工项：M0-T0 仍未完成；当前 Git 仓库没有 GitHub remote，外部账号、标识符、域名及法律文案等人工前置仍需按计划推进。Web/API/Mobile 应用脚手架分别留待 M0-T3/M0-T4/M0-T5。

## 2026-07-22 ｜ M0-T2（环境与配置）

- 内容：落实 Local/Staging/Production 三环境契约，新增 API/Web/Mobile Zod 环境变量校验和完整 `.env.example`，建立 PostGIS 17 + Redis 7.4 本地 Compose，以及 Drizzle 核心表与 Payload 内容表相互独立的 migration release command 和 reusable CI workflow；对应 PRD G-9 与环境、迁移隔离约定。
- 主要文件：新增 `compose.yaml`、`packages/config/env/*`、三个应用的环境示例、`scripts/*` 基础设施/迁移命令、`.github/workflows/release-migrations.yml` 和环境/迁移运维文档；公开行为新增 `infra:*`、`release:migrate:core`、`release:migrate:cms` 命令。
- 验证：`docker compose config --quiet` 通过；`docker compose up -d --wait` 后 PostgreSQL/Redis 均 healthy，`pnpm infra:check` 查询到 PostGIS 3.5 且 Redis 返回 PONG；`docker compose down` 后两个数据 volume 均保留；`pnpm lint`、`pnpm typecheck`、`pnpm test:unit`（14/14 通过）、`pnpm build`、`pnpm format:check` 全部通过；未发现真实密钥。
- 遗留/人工项：M0-T0 外部账号和真实凭据仍未完成；应用启动入口需在 M0-T3/M0-T4/M0-T5 接入对应解析器；真实 Drizzle/Payload migration 分别待应用骨架和 M1-T1 实现。官方 `postgis/postgis:17-3.5` 仅发布 amd64，Apple Silicon 本地开发使用 Docker Desktop 内置模拟，已实测通过。

## 2026-07-22 ｜ M0-T3（Web 骨架）

- 内容：将最小 Next.js 16、React 19、Payload、Tailwind、next-intl 与 Clerk Web 基座落入 `apps/web`，删除模板业务内容；Payload 仅拥有 `cms_users` 及内部表，`/admin` 与 Clerk `/ops/**` 身份边界独立，严格以 `metadata.role = "admin"` 放行运营入口，对应 PRD G-2、G-7、G-9，并为 F-7.2 预留鉴权基础。
- 主要文件：新增 `apps/web` 配置、路由、i18n 文案、Payload collection、初始 CMS migration、鉴权工具与单元测试；更新根级 CMS release command、reusable migration workflow、README 和环境/迁移运维文档。外部行为新增 Railway staging PostGIS、Vercel `chinasupply-web-staging` 项目、Clerk Development 实例、R2 staging bucket 及 `https://staging.chinasupply.ai` HTTPS 部署。
- 验证：`pnpm lint`、`pnpm typecheck`、`pnpm test:unit`（47 项通过）、`pnpm build`、`pnpm format:check` 均通过；Payload types/import map 重生成无漂移；Railway CMS migration 首次成功且再次执行无待迁移，仅生成 `cms_users`/Payload 内部表；Vercel 部署 READY、运行时 error 日志为 0；正式域名 `/`、`/admin`、`/ops/sign-in`、`/ops/forbidden` 返回 200，匿名 `/ops` 307 到登录，非管理员 307 到 forbidden，管理员 Google/Clerk 登录后进入 `/ops`；Let's Encrypt 证书 SAN 为 `staging.chinasupply.ai`。
- 遗留/人工项：M0-T3 验收项无遗留；GitHub 仓库已连接 Vercel，但自动 Git 部署按计划留到 M0-T6，当前不会因 GitHub push 自动更新。普通用户登录、登录回跳和账户功能仍按计划留到 M3；M0-T0 其他外部账号、商店账号及法律文案人工项仍未完成。

## 2026-07-22 ｜ M0-T3（Vercel Preview 环境修复）

- 内容：修复 PR #1 的 Vercel Preview 构建环境，使 PR 验证与受控 staging 部署使用同一套真实 staging 配置；不改变生产环境、不放宽启动时 Zod 校验，对应 M0-T3 环境与部署验收。
- 主要文件：更新 `docs/operations/environments.md`，明确 Vercel `Production`/`Preview` 是同一 staging 项目的部署目标；外部配置将 19 个 staging 变量从 Vercel Production 安全同步到 Preview，并用真实 Clerk Development、Railway PostGIS 与新生成的 Payload secret 替换三项无效值。
- 验证：失败日志最初报告 19 项环境变量缺失；补齐 Preview 范围后收敛到 `CLERK_SECRET_KEY`、`DATABASE_URL`、`PAYLOAD_SECRET` 三项值无效；修正后受控 Vercel Preview redeploy 在 2 分钟内达到 `READY`。变量值未输出、未写入仓库。
- 遗留/人工项：GitHub PR 状态需由本次提交触发的新 Preview deployment 最终复核；M0-T6 仍负责正式 CI/CD 触发策略。

## 2026-07-22 ｜ M0-T4（API + Worker 骨架）

- 内容：建立 NestJS 11 + Fastify HTTP 与 BullMQ Worker 双入口，接入 M0 runtime Zod 环境校验，实现 G-4 成功/失败 envelope、冻结错误码、全局 Zod pipe、无依赖 liveness 与 PG/Redis readiness；新增并验证 `system:ping` 队列作业，对应 PRD G-4、G-6。
- 主要文件：新增 `apps/api` 工程、HTTP 公共层、健康检查、PG/Redis 连接、BullMQ processor/producer、18 项单测与 `railway.json`；更新 API 环境解析、README 和 staging 运维文档。外部行为新增 Railway staging `api`、`worker`、Redis SFO 服务及临时 API 域名；Worker 无公网域名。
- 验证：`pnpm lint`、`pnpm typecheck`、`pnpm test:unit`（66/66 通过）、`pnpm build`、`pnpm format:check` 全部通过；本地 Compose PostGIS/Redis healthy，live/ready 为 200，404 返回 `NOT_FOUND` envelope，独立 producer 完成 `system:ping`；Railway API 健康门部署成功，临时域名 live/ready 为 200，容器内 producer 返回 completed 且独立 Worker 日志记录同一 job，Redis `maxmemory-policy=noeviction`。
- 遗留/人工项：M0-T4 验收项无遗留；Railway 自动 Git 部署留 M0-T6，Sentry 留 M0-T7，Cloudflare staging API 自定义域名、可信代理与真实客户端 IP 留 M0-T10；M0-T0 的商店账号和法律文案等人工项仍未完成。

## 2026-07-22 ｜ M0-T5a（Obytes 入 Monorepo + 版本矩阵 ADR）

- 内容：以 Obytes Starter v9.0.0 为结构基线建立 `@chinasupply/mobile` Expo Development Build 应用壳，删除模板演示认证与业务页面，按冻结技术栈将 Uniwind 替换为 NativeWind；启用 New Architecture，建立 local/staging/production 候选应用标识和英文 i18n 壳，对应 PRD G-2、G-6，并产出移动版本矩阵 ADR。
- 主要文件：新增 `apps/mobile` 的 Expo Router、React Query、Zustand、i18next、MMKV、NativeWind 配置与 7 项单测，新增 `docs/adr/0001-mobile-version-matrix.md`，更新根级 `mobile:check` 和 `pnpm-lock.yaml`；Expo Doctor 要求将 Obytes 声明的 SDK 54 补丁基线对齐到 Expo 54.0.36、Expo Router 6.0.24，未加入 MapLibre、Clerk 或 EAS project ID。
- 验证：`pnpm mobile:check` 通过（Expo Doctor 18/18、TypeScript、Expo public config、iOS/Android export bundle）；`pnpm lint`、`pnpm typecheck`、`pnpm test:unit`（73/73 通过）、`pnpm build`、`pnpm format:check`、`pnpm install --frozen-lockfile` 全部通过；仅存在根目录一个 `pnpm-lock.yaml`，未生成或提交 `ios/`、`android/`。
- 遗留/人工项：M0-T5b 仍需安装 MapLibre RN 11.3.6、完成双端 dev build 及点/Polygon/聚合渲染；M0-T5c 仍需完成 Clerk Expo、共享包导入、EAS monorepo/Preview 构建。候选 Bundle ID/Android package 尚未在商店保留，父任务 M0-T5 保持未勾选。

## 2026-07-22 ｜ M0-T5b（MapLibre 双端构建与地图验证）

- 内容：锁定并接入 MapLibre React Native 11.3.6，在 New Architecture 下以离线 background-only Style 和义乌附近 WGS-84 fixtures 验证点、闭合 Polygon 与聚合点渲染；所有界面字符串走 i18next，并保留 MapTiler/OpenStreetMap 归因，为 PRD G-1、G-2 与 F-1.3 的后续产品实现提供兼容性证据，但未实现业务地图。
- 主要文件：新增 `apps/mobile/src/features/map-spike` 的地图屏幕、Style、GeoJSON fixtures、统一图层配置、加载状态与单测，新增 MapLibre Jest mock；更新 Expo config plugin、移动依赖、锁文件及 `docs/adr/0001-mobile-version-matrix.md`。clean prebuild 解析到 MapLibre Native iOS 6.26.0、Android 13.2.0，生成的 `ios/`、`android/` 保持忽略。
- 验证：`pnpm mobile:check` 通过（Expo Doctor 18/18、TypeScript、public config、iOS/Android export）；`pnpm lint`、`pnpm typecheck`、`pnpm test:unit`（79/79 通过）、`pnpm build`、`pnpm format:check`、`pnpm install --frozen-lockfile` 全部通过。iPhone 17 Pro Simulator（iOS 26.5，Xcode 26.6）原生 build/install/launch 成功；`diaoyouji_api_36`（API 36）Gradle build/install/launch 成功；双端截图均可见点、Polygon、橙色聚合圆、图例、归因及 ready 状态，运行日志无 native crash 或 style/source 加载错误。
- 遗留/人工项：本任务仅证明模拟器双端兼容；物理 iOS/Android 真机、签名、商店标识保留仍是 M0 人工门禁。真实 MapTiler 产品样式归 M0-T10；Clerk Expo、共享包导入与 EAS Preview 归 M0-T5c；父任务 M0-T5 保持未勾选。iOS 存在 React Native `filter` 属性名碰撞引发的非致命诊断，Android 存在 ViewManager setter fallback 警告，需在未来框架升级或真机验证时复查。

## 2026-07-22 ｜ M0-T5c（Clerk Expo、共享包导入与 EAS Preview）

- 内容：接入 Clerk Expo 4.0.1 的邮箱密码与首次设备信任验证码流程，以 SecureStore 中的随机 16 字节密钥加密独立 MMKV token cache；为 `packages/schemas`、`geo`、`i18n` 建立最小 TypeScript 源导出并由 mobile 启动链真实执行，完成 pnpm isolated workspace、Metro 与 EAS 云端解析验证，对应 PRD G-2、G-6。创建并链接 `@huangsourcing/chinasupply-ai`，真实 Android Preview、登录及进程级冷启动 session 恢复全部通过，父任务 M0-T5 已勾选。
- 主要文件：新增 mobile Clerk Provider、登录/设备信任页面、加密 token cache、workspace compatibility 模块、EAS Preview 配置及单测；新增三个共享包的稳定公开接口；更新 mobile env、Expo config、版本矩阵 ADR、移动 README、环境运维文档、开发计划和根锁文件。EAS project ID 为 `cac33d97-75d7-4975-899f-00d661bf979d`，最终 build ID 为 `cf218fc6-750c-4d7c-804b-5082d52e650d`。
- 验证：EAS Android Preview build `FINISHED`，APK 65,288,599 bytes、SHA-256 `4dc18155226bd520d29324c9f254228cb58e02515ac5b52965844d959ce332c3`；安装到 `diaoyouji_api_36` 后无 Metro 完成真实 Clerk 登录和 `424242` device trust，`am force-stop` 后 `LaunchState: COLD` 仍恢复签名会话与 `Offline map ready`，logcat 无 native fatal、React Native、Clerk/session 或 MapLibre style/source 错误。`eas config --platform android --profile preview`、`pnpm mobile:check`（Expo Doctor 18/18、TypeScript、public config、双端 export）、`pnpm lint`、`pnpm typecheck`、`pnpm test:unit`（mobile 25/25）、`pnpm build`、`pnpm format:check`、`pnpm install --frozen-lockfile` 全部通过。
- 遗留/人工项：M0-T5 验收边界已满足，但 M0 出口的物理 iOS/Android 真机地图、商店签名与标识保留仍未满足；OAuth/回跳/账户页留 M3/M4，真实 MapTiler 产品底图留 M0-T10。测试输入法曾短暂显示 smoke 密码，旧值已立即在 Clerk 作废并以新随机值覆盖 macOS Keychain，相关临时截图已删除。

## 2026-07-23 ｜ M0-T6（CI/CD 仓库实现；外部门禁待完成）

- 内容：实现 PR/main 分级 GitHub Actions、Mobile 路径检查、真实 PostGIS/Redis Testcontainers API e2e、CMS staging migration 与 `Staging Release Gate` 依赖链；新增仅 `rc-*`/手动触发的 Android EAS Preview，以及仅 `v*` + EAS 人工批准触发的双端 Production Build/Submit 配置。当前不提前伪造 M1-T1 的 core migration，对应 PRD G-4、G-9 与开发计划 M0-T6。
- 主要文件：新增 `.github/workflows/ci.yml`、API e2e 配置与用例、EAS Preview/Production workflows、`docs/operations/ci-cd.md`；更新 `railway.json` watch paths、迁移边界、环境文档和工作流契约测试。公开命令新增 `pnpm test:e2e`。
- 验证：`pnpm lint`、`pnpm typecheck`、`pnpm test:unit`（95/95）、`pnpm test:e2e`（4/4，真实 `postgis/postgis:17-3.5` + `redis:7.4-alpine`）、`pnpm build`、`pnpm mobile:check`（Expo Doctor 18/18、TypeScript、public config、iOS/Android export）、`pnpm format:check`、`pnpm install --frozen-lockfile` 全部通过；两个 EAS workflow 均通过 `eas workflow:validate`。未触发任何 EAS 云构建或 Submit。
- 遗留/人工项：GitHub 仍为 Free 私有仓库，无法配置计划要求的 Environment secrets、required `CI Gate`/分支保护；因此尚未连接 Railway Git Autodeploy + Wait for CI、尚未启用 Vercel required Deployment Check，也未完成真实 PR/main staging 发布验收，M0-T6 保持未勾选。需先升级 GitHub Pro，且 Apple/Google 商店账号、production EAS 环境与提交凭据仍按 M5-T10 人工前置处理。
- 云端复验：已创建 PR #7；GitHub 接受工作流并生成 `CI Gate` 等 checks，但所有 GitHub-hosted runner 在执行任何 step 前被账户账单状态拦截（GitHub 注解：近期付款失败或 Actions spending limit 需提高）。Vercel Preview 成功，且 PR 未出现 EAS Build/Submit。需先恢复 Actions 额度/账单后才能完成真实 PR CI 验收。

## 2026-07-23 ｜ M0-T6（公开仓库外部接线复验）

- 内容：仓库改为公开后完成 GitHub `staging` Environment、真实 CMS migration、稳定 release gate 与 `main` 分支保护接线；分支保护要求 PR 和 `CI Gate`、对管理员生效并禁止 force push/delete。将 GitHub/Vercel 的 staging 数据库统一到当前 Railway PostGIS public URL，复用 M0-T3 的真实 Payload secret；Railway API/Worker 已连接 `huangsourcing-ux/supplyai:main`，恢复 API `/health/ready` 门禁和两服务 `/railway.json` 配置。
- 主要文件：更新 `docs/operations/ci-cd.md` 的公开仓库能力与外部平台实况；外部配置新增 GitHub Environment secrets/variable 和分支保护，更新 Vercel Production/Preview staging 密钥，并连接 Railway 两项服务。未提交或输出任何密钥。
- 验证：PR #7 的 lint、typecheck、unit、Web/API build、API Testcontainers e2e、Mobile checks 与 `CI Gate` 全绿且未触发 EAS；main Actions run `29986094177` 按 CI → CMS migration → `Staging Release Gate` 全绿。Vercel staging 对应 main commit `1a829e016068bbdf3bfb3aa6350bfe5f79986f3d` 且 `/`、`/admin` 返回 200、`/ops` 正确跳转登录；Railway API/Worker 同一 commit 均为 SUCCESS，`/health/live`、`/health/ready` 成功，SSH 执行 `system:ping` 由 Worker 完成。
- 遗留/人工项：Vercel 当前没有 Deployment Check run，仍需在项目设置中把 `Staging Release Gate` 选为 required；Railway 拒绝创建 deployment trigger，原因是项目成员尚未通过 Railway GitHub App 获得该仓库 contributor access，需人工连接/授权后为 API 和 Worker 启用 Autodeploy + Wait for CI。上述两项完成前 M0-T6 保持未勾选；未创建 tag、未触发 EAS Build/Submit。

## 2026-07-23 ｜ M0-T6（外部门禁完成验收）

- 内容：完成 Vercel required Deployment Check 与 Railway API/Worker GitHub Autodeploy + Wait for CI 接线；修复 Mobile 可选 job 跳过时 reusable CMS migration 被 GitHub 隐式跳过的问题，使 `main` 始终按 CI → CMS migration → `Staging Release Gate` 放行 staging，对应 PRD G-4、G-9 与开发计划 M0-T6。
- 主要文件：更新 `.github/workflows/ci.yml` 与工作流契约测试，补充 `apps/api/README.md`、`docs/operations/ci-cd.md` 并勾选 M0-T6；外部配置新增 Vercel `Staging Release Gate` required Deployment Check，以及 Railway API/Worker 两个 `main` deployment trigger（`checkSuites=true`）。未向 GitHub 写入 Vercel/Railway token，未提交或输出密钥。
- 验证：PR #9 和 #10 的 `CI Gate` 全绿且没有 EAS Build/Submit；main Actions run `29989416236` 成功执行 CMS migration 和 `Staging Release Gate`。Vercel check run `ckr_3fe47e76-31b3-48ab-a14f-74b77d83c605` 为 `succeeded` 且阻断 `deployment-alias`，`staging.chinasupply.ai` 随后切换；Railway API/Worker deployment `33485e90-8d1d-4eac-82c9-85d98e145d84`、`a6e4fcd5-fc66-43d9-bbbf-3a10a170ee0a` 对同一 commit `5a5f7fad9ae07ecb7e376eb458f7c5d002bf1f8f` 均先为 `WAITING`、CI 全绿后转为 `SUCCESS`。`/health/live`、`/health/ready` 返回 200，SSH `system:ping` completed。
- 遗留/人工项：M0-T6 验收项无遗留；未创建 `v*` tag、未执行 EAS Production Build/Submit，Apple/Google 商店凭据与真实商店发布仍按 M5-T10 人工门禁处理。M1-T1 落地真实 Drizzle `db:migrate` 后仍须按计划增加 `core` migration target。

## 2026-07-23 ｜ M0-T7（Sentry 三端验证；外部门禁待完成）

- 内容：按 PRD N-4 为 Web（浏览器/Next.js server/edge）、API/Worker 与 Mobile 接入 Sentry；统一把 local/staging/production 映射为 dev/staging/prod，Web/API release 绑定部署 commit，增加受控三端 smoke exception、flush 与 event ID 输出，并配置 Web 与 Expo/EAS source map 上传路径。未引入业务功能。
- 主要文件：新增三端 Sentry 初始化、异常捕获与测试，新增 `packages/config/env/sentry.js` 的环境/release 约定，更新 Next.js、NestJS、Expo config/Metro、环境 schema、CI 与依赖锁；新增 `docs/operations/sentry.md` 的部署变量、release/source map 规则和外部验收清单。Mobile 锁定含 Metro 0.83 阻塞修复的 Sentry React Native 7.6.0，并以 `getSentryExpoConfig` 生成 debug ID。
- 验证：`pnpm lint`、`pnpm typecheck`、`pnpm test:unit`（101/101）、`pnpm test:e2e`（4/4，真实 PostGIS + Redis）、`pnpm build`、`pnpm mobile:check`（Expo Doctor 18/18、public config、iOS/Android production export）、`pnpm format:check`、`pnpm install --frozen-lockfile` 与 `git diff --check` 全部通过；本地未向 Sentry 发送伪造验收事件。
- 遗留/人工项：Sentry 后台项目 slug/三端 DSN 尚未获得可验证清单；现有 Vercel Web 上传 token 返回 401 且缺 `SENTRY_ORG`/`SENTRY_PROJECT`，M0-T0 的 `org:ci` token 无权枚举项目。需在 Sentry UI 确认或创建 Web/API/Mobile 项目并提供有上传权限的 token，随后配置 Vercel、Railway、EAS，完成同一 staging commit 的三端事件、release、Web/Mobile symbolication 实测。上述外部门禁未完成，因此开发计划 M0-T7 保持未勾选。

## 2026-07-23 ｜ M0-T7（Sentry 三端验证；外部门禁完成）

- 内容：完成 PRD N-4 与开发计划 M0-T7 的外部验收：创建并核验 Web/API/Mobile 三个 Sentry 项目，配置 Vercel、Railway、EAS 的 staging/production 变量，三端真实测试异常均成功进入 Sentry，environment 分别验证为 staging，release 与对应 Web/API commit 及 Android EAS artifact 一致；local/production 到 dev/prod 的映射由配置测试与生产变量检查覆盖，未向 production 发送测试异常。
- 主要文件：补充 `docs/operations/sentry.md` 的真实 event、release、EAS build 与 source map debug ID 证据并勾选 `ChinaSupply.AI开发计划.md` 的 M0-T7；Mobile 增加直接锁定的 `@sentry/cli` 2.58.0，使 EAS Android Gradle 上传任务能在 pnpm monorepo 中找到 CLI。外部行为包括 Vercel Web source map 上传、EAS Mobile bundle/source map 上传，以及 Railway API/Worker Sentry DSN 接线。
- 验证：Web event `3eac560cac1a4c38aabe14c8bb47ad67`、API event `248eb56016844c19be28eca95d65d5db`、Mobile event `8d01e0919bb0430f86034a4df6693d84` 均由 Sentry Events API 返回；Mobile 环境为 `staging`、release 为 `ai.chinasupply.app.staging@0.0.1+1`。EAS Preview build `2686dd8c-97fa-427b-a1b4-679309731768` 成功，日志确认 bundle/source map 上传与 debug ID `6950b9ec-52f1-43bc-91d1-0e12ddfa38e7`；Vercel Preview 日志确认 Next.js production source maps 上传成功。最终仓库检查结果见本任务提交与 PR checks。
- 遗留/人工项：无 M0-T7 验收遗留；生产环境仅检查配置且保持 smoke 开关关闭，未发送故意异常。聊天中暴露过的 Sentry 临时/上传令牌必须在本任务收尾时撤销并由最小权限 `org:ci` 新令牌替换，任何新令牌不得写入仓库或聊天。

## 2026-07-23 ｜ M0-T8（packages/geo 坐标转换）

- 内容：实现无运行时依赖的 WGS-84↔GCJ-02↔BD-09 六向纯函数，统一 `[lng, lat]` 顺序；GCJ-02→WGS-84 使用最多 30 次、`1e-7°` 收敛阈值的高精度迭代反算，中国适用范围外返回新的原值元组，对应 PRD G-1、F-6.2、F-9、N-5。
- 主要文件：新增 `packages/geo/src/coordinates.ts`、`conversions.ts` 与 9 项公开夹具单测，更新包导出、README、Vitest 脚本和锁文件；Mobile Jest/Metro 增加相对 `.js` 说明符到 TypeScript 源码的窄范围解析回退，确保 NodeNext 共享包可被测试与双端 Expo bundler 消费。未实现 `packages/geo/navigation` 或任何导航 URL。
- 验证：`pnpm --filter @chinasupply/geo test:unit`（9/9）、`pnpm lint`、`pnpm typecheck`、`pnpm test:unit`（112/112）、`pnpm build`、`pnpm mobile:check`（Expo Doctor 18/18、iOS/Android export）、`pnpm install --frozen-lockfile`、`pnpm format:check` 与 `git diff --check` 全部通过；北京、上海、深圳 WGS/GCJ 及公开 GCJ/BD 坐标对双向误差均不超过 0.5 米。
- 遗留/人工项：M0-T8 验收项无遗留；M0-T9 的五城市、双平台真机导航验证尚未执行，因此导航目标坐标模式、URL 模板和 `buildNavUrl` 仍未实现，M4 导航继续受门禁阻塞。

## 2026-07-23 ｜ M0-T9（导航验证门）

- 内容：完成北京、上海、义乌、深圳、东莞五城的 iPhone/Android 真机导航门禁，产品负责人批准所有目标统一打开“终点已填充的路线规划页”而非直接开始实时导航；正式规则采用 WGS-84，固化 Apple、Google、高德、百度主链接与 Web fallback，对应 PRD G-1、F-6.1、F-6.2、N-5。
- 主要文件：新增 `packages/geo/src/navigation` 的 `NavigationTarget` 判别联合类型、`buildNavUrl`、五城验证夹具和精确 URL 单测；更新 `@chinasupply/geo` 根导出与 `./navigation` 子路径、README、候选页生成器，以及 `docs/operations/m0-t9-navigation-validation.md`/`m0-t9-navigation-points.json`。Android Apple Maps 在类型层禁止；百度 Web `direction` 真机失败后改用已验证的官方 `marker` →“到这去”回落。
- 验证：`pnpm --filter @chinasupply/geo test:unit`（29/29）、`pnpm lint`、`pnpm typecheck`、`pnpm test:unit`（132/132）、`pnpm build`、`pnpm mobile:check`（Expo Doctor 18/18、iOS/Android export）、`pnpm format:check` 与 `git diff --check` 全部通过；正式候选页无需 `--allow-unconfirmed` 即可生成。
- 遗留/人工项：M0-T9 无遗留；M4-T5 仍必须使用 Release 包重新执行 F-6.4 真机落点 `<50m` 发布验收。连接的 Android 真机未安装 Google Maps，Google 安装态/回落结论来自产品负责人在可用设备和网络上的补充确认，版本号未提供并已在验证结论中如实记录。

## 2026-07-23 ｜ M0-T10（Cloudflare + MapTiler；外部门禁待完成）

- 内容：实现 Cloudflare→Railway 静态边缘密钥信任边界、单值 `CF-Connecting-IP` 校验与 `FastifyRequest.clientIp`，新增 no-store `/health/edge`；将 R2 契约拆为公开媒体/私有操作双桶并改用最小用途 `CLOUDFLARE_PURGE_TOKEN`；移动 MapTiler 拆为 iOS/Android key，并按现有 Bundle ID/Package 派生受限 User-Agent，对应 PRD G-10、G-11。
- 主要文件：新增 `apps/api/src/common/http/edge-proxy.ts`、API 单元/e2e 测试、`apps/mobile/src/config/maptiler-identity.ts`、MapLibre 请求初始化、`infra/cloudflare/r2-cors.staging.json` 和 `docs/operations/cloudflare-maptiler.md`；同步三份已确认文档、`AGENTS.md`、三端 `.env.example`、CI 假值和 Zod schema。外部已创建 `chinasupply-staging-media`、应用精确 CORS、关闭两桶 `r2.dev`，并在 Railway 创建 `api-staging.chinasupply.ai:3001` 域名声明。
- 验证：`pnpm lint`、`pnpm typecheck`、`pnpm test:unit`（API 26/26、Web 35/35、Geo 29/29、Mobile 32/32、Config 24/24）、`pnpm test:e2e`（6/6，真实 PostGIS + Redis）、`R2_MEDIA_BUCKET=chinasupply-dev-media pnpm build`、`pnpm mobile:check`（Expo Doctor 18/18、iOS/Android export）、`pnpm format:check`、`git diff --check` 全部通过。Wrangler 复核媒体 CORS 为唯一 origin、GET/HEAD/PUT、Content-Type、ETag、3600；私有桶无 CORS/自定义域名，两桶 `r2.dev` 均关闭。无环境覆盖的首次 `pnpm build` 如预期拒绝本机旧 `R2_BUCKET`，未改写私有 `.env.local`。
- 遗留/人工项：当前 Cloudflare 账号 API 无法枚举 `chinasupply.ai` zone，按阻塞策略未在其他账号建资源；因此 API CNAME/TXT、SSL Full、Transform Rule、媒体 custom domain、双桶最小权限凭据、Purge token verify 及外部 smoke 未完成。MapTiler 控制台因当前浏览器运行时连接失败，三把受限 key、EAS/Vercel/Railway 加密变量和限制 smoke 未完成。Railway 域名声明仍待 DNS 验证；代码未部署，M0-T10 保持未勾选，Production 仍留 M5-T9。

## 2026-07-23 ｜ M0-T10（Cloudflare 重新授权后接线）

- 内容：重新授权后定位 `chinasupply.ai` 的真实 zone 账号，完成 Cloudflare→Railway staging API DNS 与域名所有权接线；发现并明确旧 R2 桶与 zone 跨账号、不满足 custom domain 前提，未静默复用错误账号资源，对应 PRD G-10、G-11。
- 主要文件：更新 `docs/operations/cloudflare-maptiler.md` 与 `docs/operations/m0-t0-external-accounts.md` 的账号边界和实时状态。外部新增 proxied `api-staging.chinasupply.ai` CNAME 与 `_railway-verify.api-staging.chinasupply.ai` TXT；未删除旧账号 R2 桶。
- 验证：Railway domain status 返回 ownership verified、certificate valid；`https://api-staging.chinasupply.ai/health/live` 返回 200、标准 envelope、`server: cloudflare` 与 `CF-Ray`。`pnpm format:check`、`git diff --check` 通过。
- 遗留/人工项：zone 位于共享账号 `56d1a468b0a9e0095832fc44609dc25f`，该账号尚未启用 R2；旧桶位于个人账号 `0431bd1fc2c8927a1433f273bf43bf5a`，不能绑定目标 zone。当前连接器缺 Zone Settings/Rulesets 写权限，无法设置 SSL Full 和 Transform Rule；浏览器控制运行时仍无法打开 Cloudflare/MapTiler 控制台。M0-T10 继续保持未勾选。

## 2026-07-23 ｜ M0-T10（Cloudflare + MapTiler；外部门禁完成）

- 内容：完成 PRD G-10、G-11 与开发计划 M0-T10 的 staging 代码和外部配置验收：Cloudflare 可信边缘与真实客户端 IP、Railway 直连保护、公开媒体/私有操作 R2 分桶、最小权限 Purge token，以及 Web/iOS/Android 三把受限 MapTiler key；Production 资源仍延后至 M5-T9。
- 主要文件：完成 `apps/api/src/common/http/edge-proxy.ts`、`/health/edge`、双桶/最小权限环境 schema、Mobile 平台 User-Agent 映射和测试；同步 PRD、冻结技术栈、开发计划、`AGENTS.md`、三端环境示例、CI 假值、`docs/operations/cloudflare-maptiler.md` 与 R2 CORS 策略。外部完成 Cloudflare DNS/SSL/Transform Rule、两只正确账号 R2 桶及媒体 CDN、两组分桶凭据、Purge token、Railway API/Worker、Vercel staging 和 EAS Preview 加密变量，以及三把 MapTiler key；未记录任何密钥值。
- 验证：`pnpm lint`、`pnpm typecheck`、`pnpm test:unit`（API 26、Web 35、Geo 29、Mobile 32、Config 24）、`pnpm test:e2e`（6/6）、`R2_MEDIA_BUCKET=chinasupply-staging-media pnpm build`、`pnpm mobile:check`（Expo Doctor 18/18、iOS/Android export）、`pnpm format:check` 与 `git diff --check` 通过。Railway API/Worker 部署成功；Cloudflare live/ready/edge 均为 200，`CF-Ray`、`no-store`、真实出口 IP 匹配，伪造客户端 IP 被拒绝；Railway 直连 live/ready 为 200，edge/业务路径为标准 403 `FORBIDDEN`。R2 精确 origin 预检为 204、错误 origin 为 403、媒体 CDN 读取成功、私有桶不可公开访问且测试对象已删除。三把 MapTiler key 对正确 Origin/User-Agent 为 200，对错误或缺失值为 403。Purge token verify 为 active，未执行真实 purge。
- 遗留/人工项：M0-T10 验收无阻塞；Production 双桶、域名、凭据与 MapTiler production key 仍按 M5-T9 创建。个人旧账号中的同名 R2 桶未被 staging 引用，本任务未获删除旧资源的明确范围，因此保留待单独清理。

## 2026-07-23 ｜ M1-T1（Drizzle schema 与迁移）

- 内容：按 PRD 第 3 节与 G-1/G-3/G-8/G-9 建立 Drizzle 核心数据层，完整定义 9 张核心/关联表、WGS-84 SRID 4326 geometry、21 位 nanoid、timestamptz、JSONB/数组默认值、跨表延迟约束和 3.9 全部索引；新增唯一 `buildSearchText` 入口与严格 JSON Zod schema，`clusterStatsSchema` 明确拒绝 `factoryCount`。落地真实 core `db:migrate`，并把 staging 发布顺序扩展为 CI → CMS migration → Core migration → `Staging Release Gate`。
- 主要文件：新增 `apps/api/src/database/schema.ts`、PostGIS custom types、Drizzle config、`apps/api/drizzle/0000_initial_core.sql` 与 migration journal，并由 `DatabaseService.db` 暴露 typed client；新增 `packages/schemas` 的核心 JSON/search-text 契约及单测、core migration Testcontainers e2e；更新 CI、Railway watch paths、Payload/Core 双向所有权检查和迁移运维文档。Payload collections 与 migration 未被修改。
- 验证：`pnpm install --frozen-lockfile`、`pnpm --filter @chinasupply/api db:check`、`pnpm format:check`、`pnpm lint`、`pnpm typecheck`、`pnpm test:unit`（157/157）、`pnpm test:e2e`（12/12）和 `pnpm build` 全部通过。迁移 e2e 在 `postgis/postgis:17-3.5` 中先执行真实 CMS migration，再执行两次 core migration，验证 extension、表、SRID、索引、Payload 不变、ID/SRID 拒绝、两级类目和 primary-category 双向约束。
- 遗留/人工项：未执行 staging/production migration 或发布；首次合并 `main` 后需记录真实 CMS → Core workflow、`Staging Release Gate`、Railway API/Worker 与健康检查结果。Production migration 仍按 M5-T9 人工门禁执行。
