{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "pnpm exec turbo run build --filter=@chinasupply/api",
    "watchPatterns": [
      "apps/api/**",
      "packages/config/**",
      "packages/geo/**",
      "packages/schemas/**",
      "package.json",
      "pnpm-lock.yaml",
      "pnpm-workspace.yaml",
      "railway.json",
      "turbo.json"
    ]
  },
  "deploy": {
    "drainingSeconds": 30,
    "restartPolicyMaxRetries": 10,
    "restartPolicyType": "ON_FAILURE",
    "startCommand": "pnpm --filter @chinasupply/api start:$SERVICE_ROLE"
  }
}
